{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "timeout": 10,
            "statusMessage": "Checking for session report",
            "command": "b=../SPM-Optimization; [ -d \"$b\" ] || exit 0; d=$(date +%F); ls \"$b/corpus/sessions/vassal-vessels/$d-session-\"*.md >/dev/null 2>&1 || printf '{\"systemMessage\":\"Nothing banked today (%s) for vassal-vessels. Run /bank to write this session into the knowledge store.\"}' \"$d\""
          }
        ]
      }
    ]
  }
}
