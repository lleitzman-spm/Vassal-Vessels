---
type: "guard"
id: "guard:unit-destroyed"
title: "Nothing left to break"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 127
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:unit-destroyed"
---

# Nothing left to break

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Checked before anything else in the machine, because a unit that no longer exists cannot be frightened. Units destroyed in place — as opposed to broken and run down — are what heralds write about, and the deed goes on the record for the house to remember.

*Verified verbatim against `data/guards.json`:127 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `nerve` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `strength as a permille of muster strength < battle.army.destroyedStrengthPermille, or strength is zero` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.army.destroyedStrengthPermille` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-morale.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `if (u.strength <= 0 || idiv(u.strength * 1000, maxi(1, u.musterStrength)) < K.army.destroyedStrengthPermille) {` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Army]] — *its `cites` names `battle.army.destroyedStrengthPermille` by id; this page names `battle.army` literally; +2 more*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### transition

- [[They are destroyed where they stood]] — *its `guards` names `unit-destroyed` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-morale.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:127. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
