---
type: "guard"
id: "guard:grievance-cools"
title: "Time takes the heat out"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 411
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:grievance-cools"
---

# Time takes the heat out

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Grudges fade but never to nothing. The residue is the point: a king who has wronged eleven houses a little, long ago, and fixed none of it, has an army that does not quite work and no single thing to blame. Waiting is a strategy for one grievance and a disaster as a policy.

*Verified verbatim against `data/guards.json`:411 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `grievance` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `after court.loyalty.grievanceCoolAfterYears years, weight falls toward court.loyalty.grievanceCoolShare of what it was` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `court.loyalty.grievanceCoolAfterYears`, `court.loyalty.grievanceCoolShare` |
| `sourcePath` | The file where this condition is really enforced. | `src/court/grievances.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function readGrievances(c: Chronicle, houseId: string, at: Stamp): Grievance[] {` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Court: Loyalty]] — *its `cites` names `court.loyalty.grievanceCoolAfterYears` by id; its `cites` names `court.loyalty.grievanceCoolShare` by id; +3 more*

## Modules

- [[src/court/grievances.ts]] — *this page names `readGrievances`*
- [[src/court/index.ts]] — *this page names `readGrievances`*

## flow

- [[A Grievance]] — *its `flow` names `grievance` by id*

## Backlinks

### transition

- [[It cools]] — *its `guards` names `grievance-cools` by id*
- [[It goes cold]] — *its `guards` names `grievance-cools` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/court/grievances.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:411. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
