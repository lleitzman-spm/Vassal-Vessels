---
type: "guard"
id: "guard:nerve-shakes"
title: "Nerve starts to go"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 45
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:nerve-shakes"
---

# Nerve starts to go

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The first step down. Note the band: a unit must fall clear of steady AND stay clear of wavering to sit here, which is why shaken is a real state a unit can hold for a long time rather than a moment on the way past.

*Verified verbatim against `data/guards.json`:45 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `nerve` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `morale < battle.morale.steadyThreshold - battle.morale.hysteresisMilli, and still at or above battle.morale.shakenThreshold + battle.morale.hysteresisMilli` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.morale.steadyThreshold`, `battle.morale.shakenThreshold`, `battle.morale.hysteresisMilli` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-morale.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `else if (u.morale < K.morale.steadyThreshold - h && u.morale >= K.morale.shakenThreshold + h) setState(state, u, "SHAKEN");` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Morale]] — *its `cites` names `battle.morale.hysteresisMilli` by id; its `cites` names `battle.morale.shakenThreshold` by id; +6 more*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### transition

- [[They begin to feel it]] — *its `guards` names `nerve-shakes` by id*
- [[They get a grip]] — *its `guards` names `nerve-shakes` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-morale.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:45. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
