---
type: "guard"
id: "guard:vessel-runs-dry"
title: "The cup is empty"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 371
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:vessel-runs-dry"
---

# The cup is empty

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The realm-wide default is a number in the constants, but every enfeoffment record may write its own — which is the whole design of the Vessel: the GRANT IS THE CONTRACT. A house that negotiated forty days owes forty, and you agreed to that on a specific day for a specific reason, and it is on the record.

*Verified verbatim against `data/guards.json`:371 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `vessel` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `days served this service year >= court.obligation.serviceDaysPerYear (or whatever the house's own enfeoffment record set instead)` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `court.obligation.serviceDaysPerYear` |
| `sourcePath` | The file where this condition is really enforced. | `src/court/vessel.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function readVessel(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Court: Obligation]] — *its `cites` names `court.obligation.serviceDaysPerYear` by id; this page names `court.obligation` literally; +1 more*

## Modules

- [[src/court/index.ts]] — *this page names `readVessel`*
- [[src/court/vessel.ts]] — *this page names `readVessel`*

## flow

- [[A House's Vessel]] — *its `flow` names `vessel` by id*

## Backlinks

### transition

- [[The answers come back]] — *its `guards` names `vessel-runs-dry` by id*
- [[The cup runs out]] — *its `guards` names `vessel-runs-dry` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/court/vessel.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:371. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
