---
type: "guard"
id: "guard:capture-instead-of-kill"
title: "Taken alive rather than killed"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 351
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:capture-instead-of-kill"
---

# Taken alive rather than killed

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Why a pursuit is worth money and not just corpses. Horsemen catch people; poor horsemen catch people they can sell; tired horsemen catch nobody. This one guard is most of the argument for keeping cavalry in reserve until the enemy breaks.

*Verified verbatim against `data/guards.json`:351 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `captive` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `checked every battle.pursuit.captureCheckEveryTicks ticks at battle.pursuit.captureBasePermille, raised by battle.pursuit.captureMountedBonusPermille for horsemen and by battle.pursuit.captureRansomHungryBonusPermille for men who want the money, divided down by exhaustion at battle.pursuit.captureFatigueDivisor` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.pursuit.captureCheckEveryTicks`, `battle.pursuit.captureBasePermille`, `battle.pursuit.captureMountedBonusPermille`, `battle.pursuit.captureRansomHungryBonusPermille`, `battle.pursuit.captureFatigueDivisor` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/aftermath.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function buildAftermath(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Pursuit]] — *its `cites` names `battle.pursuit.captureBasePermille` by id; its `cites` names `battle.pursuit.captureCheckEveryTicks` by id; +9 more*

## Orders

- [[In Reserve]] — *this page names "In Reserve" literally*

## Modules

- [[src/battle/aftermath.ts]] — *this page names `buildAftermath`*

## flow

- [[A Captive]] — *its `flow` names `captive` by id*

## Backlinks

### transition

- [[A price is named]] — *its `guards` names `capture-instead-of-kill` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/aftermath.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:351. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
