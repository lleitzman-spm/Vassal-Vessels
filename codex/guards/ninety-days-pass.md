---
type: "guard"
id: "guard:season-turns"
title: "Ninety days pass"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 467
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:season-turns"
---

# Ninety days pass

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The clock everything else in the court hangs off. Vessels refill on the service year, grudges cool by the year, seats become customary by the year — so the calendar is not scenery, it is the rate at which every other machine in the court runs.

*Verified verbatim against `data/guards.json`:467 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `year` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `day reaches court.calendar.daysPerSeason; four of them make court.calendar.daysPerYear` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `court.calendar.daysPerSeason`, `court.calendar.seasonsPerYear`, `court.calendar.daysPerYear` |
| `sourcePath` | The file where this condition is really enforced. | `src/court/calendar.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function seasonAt(index: number): SeasonId {` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Court: Calendar]] — *its `cites` names `court.calendar.daysPerSeason` by id; its `cites` names `court.calendar.daysPerYear` by id; +5 more*

## Modules

- [[src/core/contract.ts]] — *this page names `SeasonId`*
- [[src/core/primitives.ts]] — *this page names `SeasonId`*
- [[src/court/calendar.ts]] — *this page names `seasonAt`*
- [[src/court/index.ts]] — *this page names `seasonAt`; this page names `SeasonId`*

## flow

- [[The Year]] — *its `flow` names `year` by id*

## Backlinks

### transition

- [[The cold comes]] — *its `guards` names `season-turns` by id*
- [[The fields come in]] — *its `guards` names `season-turns` by id*
- [[The roads dry]] — *its `guards` names `season-turns` by id*
- [[The service year turns]] — *its `guards` names `season-turns` by id*
- [[The year turns]] — *its `guards` names `season-turns` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/court/calendar.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:467. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
