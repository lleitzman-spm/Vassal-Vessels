---
type: "guard"
id: "guard:rally-enemy-clear"
title: "Nobody close enough to kill them"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 101
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:rally-enemy-clear"
---

# Nobody close enough to kill them

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

You cannot re-form a broken unit with horsemen on top of it, which is precisely why pursuit is so devastating and why reining in your own cavalry hands the enemy his army back. Chase them and they stay broken. Let them go and they may be back in the line in a minute.

*Verified verbatim against `data/guards.json`:101 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `nerve` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `no living enemy unit within battle.morale.rallyEnemyClearMm` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.morale.rallyEnemyClearMm` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-morale.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `if (distUnits(u, e) <= K.morale.rallyEnemyClearMm) {` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Morale]] — *its `cites` names `battle.morale.rallyEnemyClearMm` by id; this page names `battle.morale` literally; +2 more*

## Modules

- [[src/battle/geometry.ts]] — *this page names `distUnits`*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### transition

- [[They turn and stand]] — *its `guards` names `rally-enemy-clear` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-morale.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:101. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
