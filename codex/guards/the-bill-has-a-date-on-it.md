---
type: "guard"
id: "guard:ransom-comes-due"
title: "The bill has a date on it"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 358
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:ransom-comes-due"
---

# The bill has a date on it

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Ransom scales with how important the man is, so capturing a great lord is a windfall and capturing a levy sergeant is barely worth the rope. The date is the sharp part: it does not drift, everyone can see it, and what you do when it arrives is one of the most-watched decisions you will make.

*Verified verbatim against `data/guards.json`:358 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `captive` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `the price is battle.aftermath.ransomCrownsPerStandingPoint per point of the captive's standing, and the day it falls due is fixed when he is taken` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.aftermath.ransomCrownsPerStandingPoint` |
| `sourcePath` | The file where this condition is really enforced. | `src/court/absorb.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function ransomDueBy(taken: Stamp): number {` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Aftermath]] — *its `cites` names `battle.aftermath.ransomCrownsPerStandingPoint` by id; this page names `battle.aftermath` literally; +1 more*

## Modules

- [[src/court/absorb.ts]] — *this page names `ransomDueBy`*

## flow

- [[A Captive]] — *its `flow` names `captive` by id*

## Backlinks

### transition

- [[A price is named]] — *its `guards` names `ransom-comes-due` by id*
- [[The date passes]] — *its `guards` names `ransom-comes-due` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/court/absorb.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:358. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
