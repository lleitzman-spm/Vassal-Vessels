---
type: "guard"
id: "guard:day-runs-out"
title: "The day runs out"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 196
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:day-runs-out"
---

# The day runs out

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The hard stop. A battle that has not decided itself by here is a draw, and both armies go home tired with nothing settled — which, historically, is what most battles that were going to happen and then did not look like.

*Verified verbatim against `data/guards.json`:196 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `battle` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `tick reaches battle.time.maxTicks` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.time.maxTicks`, `battle.time.tickHz`, `battle.time.plannedBattleTicks` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/engine.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `state.phase = "ENDED";` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Time]] — *its `cites` names `battle.time.maxTicks` by id; its `cites` names `battle.time.plannedBattleTicks` by id; +5 more*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## Backlinks

### transition

- [[The day runs out (transition)]] — *its `guards` names `day-runs-out` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/engine.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:196. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
