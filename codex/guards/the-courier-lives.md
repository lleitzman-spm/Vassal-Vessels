---
type: "guard"
id: "guard:courier-gets-through"
title: "The courier lives"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 291
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:courier-gets-through"
---

# The courier lives

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Orders are carried by a man on a horse and the man can be killed. Two things follow that most players learn late: sending an order ACROSS the front is far more dangerous than sending it along your own line, and a captain who is already fighting takes longer to reach because somebody has to get his attention first.

*Verified verbatim against `data/guards.json`:291 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `order` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `the rider crosses the distance at battle.speeds.courierRide without being caught; his danger scales by battle.speeds.courierDangerMulPermille through contested ground, and a recipient already fighting costs battle.speeds.courierRecipientEngagedExtraTicks more` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.speeds.courierRide`, `battle.speeds.courierDangerMulPermille`, `battle.speeds.courierRecipientEngagedExtraTicks` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-command.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function computeHeed(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Speeds]] — *its `cites` names `battle.speeds.courierDangerMulPermille` by id; its `cites` names `battle.speeds.courierRecipientEngagedExtraTicks` by id; +5 more*

## Modules

- [[src/battle/index.ts]] — *this page names `computeHeed`*
- [[src/battle/phase-command.ts]] — *this page names `computeHeed`*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## Backlinks

### transition

- [[He does not arrive]] — *its `guards` names `courier-gets-through` by id*
- [[He reaches the banner]] — *its `guards` names `courier-gets-through` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-command.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:291. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
