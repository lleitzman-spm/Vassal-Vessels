---
type: "guard"
id: "guard:called-past-the-vessel"
title: "Called anyway"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 384
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:called-past-the-vessel"
---

# Called anyway

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Three numbers, getting worse, and that escalation is the entire strategic tension of a long war. Going back to the same well once is a favour asked. Twice is presumption. Three times and you have made an enemy of a man who has done nothing but turn up whenever you called.

*Verified verbatim against `data/guards.json`:384 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `vessel` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `summoned while dry: court.willingness.dryVesselFirst off willingness the first time, court.willingness.dryVesselSecond the second, court.willingness.dryVesselThird thereafter` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `court.willingness.dryVesselFirst`, `court.willingness.dryVesselSecond`, `court.willingness.dryVesselThird`, `court.obligation.beyondTheVesselGrievanceFirst`, `court.obligation.beyondTheVesselGrievanceFurther` |
| `sourcePath` | The file where this condition is really enforced. | `src/court/answer.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function readWillingness(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Court: Obligation]] — *its `cites` names `court.obligation.beyondTheVesselGrievanceFirst` by id; its `cites` names `court.obligation.beyondTheVesselGrievanceFurther` by id; +3 more*
- [[Court: Willingness]] — *its `cites` names `court.willingness.dryVesselFirst` by id; its `cites` names `court.willingness.dryVesselSecond` by id; +5 more*

## Modules

- [[src/court/answer.ts]] — *this page names `readWillingness`*
- [[src/court/index.ts]] — *this page names `readWillingness`*

## flow

- [[A House's Vessel]] — *its `flow` names `vessel` by id*

## Backlinks

### transition

- [[The answers come back]] — *its `guards` names `called-past-the-vessel` by id*
- [[You call them anyway]] — *its `guards` names `called-past-the-vessel` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/court/answer.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:384. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
