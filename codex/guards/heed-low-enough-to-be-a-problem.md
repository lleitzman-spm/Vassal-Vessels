---
type: "guard"
id: "guard:heed-overreaches-or-worse"
title: "Heed low enough to be a problem"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 258
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:heed-overreaches-or-worse"
---

# Heed low enough to be a problem

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Actively unhelpful, in one of two directions. The aggressive version is the dangerous one and the counter-intuitive one: a captain who does not respect you and has blood up does not sit still, he goes much too far forward and takes your right wing with him.

*Verified verbatim against `data/guards.json`:258 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `heed` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `heed >= battle.heed.overreachOrHardHedgeAt and below battle.heed.dragOrHedgeAt; the temper branch at battle.heed.aggressionBranchAt again decides which` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.heed.overreachOrHardHedgeAt`, `battle.heed.dragOrHedgeAt`, `battle.heed.aggressionBranchAt`, `court.obedienceFormula.prideWeight`, `court.obedienceFormula.rivalOfCommander` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-command.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function applyInterpretation(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Heed]] — *its `cites` names `battle.heed.aggressionBranchAt` by id; its `cites` names `battle.heed.dragOrHedgeAt` by id; +5 more*
- [[Court: Obedience Formula]] — *its `cites` names `court.obedienceFormula.prideWeight` by id; its `cites` names `court.obedienceFormula.rivalOfCommander` by id; +3 more*

## Modules

- [[src/battle/index.ts]] — *this page names `applyInterpretation`*
- [[src/battle/phase-command.ts]] — *this page names `applyInterpretation`*

## flow

- [[The Captain's Heed]] — *its `flow` names `heed` by id*

## Backlinks

### transition

- [["Follow me!"]] — *its `guards` names `heed-overreaches-or-worse` by id*
- [[A gesture, and nothing more]] — *its `guards` names `heed-overreaches-or-worse` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-command.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:258. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
