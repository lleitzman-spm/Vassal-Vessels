---
type: "guard"
id: "guard:banner-breaks-army"
title: "The banner falls at the wrong moment"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 155
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:banner-breaks-army"
---

# The banner falls at the wrong moment

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

A banner going down is bad for everyone who can see it. If the army was already unhappy when it happened, that is the whole thing over — this single guard is why the banner is worth guarding and why sending it forward to inspire the line is a gamble rather than a free bonus.

*Verified verbatim against `data/guards.json`:155 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `battle` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `the army's banner is lost while army morale is under battle.army.breakOnBannerLostMoraleUnder` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.army.breakOnBannerLostMoraleUnder`, `battle.army.onBreakMoraleHit` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-morale.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `f.morale = clamp(f.morale + K.morale.pBannerLost, f.moraleFloor, f.moraleCeiling);` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Army]] — *its `cites` names `battle.army.breakOnBannerLostMoraleUnder` by id; its `cites` names `battle.army.onBreakMoraleHit` by id; +3 more*
- [[Battle: Morale]] — *this page names `K.morale` literally; this page names `pBannerLost`*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## Backlinks

### transition

- [[An army comes apart]] — *its `guards` names `banner-breaks-army` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-morale.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:155. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
