---
type: "guard"
id: "guard:gathering-done"
title: "The men are together"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 472
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:gathering-done"
---

# The men are together

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Before anybody can march anywhere, a lord has to collect his men off their farms. A tight, well-run holding does it fast; a scattered one takes nearly twice as long — and every one of those days is spent before a single league is covered. Compact land is worth more than its acreage suggests.

*Verified verbatim against `data/guards.json`:472 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `muster` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `between court.march.gatherDaysMin and court.march.gatherDaysMax days, depending on how scattered the holding is` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `court.march.gatherDaysMin`, `court.march.gatherDaysMax` |
| `sourcePath` | The file where this condition is really enforced. | `src/court/distance.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function gatherDaysFor(holding: HoldingRecord): number {` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Court: March]] — *its `cites` names `court.march.gatherDaysMax` by id; its `cites` names `court.march.gatherDaysMin` by id; +3 more*

## Modules

- [[src/court/distance.ts]] — *this page names `gatherDaysFor`*
- [[src/court/index.ts]] — *this page names `HoldingRecord`*
- [[src/court/records.ts]] — *this page names `HoldingRecord`*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## Backlinks

### transition

- [[Lords collect their men]] — *its `guards` names `gathering-done` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/court/distance.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:472. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
