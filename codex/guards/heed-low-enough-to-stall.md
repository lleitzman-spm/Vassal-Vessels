---
type: "guard"
id: "guard:heed-drags-or-hedges"
title: "Heed low enough to stall"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 241
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:heed-drags-or-hedges"
---

# Heed low enough to stall

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The band where a captain is unwilling but not defiant — and his TEMPER decides how that comes out. A cautious man drags his feet; a bolder one commits half and holds the rest. Same unwillingness, two completely different problems on your left flank.

*Verified verbatim against `data/guards.json`:241 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `heed` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `heed >= battle.heed.dragOrHedgeAt and below battle.heed.obeyHisWayAt; which of the two you get turns on the captain's temper at battle.heed.aggressionBranchAt` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.heed.dragOrHedgeAt`, `battle.heed.obeyHisWayAt`, `battle.heed.aggressionBranchAt`, `court.obedienceFormula.prideWeight`, `court.obedienceFormula.grievanceWeight` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-command.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function computeHeed(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Heed]] — *its `cites` names `battle.heed.aggressionBranchAt` by id; its `cites` names `battle.heed.dragOrHedgeAt` by id; +5 more*
- [[Court: Obedience Formula]] — *its `cites` names `court.obedienceFormula.grievanceWeight` by id; its `cites` names `court.obedienceFormula.prideWeight` by id; +3 more*
- [[Court: Resolve Formula]] — *this page names `grievanceWeight`*
- [[Court: Treachery Formula]] — *this page names `grievanceWeight`*

## Modules

- [[src/battle/index.ts]] — *this page names `computeHeed`*
- [[src/battle/phase-command.ts]] — *this page names `computeHeed`*

## flow

- [[The Captain's Heed]] — *its `flow` names `heed` by id*

## Backlinks

### transition

- [["Presently."]] — *its `guards` names `heed-drags-or-hedges` by id*
- [["Some of us, then."]] — *its `guards` names `heed-drags-or-hedges` by id*
- [[It becomes their job]] — *its `guards` names `heed-drags-or-hedges` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-command.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:241. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
