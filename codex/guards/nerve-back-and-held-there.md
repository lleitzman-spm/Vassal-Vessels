---
type: "guard"
id: "guard:rally-nerve-held"
title: "Nerve back, and held there"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 87
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:rally-nerve-held"
---

# Nerve back, and held there

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Not a moment of courage — a sustained one. The men have to be above the line and STAY above it, and a single dip resets the clock to nothing. This is what stops a routing unit bouncing back the instant it gets one step of daylight.

*Verified verbatim against `data/guards.json`:87 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `nerve` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `morale > battle.morale.rallyThreshold continuously for battle.morale.rallyHoldTicks ticks` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.morale.rallyThreshold`, `battle.morale.rallyHoldTicks` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-morale.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `const heldLongEnough = u.aboveRallySince >= 0 && state.tick - u.aboveRallySince >= K.morale.rallyHoldTicks;` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Morale]] — *its `cites` names `battle.morale.rallyHoldTicks` by id; its `cites` names `battle.morale.rallyThreshold` by id; +4 more*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### transition

- [[The running stops]] — *its `guards` names `rally-nerve-held` by id*
- [[They re-form]] — *its `guards` names `rally-nerve-held` by id*
- [[They turn and stand]] — *its `guards` names `rally-nerve-held` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-morale.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:87. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
