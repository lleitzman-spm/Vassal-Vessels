---
type: "guard"
id: "guard:seat-becomes-customary"
title: "It starts to feel like his"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 438
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:seat-becomes-customary"
---

# It starts to feel like his

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Nothing happens on this day. No message, no ceremony — the cost of a decision you have not made yet quietly goes up. This is the most easily missed clock in the game, and the reason to look at your council table occasionally even when everything is fine.

*Verified verbatim against `data/guards.json`:438 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `seat` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `held continuously for court.seats.customaryAfterYears years; taking it back now costs court.seats.customaryGrievance` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `court.seats.customaryAfterYears`, `court.seats.customaryGrievance` |
| `sourcePath` | The file where this condition is really enforced. | `src/court/seats.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function readSeat(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Court: Seats]] — *its `cites` names `court.seats.customaryAfterYears` by id; its `cites` names `court.seats.customaryGrievance` by id; +3 more*

## Modules

- [[src/court/index.ts]] — *this page names `readSeat`*
- [[src/court/seats.ts]] — *this page names `readSeat`*

## flow

- [[A Seat at Court]] — *its `flow` names `seat` by id*

## Backlinks

### transition

- [[It becomes customary]] — *its `guards` names `seat-becomes-customary` by id*
- [[You take it back]] — *its `guards` names `seat-becomes-customary` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/court/seats.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:438. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
