---
type: "guard"
id: "guard:treachery-acts"
title: "High enough to work against you"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 312
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:treachery-acts"
---

# High enough to work against you

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The first stage, and the one you can still do something about. Nothing is announced. Orders go unheeded a little more often than luck explains, and a gap appears where a gap should not be. Every point of the number came from a record you can go and read.

*Verified verbatim against `data/guards.json`:312 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `treachery` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `treachery > battle.treachery.actsAboveTreachery, re-checked every battle.treachery.checkEveryTicks ticks` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.treachery.actsAboveTreachery`, `battle.treachery.checkEveryTicks`, `battle.treachery.permillePerPointOver30` |
| `sourcePath` | The file where this condition is really enforced. | `src/court/host.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function computeTreachery(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Treachery]] — *its `cites` names `battle.treachery.actsAboveTreachery` by id; its `cites` names `battle.treachery.checkEveryTicks` by id; +5 more*

## Modules

- [[src/court/host.ts]] — *this page names `computeTreachery`*
- [[src/court/index.ts]] — *this page names `computeTreachery`*

## flow

- [[Turning]] — *its `flow` names `treachery` by id*

## Backlinks

### transition

- [[Something changes in them]] — *its `guards` names `treachery-acts` by id*
- [[They start working against you]] — *its `guards` names `treachery-acts` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/court/host.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:312. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
