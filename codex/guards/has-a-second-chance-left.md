---
type: "guard"
id: "guard:rallies-left"
title: "Has a second chance left"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 74
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:rallies-left"
---

# Has a second chance left

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

One. That is the whole number. A unit that broke, was talked back into the line, and then broke again is finished for the day no matter what you do — and knowing that changes how you spend a rallied unit. It came back SHAKEN, it will break more easily than it did the first time, and there is nothing behind it.

*Verified verbatim against `data/guards.json`:74 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `nerve` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `ralliesUsed < battle.morale.ralliesAllowed` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.morale.ralliesAllowed` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-morale.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `// A unit rallies at most ONCE. Twice-broken men are done for the day.` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Morale]] — *its `cites` names `battle.morale.ralliesAllowed` by id; this page names `battle.morale` literally; +1 more*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### transition

- [[They are gone]] — *its `guards` names `rallies-left` by id*
- [[They turn and stand]] — *its `guards` names `rallies-left` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-morale.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:74. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
