---
type: "guard"
id: "guard:army-breaks"
title: "The army breaks"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 140
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:army-breaks"
---

# The army breaks

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The moment a battle stops being a battle. Two roads lead here: enough units have physically broken, or the whole army has been miserable for long enough. The second one is the quiet killer — an army that is losing slowly and knows it will break without any single disaster you can point at.

*Verified verbatim against `data/guards.json`:140 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `battle` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `routed men exceed battle.army.breakRoutedFractionPermille of the army — or army morale sits under battle.army.breakArmyMoraleThreshold for battle.army.breakArmyMoraleTicks ticks` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.army.breakRoutedFractionPermille`, `battle.army.breakArmyMoraleThreshold`, `battle.army.breakArmyMoraleTicks` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-army.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `state.phase = "PURSUIT";` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Army]] — *its `cites` names `battle.army.breakArmyMoraleThreshold` by id; its `cites` names `battle.army.breakArmyMoraleTicks` by id; +5 more*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## Backlinks

### transition

- [[An army comes apart]] — *its `guards` names `army-breaks` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-army.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:140. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
