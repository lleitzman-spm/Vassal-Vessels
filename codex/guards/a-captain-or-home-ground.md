---
type: "guard"
id: "guard:rally-someone-to-rally-to"
title: "A captain, or home ground"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 114
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:rally-someone-to-rally-to"
---

# A captain, or home ground

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Frightened men re-form around something. Either a captain they know is near enough to see, or the ground they started from. This is the clearest reason to keep a captain alive and behind his line rather than heroically in front of it: a dead captain does not just cost his contingent nerve, he costs it the ability to come back.

*Verified verbatim against `data/guards.json`:114 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `nerve` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `captain's banner alive and within battle.morale.rallyCaptainRadiusMm — or the unit has run all the way back to its own baseline` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.morale.rallyCaptainRadiusMm` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-morale.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `if (!captainNear && !atOwnBaseline) return;` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Morale]] — *its `cites` names `battle.morale.rallyCaptainRadiusMm` by id; this page names `battle.morale` literally; +1 more*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### transition

- [[They turn and stand]] — *its `guards` names `rally-someone-to-rally-to` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-morale.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:114. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
