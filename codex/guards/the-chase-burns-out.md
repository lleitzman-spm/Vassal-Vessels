---
type: "guard"
id: "guard:pursuit-spent"
title: "The chase burns out"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 169
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:pursuit-spent"
---

# The chase burns out

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Chases end on their own. Horses tire, the light goes, the runners scatter into country nobody can follow them into. The length is not fixed — a battle whose pursuit ended early and one that ran long are meaningfully different days, and the difference lands in the ransom column.

*Verified verbatim against `data/guards.json`:169 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `battle` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `the pursuit has run for between battle.pursuit.phaseTicksMin and battle.pursuit.phaseTicksMax ticks` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.pursuit.phaseTicksMin`, `battle.pursuit.phaseTicksMax` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-army.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `state.phase = "ENDED";` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Pursuit]] — *its `cites` names `battle.pursuit.phaseTicksMax` by id; its `cites` names `battle.pursuit.phaseTicksMin` by id; +3 more*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## Backlinks

### transition

- [[The chase burns out (transition)]] — *its `guards` names `pursuit-spent` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-army.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:169. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
