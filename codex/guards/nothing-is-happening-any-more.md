---
type: "guard"
id: "guard:nobody-left-fighting"
title: "Nothing is happening any more"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 183
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:nobody-left-fighting"
---

# Nothing is happening any more

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Both sides have disengaged and nobody is closing. Someone has withdrawn in good order, which is a legitimate and badly underrated outcome — you keep your army, and an army you still have next season is worth a great deal more than a field you held for an afternoon.

*Verified verbatim against `data/guards.json`:183 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `battle` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | yes |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `no contact anywhere for battle.army.retreatNoContactEndTicks ticks` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.army.retreatNoContactEndTicks` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-army.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `state.phase = "ENDED";` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Army]] — *its `cites` names `battle.army.retreatNoContactEndTicks` by id; this page names `battle.army` literally; +1 more*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## Backlinks

### transition

- [[Both sides break contact]] — *its `guards` names `nobody-left-fighting` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-army.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:183. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
