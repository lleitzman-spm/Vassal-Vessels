---
type: "guard"
id: "guard:heed-obeys"
title: "Heed high enough to simply obey"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 211
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:heed-obeys"
---

# Heed high enough to simply obey

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The top band. Everything you did at court over the last several years is trying to push this one number up: settled grudges, paid wages, a seat given to the right man, a legitimate cause proclaimed. Get it here and orders simply work, which is a luxury you have to buy.

*Verified verbatim against `data/guards.json`:211 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `heed` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `heed >= battle.heed.obeyAt` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `battle.heed.obeyAt` |
| `sourcePath` | The file where this condition is really enforced. | `src/battle/phase-command.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function computeHeed(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Battle: Heed]] — *its `cites` names `battle.heed.obeyAt` by id; this page names `battle.heed` literally; +1 more*

## Modules

- [[src/battle/index.ts]] — *this page names `computeHeed`*
- [[src/battle/phase-command.ts]] — *this page names `computeHeed`*

## flow

- [[The Captain's Heed]] — *its `flow` names `heed` by id*

## Backlinks

### transition

- [["At once."]] — *its `guards` names `heed-obeys` by id*
- [[It becomes their job]] — *its `guards` names `heed-obeys` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/phase-command.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:211. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
