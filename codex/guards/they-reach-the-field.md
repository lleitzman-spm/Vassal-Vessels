---
type: "guard"
id: "guard:host-arrives"
title: "They reach the field"
standing: "built"
standing_source: "derived"
source_path: "data/guards.json"
source_line: 486
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "guard:host-arrives"
---

# They reach the field

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Distance is not a delay, it is a TAX. Men who walked a long way on bad roads arrive with fatigue already in their legs, and fatigue lowers the ceiling on how brave they can get. An army that marched hard to be there starts the battle less able to win it, and that is decided long before anyone draws anything.

*Verified verbatim against `data/guards.json`:486 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this guard belongs to (data/flows.json). | `muster` |
| `sharp` | True when the condition has NO hysteresis and no grace — it fires the instant it is met. Worth marking, because a sharp guard is a cliff and a soft one is a slope, and players need to know which they are standing on. | no |
| `test` | The condition itself, written as close to arithmetic as plain words allow. This is the sentence a reader should be able to check against the numbers on the constant's own page. | `leagues covered at court.march.leaguesPerDayFoot (or court.march.leaguesPerDayHorse), scaled by the road at court.march.roadFactorHighway, roadFactorTrack or roadFactorPath, wearing men down by court.wear.dailyWearFoot each day` |
| `cites` | The constant paths this guard actually reads, dotted, exactly as they appear in data/constants.json. THIS IS THE LOAD-BEARING FIELD — it is what makes a guard checkable rather than decorative, and the lint is fatal on a path that is not there. | `court.march.leaguesPerDayFoot`, `court.march.leaguesPerDayHorse`, `court.march.roadFactorHighway`, `court.march.roadFactorTrack`, `court.march.roadFactorPath`, `court.wear.dailyWearFoot` |
| `sourcePath` | The file where this condition is really enforced. | `src/court/distance.ts` |
| `quote` | A verbatim line from sourcePath. No quote, no object. | `export function journey(` |

*Shelf: `guards` in `data/guards.json`.*

## Governing numbers

- [[Court: March]] — *its `cites` names `court.march.leaguesPerDayFoot` by id; its `cites` names `court.march.leaguesPerDayHorse` by id; +9 more*
- [[Court: Wear]] — *its `cites` names `court.wear.dailyWearFoot` by id; this page names `court.wear` literally; +1 more*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## Backlinks

### transition

- [[The host stands]] — *its `guards` names `host-arrives` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/court/distance.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/guards.json`:486. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
