---
type: "transition"
id: "transition:order-refused"
title: "It is refused"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 513
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:order-refused"
---

# It is refused

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The captain will not do it. The contingent falls back on its standing charge and carries on with whatever it was already doing.

*Verified verbatim against `data/transitions.json`:513 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `order` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `order-arrived` |
| `to` | The place the case arrives in. Always exactly one. | `order-abandoned` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the heed resolving to defiance` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `heed-defies` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the order, and a great deal of face` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Charge]] — *this page names "Charge" literally*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## guard

- [[Heed gone entirely]] — *its `guards` names `heed-defies` by id*

## place

- [[Abandoned (place)]] — *its `to` names `order-abandoned` by id*
- [[Arrived]] — *its `from` names `order-arrived` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:513. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
