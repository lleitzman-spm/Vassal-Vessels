---
type: "transition"
id: "transition:battle-disengage"
title: "Both sides break contact"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 82
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:battle-disengage"
---

# Both sides break contact

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Somebody withdrew in good order and nobody chased. A perfectly respectable outcome that players routinely undervalue: you still have an army, and an army you still have next season is worth more than a field you held for an afternoon.

*Verified verbatim against `data/transitions.json`:82 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `battle` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `battle-joined` |
| `to` | The place the case arrives in. Always exactly one. | `battle-ended` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `no contact anywhere, sustained` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `nobody-left-fighting` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `WITHDRAW`, `FALL_BACK` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Fall Back]] — *its `consumes` names `FALL_BACK` by id*
- [[Withdraw]] — *its `consumes` names `WITHDRAW` by id; this page names "Withdraw" literally*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## guard

- [[Nothing is happening any more]] — *its `guards` names `nobody-left-fighting` by id*

## place

- [[Battle Joined]] — *its `from` names `battle-joined` by id*
- [[Ended]] — *its `to` names `battle-ended` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:82. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
