---
type: "transition"
id: "transition:nerve-breaks"
title: "They break"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 133
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:nerve-breaks"
---

# They break

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The unit stops being a unit. Note the origins: a STEADY unit can break outright without passing through the middle states, if enough happens at once — a flank charge into a fresh line does exactly that. This is the one arrow in the machine with no cushion on it at all.

*Verified verbatim against `data/transitions.json`:133 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `nerve` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `nerve-steady`, `nerve-shaken`, `nerve-wavering` |
| `to` | The place the case arrives in. Always exactly one. | `nerve-routing` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the morale check, every tick` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `nerve-breaks` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `all cohesion, the current order, and any posture — thrown away at once` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Charge]] — *this page names "Charge" literally*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## guard

- [[Nerve gives out]] — *its `guards` names `nerve-breaks` by id*

## place

- [[Routing]] — *its `to` names `nerve-routing` by id*
- [[Shaken]] — *its `from` names `nerve-shaken` by id*
- [[Steady]] — *its `from` names `nerve-steady` by id*
- [[Wavering]] — *its `from` names `nerve-wavering` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:133. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
