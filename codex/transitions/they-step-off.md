---
type: "transition"
id: "transition:pace-move-off"
title: "They step off"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 383
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:pace-move-off"
---

# They step off

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Moving at last. Cheap in every currency, and the pace a well-handled army spends most of its day at.

*Verified verbatim against `data/transitions.json`:383 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `pace` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `pace-stop` |
| `to` | The place the case arrives in. Always exactly one. | `pace-walk` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `any movement order` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `cohesion recovery stops` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `ADVANCE_TO`, `MARCH`, `column` |

*Shelf: `transitions` in `data/transitions.json`.*

## Formations

- [[Column of March]] — *its `consumes` names `column` by id*

## Orders

- [[Advance To]] — *its `consumes` names `ADVANCE_TO` by id*
- [[March]] — *its `consumes` names `MARCH` by id*

## flow

- [[A Unit's Pace]] — *its `flow` names `pace` by id*

## place

- [[Halted]] — *its `from` names `pace-stop` by id*
- [[Walking]] — *its `to` names `pace-walk` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:383. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
