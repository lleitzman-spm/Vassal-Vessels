---
type: "transition"
id: "transition:order-courier-killed"
title: "He does not arrive"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 677
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:order-courier-killed"
---

# He does not arrive

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Killed on the way, and nobody tells you. From where you stand, an order that never arrived looks exactly like an order that was defied — and that ambiguity is deliberate, because it was true.

*Verified verbatim against `data/transitions.json`:677 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `order` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `order-riding` |
| `to` | The place the case arrives in. Always exactly one. | `order-lost` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the courier being caught` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `courier-gets-through` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the order, silently` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `RIDE_TO` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Move the Banner]] — *its `consumes` names `RIDE_TO` by id*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## guard

- [[The courier lives]] — *its `guards` names `courier-gets-through` by id*

## place

- [[Never Arrived]] — *its `to` names `order-lost` by id*
- [[Riding]] — *its `from` names `order-riding` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:677. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
