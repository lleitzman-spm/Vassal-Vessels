---
type: "transition"
id: "transition:order-arrive"
title: "He reaches the banner"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 656
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:order-arrive"
---

# He reaches the banner

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

It got there. The distance you sent it across has already been paid in seconds, and in a battle seconds are the currency that matters.

*Verified verbatim against `data/transitions.json`:656 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `order` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `order-riding` |
| `to` | The place the case arrives in. Always exactly one. | `order-arrived` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the ride completing` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `courier-gets-through`, `horn-carries` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the time it took, which is the whole problem` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `HORN`, `HORN_SOUNDED`, `BIND_PLAN`, `wits`, `ENEMY_WITHIN` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Seal a Standing Plan]] — *its `consumes` names `BIND_PLAN` by id*
- [[Sound the Horn]] — *its `consumes` names `HORN` by id*

## Standing plans

- [[ENEMY_WITHIN]] — *its `consumes` names `ENEMY_WITHIN` by id; this page names "ENEMY_WITHIN" literally*
- [[HORN_SOUNDED]] — *its `consumes` names `HORN_SOUNDED` by id; this page names "HORN_SOUNDED" literally*

## Traits

- [[Wits]] — *its `consumes` names `wits` by id*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## guard

- [[The courier lives]] — *its `guards` names `courier-gets-through` by id*
- [[The horn is heard]] — *its `guards` names `horn-carries` by id*

## place

- [[Arrived]] — *its `to` names `order-arrived` by id*
- [[Riding]] — *its `from` names `order-riding` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:656. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
