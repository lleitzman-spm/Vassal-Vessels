---
type: "transition"
id: "transition:muster-stand"
title: "The host stands"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 713
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:muster-stand"
---

# The host stands

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Everyone who is coming has come. This is the moment the court half of the game is scored — and the army is always smaller than the one you imagined, because it always is.

*Verified verbatim against `data/transitions.json`:713 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `muster` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `muster-marching` |
| `to` | The place the case arrives in. Always exactly one. | `muster-stood` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `arrival at the appointed place` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `host-arrives` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `whatever the roads took` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## guard

- [[They reach the field]] — *its `guards` names `host-arrives` by id*

## place

- [[On the Road]] — *its `from` names `muster-marching` by id*
- [[The Host Stands]] — *its `to` names `muster-stood` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:713. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
