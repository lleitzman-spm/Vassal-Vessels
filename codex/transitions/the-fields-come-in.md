---
type: "transition"
id: "transition:year-to-harvest"
title: "The fields come in"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 614
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:year-to-harvest"
---

# The fields come in

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Into autumn. Food is everywhere and the army can feed itself off the country — and every farmer you call up now will remember it for years.

*Verified verbatim against `data/transitions.json`:614 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `year` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `year-highsun` |
| `to` | The place the case arrives in. Always exactly one. | `year-harvest` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `ninety days passing` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `season-turns` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `an idle summer costs standing` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[The Year]] — *its `flow` names `year` by id*

## guard

- [[Ninety days pass]] — *its `guards` names `season-turns` by id*

## place

- [[Harvest]] — *its `to` names `year-harvest` by id*
- [[Highsun]] — *its `from` names `year-highsun` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:614. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
