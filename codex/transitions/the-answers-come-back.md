---
type: "transition"
id: "transition:muster-answers"
title: "The answers come back"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 939
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:muster-answers"
---

# The answers come back

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

No dice anywhere. Every answer is computed from loyalty, cause, season, grudges, distance and how much service is left in the cup — all of it visible beforehand if you looked. Whoever is missing, you can name the day you lost them.

*Verified verbatim against `data/transitions.json`:939 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `muster` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `muster-summoned` |
| `to` | The place the case arrives in. Always exactly one. | `muster-answered` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `each house's willingness being read` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `vessel-runs-dry`, `called-past-the-vessel` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `willingness, wherever a cup was dry` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `more-than-owed`, `in-full`, `near-full`, `short`, `late-and-short`, `deputy`, `scutage`, `token`, `refusal`, `turncoat` |

*Shelf: `transitions` in `data/transitions.json`.*

## Answers

- [[Comes in full]] — *its `consumes` names `in-full` by id*
- [[Comes late and short]] — *its `consumes` names `late-and-short` by id*
- [[Comes near full]] — *its `consumes` names `near-full` by id*
- [[Comes short]] — *its `consumes` names `short` by id*
- [[Comes with more than he owes]] — *its `consumes` names `more-than-owed` by id*
- [[Refuses]] — *its `consumes` names `refusal` by id*
- [[Sends a deputy]] — *its `consumes` names `deputy` by id*
- [[Sends a token]] — *its `consumes` names `token` by id*
- [[Sends coin instead]] — *its `consumes` names `scutage` by id*
- [[Turns his coat]] — *its `consumes` names `turncoat` by id*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## guard

- [[Called anyway]] — *its `guards` names `called-past-the-vessel` by id*
- [[The cup is empty]] — *its `guards` names `vessel-runs-dry` by id*

## place

- [[Answers In]] — *its `to` names `muster-answered` by id*
- [[Houses Summoned]] — *its `from` names `muster-summoned` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:939. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
