---
type: "transition"
id: "transition:order-accepted"
title: "It becomes their job"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 693
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:order-accepted"
---

# It becomes their job

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Accepted in SOME form — which may not be the form you sent. From here it is the contingent's standing job until something ends it.

*Verified verbatim against `data/transitions.json`:693 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `order` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `order-arrived` |
| `to` | The place the case arrives in. Always exactly one. | `order-active` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the heed resolving to any form of compliance` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `heed-obeys`, `heed-his-way`, `heed-drags-or-hedges` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `whatever the interpretation cost you` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `SET_CHARGE` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Change the Standing Job]] — *its `consumes` names `SET_CHARGE` by id*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## guard

- [[Heed enough to comply, not to conform]] — *its `guards` names `heed-his-way` by id*
- [[Heed high enough to simply obey]] — *its `guards` names `heed-obeys` by id*
- [[Heed low enough to stall]] — *its `guards` names `heed-drags-or-hedges` by id*

## place

- [[Arrived]] — *its `from` names `order-arrived` by id*
- [[In Force]] — *its `to` names `order-active` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:693. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
