---
type: "transition"
id: "transition:footing-cease"
title: "Hold!"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 339
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:footing-cease"
---

# Hold!

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Stop shooting. Worth ordering deliberately rather than waiting for the quiver to empty, because ammunition spent on a target that was going to lose anyway is ammunition you do not have when the real threat arrives.

*Verified verbatim against `data/transitions.json`:339 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `footing` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `footing-shooting` |
| `to` | The place the case arrives in. Always exactly one. | `footing-none` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `a cease order, running dry, or nothing left worth shooting at` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `HOLD_FIRE`, `AMMO_BELOW` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Hold Your Arrows]] — *its `consumes` names `HOLD_FIRE` by id*

## Standing plans

- [[AMMO_BELOW]] — *its `consumes` names `AMMO_BELOW` by id; this page names "AMMO_BELOW" literally*

## flow

- [[A Unit's Footing]] — *its `flow` names `footing` by id*

## place

- [[Loose Footing]] — *its `to` names `footing-none` by id*
- [[Shooting]] — *its `from` names `footing-shooting` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:339. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
