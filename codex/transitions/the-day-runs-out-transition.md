---
type: "transition"
id: "transition:battle-timeout"
title: "The day runs out (transition)"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 68
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:battle-timeout"
---

# The day runs out (transition)

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

A draw. Neither side broke, neither side left, and the light went. Both armies go home tired with nothing settled and every grudge that got them there still in place.

*Verified verbatim against `data/transitions.json`:68 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `battle` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `battle-joined` |
| `to` | The place the case arrives in. Always exactly one. | `battle-ended` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the tick ceiling` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `day-runs-out` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `both armies keep their fatigue and their dead` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## guard

- [[The day runs out]] — *its `guards` names `day-runs-out` by id*

## place

- [[Battle Joined]] — *its `from` names `battle-joined` by id*
- [[Ended]] — *its `to` names `battle-ended` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:68. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
