---
type: "transition"
id: "transition:order-superseded"
title: "It stops mattering"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 739
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:order-superseded"
---

# It stops mattering

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Overtaken by events. Not a failure so much as a fact — and a reminder that an order you sent ninety seconds ago was aimed at a battle that no longer exists.

*Verified verbatim against `data/transitions.json`:739 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `order` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `order-active` |
| `to` | The place the case arrives in. Always exactly one. | `order-abandoned` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `a newer order arriving, the target being gone, or the unit breaking` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `SET_CHARGE`, `HOLD` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Change the Standing Job]] — *its `consumes` names `SET_CHARGE` by id*
- [[Hold Ground]] — *its `consumes` names `HOLD` by id*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## place

- [[Abandoned (place)]] — *its `to` names `order-abandoned` by id*
- [[In Force]] — *its `from` names `order-active` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:739. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
