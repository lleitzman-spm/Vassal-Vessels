---
type: "transition"
id: "transition:footing-shoot"
title: "Loose!"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 233
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:footing-shoot"
---

# Loose!

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Begin shooting. Every arrow is a real object out of a finite quiver, and a unit that is shooting is not bracing — which archers rediscover the moment horsemen appear on their flank.

*Verified verbatim against `data/transitions.json`:233 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `footing` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `footing-none` |
| `to` | The place the case arrives in. Always exactly one. | `footing-shooting` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `a shoot order, or a plan with a target in range` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `ammunition, which does not come back` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[A Unit's Footing]] — *its `flow` names `footing` by id*

## place

- [[Loose Footing]] — *its `from` names `footing-none` by id*
- [[Shooting]] — *its `to` names `footing-shooting` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:233. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
