---
type: "transition"
id: "transition:captive-price"
title: "A price is named"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 1252
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:captive-price"
---

# A price is named

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Scaled to the man's standing, with a due date fixed the moment he was taken. A great lord is a windfall; a levy sergeant is barely worth the rope.

*Verified verbatim against `data/transitions.json`:1252 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `captive` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `captive-taken` |
| `to` | The place the case arrives in. Always exactly one. | `captive-ransom-set` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the aftermath being built` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `capture-instead-of-kill`, `ransom-comes-due` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none to the captor` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `hungry-for-ransom`, `knights` |

*Shelf: `transitions` in `data/transitions.json`.*

## Units

- [[Knights]] — *its `consumes` names `knights` by id; this page names "Knights" literally*

## Quirks

- [[Hungry for Ransom]] — *its `consumes` names `hungry-for-ransom` by id*

## flow

- [[A Captive]] — *its `flow` names `captive` by id*

## guard

- [[Taken alive rather than killed]] — *its `guards` names `capture-instead-of-kill` by id*
- [[The bill has a date on it]] — *its `guards` names `ransom-comes-due` by id*

## place

- [[Ransom Set]] — *its `to` names `captive-ransom-set` by id*
- [[Taken]] — *its `from` names `captive-taken` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:1252. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
