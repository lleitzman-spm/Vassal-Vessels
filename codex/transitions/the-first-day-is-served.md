---
type: "transition"
id: "transition:vessel-draw"
title: "The first day is served"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 766
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:vessel-draw"
---

# The first day is served

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The cup starts to empty. Nobody minds yet — this is exactly what the grant was for — but from here both of you are counting.

*Verified verbatim against `data/transitions.json`:766 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `vessel` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `vessel-full` |
| `to` | The place the case arrives in. Always exactly one. | `vessel-drawn` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `a house's men standing in your army for a day` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `one day of the year's service` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[A House's Vessel]] — *its `flow` names `vessel` by id*

## place

- [[Drawn Down]] — *its `to` names `vessel-drawn` by id*
- [[Full]] — *its `from` names `vessel-full` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:766. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
