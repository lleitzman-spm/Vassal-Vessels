---
type: "transition"
id: "transition:year-to-wolfmoon"
title: "The cold comes"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 627
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:year-to-wolfmoon"
---

# The cold comes

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Into winter. Marching is slow and exhausting and there is nothing to forage, so the granary drains fast. This is how armies starve without meeting anybody.

*Verified verbatim against `data/transitions.json`:627 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `year` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `year-harvest` |
| `to` | The place the case arrives in. Always exactly one. | `year-wolfmoon` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `ninety days passing` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `season-turns` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[The Year]] — *its `flow` names `year` by id*

## guard

- [[Ninety days pass]] — *its `guards` names `season-turns` by id*

## place

- [[Harvest]] — *its `from` names `year-harvest` by id*
- [[Wolfmoon]] — *its `to` names `year-wolfmoon` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:627. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
