---
type: "transition"
id: "transition:seat-empties"
title: "The chair stands empty"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 1425
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:seat-empties"
---

# The chair stands empty

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

And now nobody is doing the job. A vacant Marshalcy means you direct two contingents at a time — so the gap between taking a seat back and filling it is a real and often badly-judged risk.

*Verified verbatim against `data/transitions.json`:1425 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `seat` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `seat-taken-back` |
| `to` | The place the case arrives in. Always exactly one. | `seat-vacant` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the removal taking effect` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the seat's whole effect, until you fill it` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `unseated` |

*Shelf: `transitions` in `data/transitions.json`.*

## Grievances

- [[The seat left empty]] — *its `consumes` names `unseated` by id*

## flow

- [[A Seat at Court]] — *its `flow` names `seat` by id*

## place

- [[Taken Back]] — *its `from` names `seat-taken-back` by id*
- [[Vacant]] — *its `to` names `seat-vacant` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:1425. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
