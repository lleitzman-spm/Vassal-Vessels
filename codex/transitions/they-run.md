---
type: "transition"
id: "transition:pace-hurry"
title: "They run"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 414
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:pace-hurry"
---

# They run

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Hurrying. The fit men pull ahead of the tired ones and you arrive as a worse-organised version of what you set out as — which is a genuine trade, not a mistake, when arriving late is the bigger risk.

*Verified verbatim against `data/transitions.json`:414 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `pace` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `pace-advance` |
| `to` | The place the case arrives in. Always exactly one. | `pace-run` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `an urgent order, or a plan firing` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `real cohesion and real fatigue` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `ATTACK`, `soft`, `ploughed`, `marsh` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Attack]] — *its `consumes` names `ATTACK` by id; this page names "Attack" literally*

## Terrain

- [[Marsh]] — *its `consumes` names `marsh` by id*
- [[Ploughed Field]] — *its `consumes` names `ploughed` by id*
- [[Soft Ground and Mud]] — *its `consumes` names `soft` by id*

## flow

- [[A Unit's Pace]] — *its `flow` names `pace` by id*

## place

- [[Advancing]] — *its `from` names `pace-advance` by id*
- [[Running]] — *its `to` names `pace-run` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:414. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
