---
type: "transition"
id: "transition:heed-defy"
title: "\"No.\""
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 621
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:heed-defy"
---

# "No."

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Flat refusal in front of everyone. Nothing random put him here — and the herald names the largest cause out loud when it happens, which is the game telling you which of your decisions this was.

*Verified verbatim against `data/transitions.json`:621 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `heed` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `heed-weighing` |
| `to` | The place the case arrives in. Always exactly one. | `heed-defy` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the heed being computed, on arrival` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `heed-defies` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the contingent, and the watching army's opinion of you` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `may-turn`, `blood-debt`, `seat-taken-back`, `abandoned-on-the-field`, `answers-to-the-grandmaster` |

*Shelf: `transitions` in `data/transitions.json`.*

## Quirks

- [[Answers to His Own Master]] — *its `consumes` names `answers-to-the-grandmaster` by id*
- [[May Turn]] — *its `consumes` names `may-turn` by id*

## Grievances

- [[A seat taken back]] — *its `consumes` names `seat-taken-back` by id*
- [[Blood-debt]] — *its `consumes` names `blood-debt` by id; this page names "Blood-debt" literally*
- [[Left to die]] — *its `consumes` names `abandoned-on-the-field` by id*

## flow

- [[The Captain's Heed]] — *its `flow` names `heed` by id*

## guard

- [[Heed gone entirely]] — *its `guards` names `heed-defies` by id*

## place

- [[Defy]] — *its `to` names `heed-defy` by id*
- [[Weighing It]] — *its `from` names `heed-weighing` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:621. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
