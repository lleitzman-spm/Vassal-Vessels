---
type: "transition"
id: "transition:nerve-rally"
title: "They turn and stand"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 150
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:nerve-rally"
---

# They turn and stand

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Four conditions, all of which must hold at once, which is why rallying is uncommon and worth a great deal when it happens. The most controllable of the four is the last: a live captain near his men is the difference between a rout and a delay.

*Verified verbatim against `data/transitions.json`:150 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `nerve` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `nerve-routing` |
| `to` | The place the case arrives in. Always exactly one. | `nerve-rallying` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the rally check` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `rallies-left`, `rally-nerve-held`, `rally-enemy-clear`, `rally-someone-to-rally-to` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the unit's one and only second chance` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## guard

- [[A captain, or home ground]] — *its `guards` names `rally-someone-to-rally-to` by id*
- [[Has a second chance left]] — *its `guards` names `rallies-left` by id*
- [[Nerve back, and held there]] — *its `guards` names `rally-nerve-held` by id*
- [[Nobody close enough to kill them]] — *its `guards` names `rally-enemy-clear` by id*

## place

- [[Rallying]] — *its `to` names `nerve-rallying` by id*
- [[Routing]] — *its `from` names `nerve-routing` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:150. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
