---
type: "transition"
id: "transition:battle-breaks"
title: "An army comes apart"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 43
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:battle-breaks"
---

# An army comes apart

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

One side has stopped being an army. The battle phase ends and the pursuit begins, and the most important consequence is invisible unless you know to look for it: the command phase stops running. Every order you send from here is thrown into a hole.

*Verified verbatim against `data/transitions.json`:43 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `battle` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `battle-joined` |
| `to` | The place the case arrives in. Always exactly one. | `battle-pursuit` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the army check, every tick` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `army-breaks`, `banner-breaks-army` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the losing army takes a further morale blow on top` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `SOUND_RETREAT`, `ENEMY_BROKE`, `BANNER_LOST` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Sound the Retreat]] — *its `consumes` names `SOUND_RETREAT` by id*

## Standing plans

- [[BANNER_LOST]] — *its `consumes` names `BANNER_LOST` by id; this page names "BANNER_LOST" literally*
- [[ENEMY_BROKE]] — *its `consumes` names `ENEMY_BROKE` by id; this page names "ENEMY_BROKE" literally*

## Traits

- [[Command]] — *this page names "Command" literally*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## guard

- [[The army breaks]] — *its `guards` names `army-breaks` by id*
- [[The banner falls at the wrong moment]] — *its `guards` names `banner-breaks-army` by id*

## place

- [[Battle Joined]] — *its `from` names `battle-joined` by id*
- [[The Pursuit]] — *its `to` names `battle-pursuit` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:43. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
