---
type: "transition"
id: "transition:treachery-souring"
title: "Something changes in them"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 754
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:treachery-souring"
---

# Something changes in them

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Nothing has happened. The number moved, and the number is public if you go and look at it — the forecast will tell you before the battle. Almost nobody looks.

*Verified verbatim against `data/transitions.json`:754 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `treachery` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `treachery-true` |
| `to` | The place the case arrives in. Always exactly one. | `treachery-grudging` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the treachery reading crossing its first line` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `treachery-acts` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `nothing yet, which is the trap` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `unrewarded`, `taxed-hard`, `mercenaries-preferred`, `passed-over` |

*Shelf: `transitions` in `data/transitions.json`.*

## Grievances

- [[Foreigners hired over him]] — *its `consumes` names `mercenaries-preferred` by id*
- [[Passed over for a seat]] — *its `consumes` names `passed-over` by id*
- [[Taxed beyond custom]] — *its `consumes` names `taxed-hard` by id*
- [[Unrewarded]] — *its `consumes` names `unrewarded` by id; this page names "Unrewarded" literally*

## flow

- [[Turning]] — *its `flow` names `treachery` by id*

## guard

- [[High enough to work against you]] — *its `guards` names `treachery-acts` by id*

## place

- [[Grudging]] — *its `to` names `treachery-grudging` by id*
- [[True]] — *its `from` names `treachery-true` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:754. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
