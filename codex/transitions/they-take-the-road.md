---
type: "transition"
id: "transition:muster-march"
title: "They take the road"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 985
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:muster-march"
---

# They take the road

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The long middle. Nothing dramatic happens and the battle is being decided anyway, one tired league at a time.

*Verified verbatim against `data/transitions.json`:985 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `muster` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `muster-gathering` |
| `to` | The place the case arrives in. Always exactly one. | `muster-marching` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `a gathered contingent setting off` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `fatigue, every day, all the way` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `seedtime`, `wolfmoon`, `constable`, `marched-through-the-night` |

*Shelf: `transitions` in `data/transitions.json`.*

## Quirks

- [[Marched Through the Night]] — *its `consumes` names `marched-through-the-night` by id*

## Seats

- [[The Constable]] — *its `consumes` names `constable` by id*

## Seasons

- [[Seedtime (season)]] — *its `consumes` names `seedtime` by id; this page names "Seedtime" literally*
- [[Wolfmoon (season)]] — *its `consumes` names `wolfmoon` by id; this page names "Wolfmoon" literally*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## place

- [[Gathering]] — *its `from` names `muster-gathering` by id*
- [[On the Road]] — *its `to` names `muster-marching` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:985. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
