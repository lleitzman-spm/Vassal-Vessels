---
type: "transition"
id: "transition:nerve-flees"
title: "They are gone"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 179
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:nerve-flees"
---

# They are gone

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Off the field. Most of these men are alive and will be counted alive in the aftermath — they are simply no use to you now. Losing a unit to flight rather than to death is much the better outcome next season and no comfort at all this afternoon.

*Verified verbatim against `data/transitions.json`:179 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `nerve` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `nerve-routing` |
| `to` | The place the case arrives in. Always exactly one. | `nerve-fled` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `running long enough with no rally left` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `rallies-left` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the unit, for the rest of the day` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## guard

- [[Has a second chance left]] — *its `guards` names `rallies-left` by id*

## place

- [[Fled]] — *its `to` names `nerve-fled` by id*
- [[Routing]] — *its `from` names `nerve-routing` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:179. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
