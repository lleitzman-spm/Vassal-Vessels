---
type: "transition"
id: "transition:treachery-bought-back"
title: "You settle with them"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 810
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:treachery-bought-back"
---

# You settle with them

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The number is a READING, not a stored score, so the moment the records behind it change, it changes. This is the arrow the whole court half of the game exists to let you fire.

*Verified verbatim against `data/transitions.json`:810 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `treachery` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `treachery-grudging`, `treachery-acting` |
| `to` | The place the case arrives in. Always exactly one. | `treachery-true` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `amends, coin, a favour, or a grudge cooling` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `whatever it took — and it is always cheaper than the alternative` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `gift`, `justice-for`, `seat`, `spoils-shared`, `feast`, `enfeoffment`, `wardship-given`, `van-given` |

*Shelf: `transitions` in `data/transitions.json`.*

## Favours

- [[A feast]] — *its `consumes` names `feast` by id*
- [[A gift of coin]] — *its `consumes` names `gift` by id*
- [[A grant of land]] — *its `consumes` names `enfeoffment` by id*
- [[A seat at court]] — *its `consumes` names `seat` by id*
- [[A share of the plunder]] — *its `consumes` names `spoils-shared` by id*
- [[A wardship given away]] — *its `consumes` names `wardship-given` by id*
- [[Given the place of honour]] — *its `consumes` names `van-given` by id*
- [[Judgement given in his favour]] — *its `consumes` names `justice-for` by id*

## flow

- [[Turning]] — *its `flow` names `treachery` by id*

## place

- [[Acting Against You]] — *its `from` names `treachery-acting` by id*
- [[Grudging]] — *its `from` names `treachery-grudging` by id*
- [[True]] — *its `to` names `treachery-true` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:810. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
