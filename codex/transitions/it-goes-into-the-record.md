---
type: "transition"
id: "transition:muster-absorb"
title: "It goes into the record"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 1034
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:muster-absorb"
---

# It goes into the record

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The dead, the taken, the deeds and the debts all become ACTS on the chronicle. Nothing is stored as a summary; every future reading recomputes itself from these records, which is why a battle keeps mattering for years.

*Verified verbatim against `data/transitions.json`:1034 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `muster` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `muster-fought` |
| `to` | The place the case arrives in. Always exactly one. | `muster-absorbed` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the aftermath being absorbed` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none — and everything, later` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `blood-debt`, `unransomed`, `spoils-shared`, `victory-shared`, `justice-for` |

*Shelf: `transitions` in `data/transitions.json`.*

## Grievances

- [[Blood-debt]] — *its `consumes` names `blood-debt` by id; this page names "Blood-debt" literally*
- [[Kin left in captivity]] — *its `consumes` names `unransomed` by id*

## Favours

- [[A share of the plunder]] — *its `consumes` names `spoils-shared` by id*
- [[A victory won together]] — *its `consumes` names `victory-shared` by id*
- [[Judgement given in his favour]] — *its `consumes` names `justice-for` by id*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## place

- [[Absorbed]] — *its `to` names `muster-absorbed` by id*
- [[Fought]] — *its `from` names `muster-fought` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:1034. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
