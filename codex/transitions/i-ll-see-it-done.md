---
type: "transition"
id: "transition:heed-his-way"
title: "\"I'll see it done.\""
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 382
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:heed-his-way"
---

# "I'll see it done."

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Compliance without conformity. He gets you the outcome by a route you did not pick — usually harmless, occasionally better than your idea, occasionally catastrophic in a way you will hear explained afterwards.

*Verified verbatim against `data/transitions.json`:382 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `heed` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `heed-weighing` |
| `to` | The place the case arrives in. Always exactly one. | `heed-obey-his-way` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the heed being computed, on arrival` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `heed-his-way` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `you do not choose the method` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[The Captain's Heed]] — *its `flow` names `heed` by id*

## guard

- [[Heed enough to comply, not to conform]] — *its `guards` names `heed-his-way` by id*

## place

- [[Obey, His Way]] — *its `to` names `heed-obey-his-way` by id*
- [[Weighing It]] — *its `from` names `heed-weighing` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:382. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
