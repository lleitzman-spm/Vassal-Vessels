---
type: "transition"
id: "transition:seat-invest"
title: "You invest someone"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 974
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:seat-invest"
---

# You invest someone

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The seat starts working — scaled by how much the holder likes you, so a brilliant officer who resents you is worth less than a competent one who does not. Everyone who wanted it and did not get it now has a number against your name.

*Verified verbatim against `data/transitions.json`:974 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `seat` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `seat-vacant` |
| `to` | The place the case arrives in. Always exactly one. | `seat-filled` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `an investiture going onto the record` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `days at court, a stipend, and every rival's opinion` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[A Seat at Court]] — *its `flow` names `seat` by id*

## place

- [[Held]] — *its `to` names `seat-filled` by id*
- [[Vacant]] — *its `from` names `seat-vacant` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:974. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
