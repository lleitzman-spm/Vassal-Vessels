---
type: "transition"
id: "transition:grievance-goes-cold"
title: "It goes cold"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 855
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:grievance-goes-cold"
---

# It goes cold

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Down to its residue and no further. Eleven cold grievances make an army that does not quite work and gives you nothing to point at — which is how most bad reigns actually feel from the inside.

*Verified verbatim against `data/transitions.json`:855 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `grievance` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `grievance-cooling` |
| `to` | The place the case arrives in. Always exactly one. | `grievance-cold` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the decay running its course` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `grievance-cools` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[A Grievance]] — *its `flow` names `grievance` by id*

## guard

- [[Time takes the heat out]] — *its `guards` names `grievance-cools` by id*

## place

- [[Cold]] — *its `to` names `grievance-cold` by id*
- [[Cooling]] — *its `from` names `grievance-cooling` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:855. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
