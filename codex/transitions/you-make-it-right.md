---
type: "transition"
id: "transition:grievance-amends"
title: "You make it right"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 1206
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:grievance-amends"
---

# You make it right

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The real fix, and note how it works: the grudge is not deleted. The record of your amends is read ALONGSIDE the record of the wrong, which is both more honest and more durable than pretending it never happened.

*Verified verbatim against `data/transitions.json`:1206 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `grievance` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `grievance-known`, `grievance-burning`, `grievance-cooling` |
| `to` | The place the case arrives in. Always exactly one. | `grievance-amended` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `an act of amends going onto the record` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `coin, land, a judgement, or your pride in public` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `justice-for`, `gift`, `enfeoffment`, `seat`, `marriage-royal`, `justiciar`, `van-given`, `wardship-given` |

*Shelf: `transitions` in `data/transitions.json`.*

## Seats

- [[The Justiciar]] — *its `consumes` names `justiciar` by id*

## Favours

- [[A gift of coin]] — *its `consumes` names `gift` by id*
- [[A grant of land]] — *its `consumes` names `enfeoffment` by id*
- [[A seat at court]] — *its `consumes` names `seat` by id*
- [[A wardship given away]] — *its `consumes` names `wardship-given` by id*
- [[Given the place of honour]] — *its `consumes` names `van-given` by id*
- [[Judgement given in his favour]] — *its `consumes` names `justice-for` by id*
- [[Marriage into the crown's house]] — *its `consumes` names `marriage-royal` by id*

## flow

- [[A Grievance]] — *its `flow` names `grievance` by id*

## place

- [[Amended]] — *its `to` names `grievance-amended` by id*
- [[Burning]] — *its `from` names `grievance-burning` by id*
- [[Cooling]] — *its `from` names `grievance-cooling` by id*
- [[Known]] — *its `from` names `grievance-known` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:1206. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
