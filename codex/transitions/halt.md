---
type: "transition"
id: "transition:pace-halt"
title: "Halt"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 321
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:pace-halt"
---

# Halt

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Stopping. Underrated as an order: a halted unit recovers its cohesion, and cohesion is what decides whether it can turn, hold and refuse.

*Verified verbatim against `data/transitions.json`:321 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `pace` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `pace-walk`, `pace-advance`, `pace-run`, `pace-charge` |
| `to` | The place the case arrives in. Always exactly one. | `pace-stop` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `a halt order, reaching the objective, or contact` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none — and standing still is where cohesion comes back` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[A Unit's Pace]] — *its `flow` names `pace` by id*

## place

- [[Advancing]] — *its `from` names `pace-advance` by id*
- [[Charging]] — *its `from` names `pace-charge` by id*
- [[Halted]] — *its `to` names `pace-stop` by id*
- [[Running]] — *its `from` names `pace-run` by id*
- [[Walking]] — *its `from` names `pace-walk` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:321. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
