---
type: "transition"
id: "transition:pace-charge"
title: "Charge!"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 431
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:pace-charge"
---

# Charge!

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The last hundred metres. Shock scales with the SQUARE of closing speed, so this is where battles are actually decided — and a charge that arrives at a braced line pays every one of those costs for nothing at all.

*Verified verbatim against `data/transitions.json`:431 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `pace` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `pace-run` |
| `to` | The place the case arrives in. Always exactly one. | `pace-charge` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `a charge order, inside charging distance` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the most expensive move on the table, whether it lands or not` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `CHARGE`, `knights`, `charges-without-orders`, `turf`, `road` |

*Shelf: `transitions` in `data/transitions.json`.*

## Units

- [[Knights]] — *its `consumes` names `knights` by id; this page names "Knights" literally*

## Orders

- [[Charge]] — *its `consumes` names `CHARGE` by id; this page names "Charge" literally*

## Terrain

- [[Firm Turf]] — *its `consumes` names `turf` by id*
- [[Road]] — *its `consumes` names `road` by id*

## Quirks

- [[Charges Without Orders]] — *its `consumes` names `charges-without-orders` by id*

## flow

- [[A Unit's Pace]] — *its `flow` names `pace` by id*

## place

- [[Charging]] — *its `to` names `pace-charge` by id*
- [[Running]] — *its `from` names `pace-run` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:431. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
