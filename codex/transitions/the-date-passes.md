---
type: "transition"
id: "transition:captive-lapses"
title: "The date passes"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 1285
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:captive-lapses"
---

# The date passes

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Nothing dramatic happens and everyone notices. A house watching its lord sit in another man's hall while its king does nothing is a house doing arithmetic about next season.

*Verified verbatim against `data/transitions.json`:1285 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `captive` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `captive-ransom-set` |
| `to` | The place the case arrives in. Always exactly one. | `captive-overdue` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the due date arriving unpaid` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `ransom-comes-due` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the realm's opinion, daily` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `unransomed` |

*Shelf: `transitions` in `data/transitions.json`.*

## Grievances

- [[Kin left in captivity]] — *its `consumes` names `unransomed` by id*

## flow

- [[A Captive]] — *its `flow` names `captive` by id*

## guard

- [[The bill has a date on it]] — *its `guards` names `ransom-comes-due` by id*

## place

- [[Overdue]] — *its `to` names `captive-overdue` by id*
- [[Ransom Set]] — *its `from` names `captive-ransom-set` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:1285. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
