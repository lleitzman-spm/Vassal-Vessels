---
type: "transition"
id: "transition:pace-break-into-rout"
title: "They run the other way"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 337
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:pace-break-into-rout"
---

# They run the other way

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The nerve machine reaching across and taking the pace machine with it. Note that it fires from ANY pace, including a charge — a unit can break in the act of attacking, and men who break while charging are the ones who die in the greatest numbers.

*Verified verbatim against `data/transitions.json`:337 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `pace` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `pace-stop`, `pace-walk`, `pace-advance`, `pace-run`, `pace-charge` |
| `to` | The place the case arrives in. Always exactly one. | `pace-rout` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the unit's nerve giving out` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `nerve-breaks` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `everything` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Charge]] — *this page names "Charge" literally*

## flow

- [[A Unit's Pace]] — *its `flow` names `pace` by id*

## guard

- [[Nerve gives out]] — *its `guards` names `nerve-breaks` by id*

## place

- [[Advancing]] — *its `from` names `pace-advance` by id*
- [[Charging]] — *its `from` names `pace-charge` by id*
- [[Fleeing]] — *its `to` names `pace-rout` by id*
- [[Halted]] — *its `from` names `pace-stop` by id*
- [[Running]] — *its `from` names `pace-run` by id*
- [[Walking]] — *its `from` names `pace-walk` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:337. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
