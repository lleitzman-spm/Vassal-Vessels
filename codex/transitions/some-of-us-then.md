---
type: "transition"
id: "transition:heed-hedge"
title: "\"Some of us, then.\""
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 408
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:heed-hedge"
---

# "Some of us, then."

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The bolder face of the same unwillingness. He commits what he can afford to lose and keeps the rest, because he does not trust the order, or you, or the day.

*Verified verbatim against `data/transitions.json`:408 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `heed` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `heed-weighing` |
| `to` | The place the case arrives in. Always exactly one. | `heed-hedge` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the heed being computed, on arrival` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `heed-drags-or-hedges` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `half the contingent stays home` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[The Captain's Heed]] — *its `flow` names `heed` by id*

## guard

- [[Heed low enough to stall]] — *its `guards` names `heed-drags-or-hedges` by id*

## place

- [[Hedge]] — *its `to` names `heed-hedge` by id*
- [[Weighing It]] — *its `from` names `heed-weighing` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:408. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
