---
type: "transition"
id: "transition:nerve-reformed"
title: "They re-form"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 166
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:nerve-reformed"
---

# They re-form

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Back in the battle — as SHAKEN, never steady, and with nothing behind them. A rallied unit is a real asset and a fragile one, and the commander who forgets which is about to lose it permanently.

*Verified verbatim against `data/transitions.json`:166 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `nerve` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `nerve-rallying` |
| `to` | The place the case arrives in. Always exactly one. | `nerve-shaken` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the rally hold completing` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `rally-nerve-held` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the second chance is now spent` |

*Shelf: `transitions` in `data/transitions.json`.*

## Keywords

- [[Fragile]] — *this page names "Fragile" literally*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## guard

- [[Nerve back, and held there]] — *its `guards` names `rally-nerve-held` by id*

## place

- [[Rallying]] — *its `from` names `nerve-rallying` by id*
- [[Shaken]] — *its `to` names `nerve-shaken` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:166. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
