---
type: "transition"
id: "transition:grievance-word-spreads"
title: "They find out"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 820
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:grievance-word-spreads"
---

# They find out

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The clock starts HERE, not on the day you did it. A distant house can carry a brand-new grudge about something years old, and a secret injustice genuinely costs nothing right up until it does not.

*Verified verbatim against `data/transitions.json`:820 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `grievance` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `grievance-done` |
| `to` | The place the case arrives in. Always exactly one. | `grievance-known` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `word reaching the wronged house` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none — the cost starts now` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[A Grievance]] — *its `flow` names `grievance` by id*

## place

- [[Done]] — *its `from` names `grievance-done` by id*
- [[Known]] — *its `to` names `grievance-known` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:820. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
