---
type: "transition"
id: "transition:nerve-steady-again"
title: "They are themselves again"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 165
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:nerve-steady-again"
---

# They are themselves again

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Fully recovered. Rare in the middle of a fight and usually the result of getting out of contact for a while — which is an argument for rotating a line that you can only act on if you kept a reserve.

*Verified verbatim against `data/transitions.json`:165 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `nerve` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `nerve-shaken` |
| `to` | The place the case arrives in. Always exactly one. | `nerve-steady` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the morale check, every tick` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `nerve-steadies` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `steady-hand`, `will-not-flee` |

*Shelf: `transitions` in `data/transitions.json`.*

## Quirks

- [[Steady Hand]] — *its `consumes` names `steady-hand` by id*
- [[Will Not Flee]] — *its `consumes` names `will-not-flee` by id*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## guard

- [[Nerve comes back]] — *its `guards` names `nerve-steadies` by id*

## place

- [[Shaken]] — *its `from` names `nerve-shaken` by id*
- [[Steady]] — *its `to` names `nerve-steady` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:165. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
