---
type: "transition"
id: "transition:vessel-empty"
title: "The cup runs out"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 1088
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:vessel-empty"
---

# The cup runs out

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

They owe you nothing more this year. Note what does NOT happen: no rule stops you calling them. The Vessel is not a fence, it is a price list.

*Verified verbatim against `data/transitions.json`:1088 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `vessel` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `vessel-drawn` |
| `to` | The place the case arrives in. Always exactly one. | `vessel-dry` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the last owed day being served` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `vessel-runs-dry` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none, yet` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `feudal-service`, `scutage` |

*Shelf: `transitions` in `data/transitions.json`.*

## Obligations

- [[Feudal service]] — *its `consumes` names `feudal-service` by id*

## Answers

- [[Sends coin instead]] — *its `consumes` names `scutage` by id*

## flow

- [[A House's Vessel]] — *its `flow` names `vessel` by id*

## guard

- [[The cup is empty]] — *its `guards` names `vessel-runs-dry` by id*

## place

- [[Drawn Down]] — *its `from` names `vessel-drawn` by id*
- [[Dry]] — *its `to` names `vessel-dry` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:1088. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
