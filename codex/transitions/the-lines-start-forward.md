---
type: "transition"
id: "transition:battle-join"
title: "The lines start forward"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 18
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:battle-join"
---

# The lines start forward

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Deployment ends and the simulation begins. Nothing else in the game has this quality of a door closing behind you: every decision before this was reversible and none of them are now.

*Verified verbatim against `data/transitions.json`:18 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `battle` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `battle-deploy` |
| `to` | The place the case arrives in. Always exactly one. | `battle-joined` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the first tick of the battle` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `turf`, `road`, `ploughed`, `soft`, `marsh`, `scrub`, `woods`, `rock`, `ford`, `water`, `crag`, `TICK_REACHED` |

*Shelf: `transitions` in `data/transitions.json`.*

## Standing plans

- [[TICK_REACHED]] — *its `consumes` names `TICK_REACHED` by id; this page names "TICK_REACHED" literally*

## Terrain

- [[Crag]] — *its `consumes` names `crag` by id*
- [[Deep Water]] — *its `consumes` names `water` by id*
- [[Firm Turf]] — *its `consumes` names `turf` by id*
- [[Ford]] — *its `consumes` names `ford` by id*
- [[Marsh]] — *its `consumes` names `marsh` by id*
- [[Ploughed Field]] — *its `consumes` names `ploughed` by id*
- [[Road]] — *its `consumes` names `road` by id*
- [[Rock and Scree]] — *its `consumes` names `rock` by id*
- [[Scrub and Gorse]] — *its `consumes` names `scrub` by id*
- [[Soft Ground and Mud]] — *its `consumes` names `soft` by id*
- [[Woodland]] — *its `consumes` names `woods` by id*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## place

- [[Battle Joined]] — *its `to` names `battle-joined` by id*
- [[Deploying]] — *its `from` names `battle-deploy` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:18. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
