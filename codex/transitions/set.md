---
type: "transition"
id: "transition:footing-brace"
title: "Set!"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 285
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:footing-brace"
---

# Set!

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Butts in the earth, points level. The single most valuable second in the game and one you very often do not have — which is exactly what standing plans are for. A plan that braces automatically when horses come inside a named distance is worth more than any order you could send by courier.

*Verified verbatim against `data/transitions.json`:285 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `footing` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `footing-none` |
| `to` | The place the case arrives in. Always exactly one. | `footing-braced` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `a brace order, or a standing plan firing on cavalry closing` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | *none* |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the unit must be stationary, and stays stationary` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `BRACE`, `ENEMY_CAVALRY_WITHIN`, `spearmen`, `pikes`, `CanBrace`, `close`, `shieldwall`, `ENEMY_WITHIN` |

*Shelf: `transitions` in `data/transitions.json`.*

## Units

- [[Levy Spearmen]] — *its `consumes` names `spearmen` by id*

## Keywords

- [[Can Brace]] — *its `consumes` names `CanBrace` by id*
- [[Shield Wall (keyword)]] — *its `consumes` names `shieldwall` by id*

## Formations

- [[Close Order]] — *its `consumes` names `close` by id*

## Orders

- [[Set and Brace]] — *its `consumes` names `BRACE` by id*

## Standing plans

- [[ENEMY_CAVALRY_WITHIN]] — *its `consumes` names `ENEMY_CAVALRY_WITHIN` by id; this page names "ENEMY_CAVALRY_WITHIN" literally*
- [[ENEMY_WITHIN]] — *its `consumes` names `ENEMY_WITHIN` by id; this page names "ENEMY_WITHIN" literally*

## flow

- [[A Unit's Footing]] — *its `flow` names `footing` by id*

## place

- [[Braced]] — *its `to` names `footing-braced` by id*
- [[Loose Footing]] — *its `from` names `footing-none` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:285. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
