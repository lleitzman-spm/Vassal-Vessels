---
type: "transition"
id: "transition:grievance-cools"
title: "It cools"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 1176
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:grievance-cools"
---

# It cools

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Time takes the heat out. This is the lazy king's strategy and it half works — it stops any single grudge from dominating, and it never clears the board.

*Verified verbatim against `data/transitions.json`:1176 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `grievance` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `grievance-burning` |
| `to` | The place the case arrives in. Always exactly one. | `grievance-cooling` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `enough years passing` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `grievance-cools` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `feast`, `tourney` |

*Shelf: `transitions` in `data/transitions.json`.*

## Favours

- [[A feast]] — *its `consumes` names `feast` by id*
- [[A tourney]] — *its `consumes` names `tourney` by id*

## flow

- [[A Grievance]] — *its `flow` names `grievance` by id*

## guard

- [[Time takes the heat out]] — *its `guards` names `grievance-cools` by id*

## place

- [[Burning]] — *its `from` names `grievance-burning` by id*
- [[Cooling]] — *its `to` names `grievance-cooling` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:1176. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
