---
type: "transition"
id: "transition:pace-rally-pace"
title: "The running stops"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 492
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:pace-rally-pace"
---

# The running stops

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Tied to the nerve machine's rally. The men stop, out of breath, and start finding each other again.

*Verified verbatim against `data/transitions.json`:492 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `pace` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `pace-rout` |
| `to` | The place the case arrives in. Always exactly one. | `pace-stop` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the unit rallying` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `rally-nerve-held` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `none` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `EXHORT` |

*Shelf: `transitions` in `data/transitions.json`.*

## Orders

- [[Exhort]] — *its `consumes` names `EXHORT` by id; this page names "Exhort" literally*

## flow

- [[A Unit's Pace]] — *its `flow` names `pace` by id*

## guard

- [[Nerve back, and held there]] — *its `guards` names `rally-nerve-held` by id*

## place

- [[Fleeing]] — *its `from` names `pace-rout` by id*
- [[Halted]] — *its `to` names `pace-stop` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:492. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
