---
type: "transition"
id: "transition:nerve-wavering"
title: "The back ranks start to drift"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 94
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:nerve-wavering"
---

# The back ranks start to drift

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Your last warning. The men at the back have already decided and the men at the front have not noticed. If a captain is going to shout, this is when.

*Verified verbatim against `data/transitions.json`:94 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `nerve` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `nerve-shaken` |
| `to` | The place the case arrives in. Always exactly one. | `nerve-wavering` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `the morale check, every tick` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `nerve-wavers` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `the unit is one bad moment from breaking` |

*Shelf: `transitions` in `data/transitions.json`.*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## guard

- [[About to go]] — *its `guards` names `nerve-wavers` by id*

## place

- [[Shaken]] — *its `from` names `nerve-shaken` by id*
- [[Wavering]] — *its `to` names `nerve-wavering` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:94. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
