---
type: "transition"
id: "transition:muster-gather"
title: "Lords collect their men"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/transitions.json"
source_line: 965
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "transition:muster-gather"
---

# Lords collect their men

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Men have to come in off farms before anyone can march. A compact holding does it fast and a scattered one takes nearly twice as long, which is why land shape matters as much as land quantity.

*Verified verbatim against `data/transitions.json`:965 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this arrow belongs to (data/flows.json). | `muster` |
| `from` | The place (or places) a case can be in for this arrow to fire. A list means the machine genuinely allows several origins, not that the author was unsure. | `muster-answered` |
| `to` | The place the case arrives in. Always exactly one. | `muster-gathering` |
| `on` | The EVENT that fires it — the thing that happens. A transition with no event is a condition being re-checked, which is a different animal and is written as such. | `each house beginning to assemble` |
| `guards` | The conditions that must hold (data/guards.json). An empty list means the arrow fires on the event alone, which is rare and worth noticing. | `gathering-done` |
| `cost` | What the case pays to make this move — cohesion, fatigue, standing, willingness, coin. Free moves are marked so, because in this game most of them are not. | `days, before a single league is walked` |
| `consumes` | The knowledge this step of the machine actually READS, by id — a cause, a season, an order, a quirk, a grievance, a favour, a seat. This is the writ's claim made into a road: flows do not CONTAIN knowledge, they consume it. Ids only; a relationship stated in prose gets no edge, and a value resolving to nothing is dropped rather than invented. | `manor`, `village`, `castle`, `feudal-service`, `charter-militia` |

*Shelf: `transitions` in `data/transitions.json`.*

## Obligations

- [[Feudal service]] — *its `consumes` names `feudal-service` by id*
- [[The town charter]] — *its `consumes` names `charter-militia` by id*

## Holdings

- [[Castle]] — *its `consumes` names `castle` by id; this page names "Castle" literally*
- [[Manor]] — *its `consumes` names `manor` by id*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## guard

- [[The men are together]] — *its `guards` names `gathering-done` by id*

## place

- [[Answers In]] — *its `from` names `muster-answered` by id*
- [[Gathering]] — *its `to` names `muster-gathering` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/transitions.json`:965. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
