---
type: "answer"
id: "answer:scutage"
title: "Sends coin instead"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 77
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:scutage"
---

# Sends coin instead

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Shield money. Legal, blameless, no grudge either way — and it buys back about a third as many mercenaries as he did not send. If he cannot afford it, he sends a token instead.

*Verified verbatim against `data/answers.json`:77 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | *undocumented in the file's own `fields` block* | `12` |
| `fraction` | *undocumented in the file's own `fields` block* | `0` |
| `crownsPerManOwed` | *undocumented in the file's own `fields` block* | `2` |

*Shelf: `answers` in `data/answers.json`.*

## Answers

- [[Sends a token]] — *this page names "Sends a token" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:77. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
