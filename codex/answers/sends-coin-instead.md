---
type: "answer"
id: "answer:scutage"
title: "Sends coin instead"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 135
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:scutage"
---

# Sends coin instead

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Shield money. Legal, blameless, no grudge either way — and it buys back about a third as many mercenaries as he did not send. If he cannot afford it, he sends a token instead.

*Verified verbatim against `data/answers.json`:135 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | The lowest willingness that still lands in this band. Willingness is computed, never rolled — so a house's answer is knowable before you send the letter. | `12` |
| `fraction` | What share of the men he owes actually arrive. 1.0 is everything; the bands below it are where an army quietly becomes smaller than the one on your list. | `0` |
| `crownsPerManOwed` | What he pays in scutage per man not sent — the legal way to buy out of service. It is honest, it is expensive, and taken year after year it quietly decays the custom that he owes you men at all. | `2` |

*Shelf: `answers` in `data/answers.json`.*

## Answers

- [[Sends a token]] — *this page names "Sends a token" literally*

## Backlinks

### transition

- [[The answers come back]] — *its `consumes` names `scutage` by id*
- [[The cup runs out]] — *its `consumes` names `scutage` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:135. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
