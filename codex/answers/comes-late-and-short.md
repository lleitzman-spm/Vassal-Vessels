---
type: "answer"
id: "answer:late-and-short"
title: "Comes late and short"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 60
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:late-and-short"
---

# Comes late and short

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Three-fifths, five days late. If the battle is on the muster day these men arrive partway through it — which the battle may model as reinforcements, or may not model at all, in which case they simply were not there and the report says so.

*Verified verbatim against `data/answers.json`:60 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | *undocumented in the file's own `fields` block* | `35` |
| `fraction` | *undocumented in the file's own `fields` block* | `0.6` |
| `lateDays` | *undocumented in the file's own `fields` block* | `5` |

*Shelf: `answers` in `data/answers.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:60. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
