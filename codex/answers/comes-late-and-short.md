---
type: "answer"
id: "answer:late-and-short"
title: "Comes late and short"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 118
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:late-and-short"
---

# Comes late and short

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Three-fifths, five days late. If the battle is on the muster day these men arrive partway through it — which the battle may model as reinforcements, or may not model at all, in which case they simply were not there and the report says so.

*Verified verbatim against `data/answers.json`:118 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | The lowest willingness that still lands in this band. Willingness is computed, never rolled — so a house's answer is knowable before you send the letter. | `35` |
| `fraction` | What share of the men he owes actually arrive. 1.0 is everything; the bands below it are where an army quietly becomes smaller than the one on your list. | `0.6` |
| `lateDays` | How many days after the appointed one they turn up. Latecomers are not absent — they walk onto the field mid-battle, which is sometimes a rescue and sometimes a waste. | `5` |

*Shelf: `answers` in `data/answers.json`.*

## Backlinks

### transition

- [[The answers come back]] — *its `consumes` names `late-and-short` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:118. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
