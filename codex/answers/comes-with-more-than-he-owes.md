---
type: "answer"
id: "answer:more-than-owed"
title: "Comes with more than he owes"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 85
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:more-than-owed"
---

# Comes with more than he owes

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

He brings volunteers: a tenth more men than the grant requires, and they arrive keen. This is what a devoted house in a just war on its own doorstep looks like, and it is worth working for.

*Verified verbatim against `data/answers.json`:85 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | The lowest willingness that still lands in this band. Willingness is computed, never rolled — so a house's answer is knowable before you send the letter. | `95` |
| `fraction` | What share of the men he owes actually arrive. 1.0 is everything; the bands below it are where an army quietly becomes smaller than the one on your list. | `1.1` |
| `lateDays` | How many days after the appointed one they turn up. Latecomers are not absent — they walk onto the field mid-battle, which is sometimes a rescue and sometimes a waste. | `0` |
| `resolveBonus` | Added to the resolve of the men he sends. A lord who comes gladly brings men who fight harder, and it stacks on top of everything the court already gave them. | `10` |

*Shelf: `answers` in `data/answers.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:85. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
