---
type: "answer"
id: "answer:token"
title: "Sends a token"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 143
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:token"
---

# Sends a token

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Fifteen men and an apology. It is not an army; it is a way of not quite refusing.

*Verified verbatim against `data/answers.json`:143 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | The lowest willingness that still lands in this band. Willingness is computed, never rolled — so a house's answer is knowable before you send the letter. | `12` |
| `condition` | The extra thing that must be true for this answer to be possible at all, beyond the willingness band. | `cannot afford the scutage` |
| `fraction` | What share of the men he owes actually arrive. 1.0 is everything; the bands below it are where an army quietly becomes smaller than the one on your list. | `0.15` |
| `lateDays` | How many days after the appointed one they turn up. Latecomers are not absent — they walk onto the field mid-battle, which is sometimes a rescue and sometimes a waste. | `6` |

*Shelf: `answers` in `data/answers.json`.*

## Backlinks

### Answers

- [[Sends coin instead]] — *this page names "Sends a token" literally*

### transition

- [[The answers come back]] — *its `consumes` names `token` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:143. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
