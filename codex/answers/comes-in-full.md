---
type: "answer"
id: "answer:in-full"
title: "Comes in full"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 36
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:in-full"
---

# Comes in full

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Exactly what he owes, on the day, in good order. The thing the entire court exists to produce.

*Verified verbatim against `data/answers.json`:36 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | *undocumented in the file's own `fields` block* | `80` |
| `fraction` | *undocumented in the file's own `fields` block* | `1` |
| `lateDays` | *undocumented in the file's own `fields` block* | `0` |

*Shelf: `answers` in `data/answers.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:36. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
