---
type: "answer"
id: "answer:deputy"
title: "Sends a deputy"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 126
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:deputy"
---

# Sends a deputy

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

He does not come himself. He sends a cousin with half the men and none of the good ones, and that cousin is twenty points worse at commanding them. Everybody at the muster understands precisely what has been said.

*Verified verbatim against `data/answers.json`:126 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | The lowest willingness that still lands in this band. Willingness is computed, never rolled — so a house's answer is knowable before you send the letter. | `22` |
| `fraction` | What share of the men he owes actually arrive. 1.0 is everything; the bands below it are where an army quietly becomes smaller than the one on your list. | `0.5` |
| `lateDays` | How many days after the appointed one they turn up. Latecomers are not absent — they walk onto the field mid-battle, which is sometimes a rescue and sometimes a waste. | `4` |
| `captainCommandPenalty` | Taken off the captain's command when he sends a deputy instead of coming himself. The men are there; the man who could make them listen is not. | `-20` |

*Shelf: `answers` in `data/answers.json`.*

## Backlinks

### transition

- [[The answers come back]] — *its `consumes` names `deputy` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:126. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
