---
type: "answer"
id: "answer:deputy"
title: "Sends a deputy"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 68
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:deputy"
---

# Sends a deputy

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

He does not come himself. He sends a cousin with half the men and none of the good ones, and that cousin is twenty points worse at commanding them. Everybody at the muster understands precisely what has been said.

*Verified verbatim against `data/answers.json`:68 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | *undocumented in the file's own `fields` block* | `22` |
| `fraction` | *undocumented in the file's own `fields` block* | `0.5` |
| `lateDays` | *undocumented in the file's own `fields` block* | `4` |
| `captainCommandPenalty` | *undocumented in the file's own `fields` block* | `-20` |

*Shelf: `answers` in `data/answers.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:68. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
