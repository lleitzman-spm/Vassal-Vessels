---
type: "answer"
id: "answer:short"
title: "Comes short"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/answers.json"
source_line: 110
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "answer:short"
---

# Comes short

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Three-quarters, two days late, and a courteous letter about the state of the roads. Read the next section before you shrug at this one: short does not mean everything got smaller.

*Verified verbatim against `data/answers.json`:110 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `willingnessAtLeast` | The lowest willingness that still lands in this band. Willingness is computed, never rolled — so a house's answer is knowable before you send the letter. | `50` |
| `fraction` | What share of the men he owes actually arrive. 1.0 is everything; the bands below it are where an army quietly becomes smaller than the one on your list. | `0.75` |
| `lateDays` | How many days after the appointed one they turn up. Latecomers are not absent — they walk onto the field mid-battle, which is sometimes a rescue and sometimes a waste. | `2` |

*Shelf: `answers` in `data/answers.json`.*

## Backlinks

### Writs that specify it

- [[VASSAL VESSELS — The Constitution]] — *this writ names "Comes short" literally*
- [[WRIT — THE COURT]] — *this writ names "Comes short" literally*

### transition

- [[The answers come back]] — *its `consumes` names `short` by id*
- [[The chase burns out (transition)]] — *its `consumes` names `SHORT` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/answers.json`:110. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
