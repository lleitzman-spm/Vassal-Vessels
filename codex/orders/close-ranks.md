---
type: "order"
id: "order:close-order"
title: "Close Ranks"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 289
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:close-order"
---

# Close Ranks

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Shoulder to shoulder. Everything to do with pushing, holding and receiving a charge wants close order; everything to do with not being shot hates it. Half of commanding infantry is knowing which of those two things is about to happen to you.

*Verified verbatim against `data/orders.json`:289 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | Which of the three voices this belongs to: a standing charge always in force, an improvised order that must ride to its captain, or a pursuit policy. | `posture` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `1` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `15` |
| `commitTicks` | How long you cannot change your mind after it starts. | `80` |
| `windupTicks` | How long before it begins to take effect at all. | `80` |
| `params` | What the order needs pointing at — a place, a unit, a friend, a facing. An order with nowhere to point is not an order. | *none* |
| `theCost` | What issuing it costs you, in something other than coin — usually a contingent doing nothing, or a flank left thin. Every order below is a trade. | `You become the best target on the field — three and a half times the arrows.` |

## What you would see on the field

> The block visibly tightens.

*Shelf: `orders` in `data/orders.json`.*

## Formations

- [[Close Order]] — *this page names "Close Order" literally*

## Orders

- [[Charge]] — *this page names "Charge" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:289. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
