---
type: "order"
id: "order:commit-reserve"
title: "Commit the Reserve"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 459
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:commit-reserve"
---

# Commit the Reserve

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Loose the men you have been holding. Everyone who can see it takes heart at once. You are trading a permanent trickle for a single surge, and the whole art is knowing when the surge is worth more than the trickle.

*Verified verbatim against `data/orders.json`:459 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | Which of the three voices this belongs to: a standing charge always in force, an improvised order that must ride to its captain, or a pursuit policy. | `commitment` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `1` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `10` |
| `commitTicks` | How long you cannot change your mind after it starts. | `0` |
| `windupTicks` | How long before it begins to take effect at all. | `0` |
| `params` | What the order needs pointing at — a place, a unit, a friend, a facing. An order with nowhere to point is not an order. | `contingent` |
| `oncePerContingent` | Whether it can be used only once by each contingent for the whole battle. | yes |
| `theCost` | What issuing it costs you, in something other than coin — usually a contingent doing nothing, or a flank left thin. Every order below is a trade. | `It is ONCE, and here is the bite: while a reserve stands uncommitted, every friend who can see it gains a little heart every single tick. The moment you spend it, that trickle stops forever.` |

## What you would see on the field

> The reserve moves for the first time all battle, and every man who can see it lifts his head.

*Shelf: `orders` in `data/orders.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:459. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
