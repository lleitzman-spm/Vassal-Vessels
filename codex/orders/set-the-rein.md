---
type: "order"
id: "order:set-pursuit"
title: "Set the Rein"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 364
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:set-pursuit"
---

# Set the Rein

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Say what this contingent does when the enemy breaks. Setting it before the battle is free; changing it mid-battle costs a rider and passes through heed like anything else — and a greedy captain in a blood-fury will fail that exactly the way he fails any order he does not like. See the pursuit policies below.

*Verified verbatim against `data/orders.json`:364 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | *undocumented in the file's own `fields` block* | `policy` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `1` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `10` |
| `commitTicks` | How long you cannot change your mind after it starts. | `0` |
| `windupTicks` | How long before it begins to take effect at all. | `20` |
| `params` | *undocumented in the file's own `fields` block* | `policy` |
| `grudgeSensitive` | *undocumented in the file's own `fields` block* | yes |
| `theCost` | *undocumented in the file's own `fields` block* | `Almost nothing to give, and almost impossible to enforce on the wrong man.` |

## What you would see on the field

> Nothing, until the enemy breaks. Then everything.

*Shelf: `orders` in `data/orders.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:364. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
