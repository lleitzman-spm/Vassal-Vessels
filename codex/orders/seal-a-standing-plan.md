---
type: "order"
id: "order:bind-plan"
title: "Seal a Standing Plan"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 499
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:bind-plan"
---

# Seal a Standing Plan

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

A sealed order with a condition written on the outside: 'if horsemen come within a hundred paces, brace'. The captain weighs it when it ARRIVES — he can refuse to take the envelope — but once he has taken it, the order fires INSTANTLY when the condition is met, with no rider, no delay and no second thoughts, because he agreed to it while he was calm. This is how you are in three places at once in a game that never pauses, and it is the loophole in the politics: you cannot make a disloyal lord obey you in a crisis, but you can get him to agree to something hypothetical at the morning briefing. See data/standing-plans.json.

*Verified verbatim against `data/orders.json`:499 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | Which of the three voices this belongs to: a standing charge always in force, an improvised order that must ride to its captain, or a pursuit policy. | `foresight` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `1` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `10` |
| `commitTicks` | How long you cannot change your mind after it starts. | `0` |
| `windupTicks` | How long before it begins to take effect at all. | `0` |
| `params` | What the order needs pointing at — a place, a unit, a friend, a facing. An order with nowhere to point is not an order. | `trigger`, `value`, `order`, `orderParams` |
| `theCost` | What issuing it costs you, in something other than coin — usually a contingent doing nothing, or a flank left thin. Every order below is a trade. | `A whole rider NOW, spent on something that has not happened yet — and if you guessed the wrong trigger, you spent him on nothing.` |

## What you would see on the field

> Nothing — until it fires, and a unit does exactly the right thing at exactly the right instant with no rider in sight.

*Shelf: `orders` in `data/orders.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:499. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
