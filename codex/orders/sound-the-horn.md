---
type: "order"
id: "order:horn"
title: "Sound the Horn"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 563
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:horn"
---

# Sound the Horn

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Three horn channels exist, and a horn is not an order: it is a TRIGGER you pull by hand. Any standing plan bound to that channel fires instantly, everywhere, with no rider and no heed check. This is how you make two contingents that cannot see each other act on the same heartbeat. A horn with no plan bound to it is just a noise.

*Verified verbatim against `data/orders.json`:563 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | Which of the three voices this belongs to: a standing charge always in force, an improvised order that must ride to its captain, or a pursuit policy. | `voice` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `0` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `0` |
| `commitTicks` | How long you cannot change your mind after it starts. | `0` |
| `windupTicks` | How long before it begins to take effect at all. | `0` |
| `params` | What the order needs pointing at — a place, a unit, a friend, a facing. An order with nowhere to point is not an order. | `channel` |
| `theCost` | What issuing it costs you, in something other than coin — usually a contingent doing nothing, or a flank left thin. Every order below is a trade. | `Nothing at all — which is why the cost sits earlier, in the rider you spent binding the plan and the guess you made about which crisis was coming.` |

## What you would see on the field

> One long note, and then several things happening at once in different parts of the field.

*Shelf: `orders` in `data/orders.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:563. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
