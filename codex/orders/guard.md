---
type: "order"
id: "order:guard"
title: "Guard"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 85
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:guard"
---

# Guard

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

How you protect archers, crossbowmen, the relic and the Crown Banner. Forgetting to do it is how most players lose their archers to one troop of light horse they never saw coming.

*Verified verbatim against `data/orders.json`:85 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `10` |
| `params` | *undocumented in the file's own `fields` block* | `friendlyUnit` |
| `compiles` | *undocumented in the file's own `fields` block* | `Stay within forty metres of a named friend and engage anything that threatens it. Never advance beyond the leash.` |
| `theCost` | *undocumented in the file's own `fields` block* | `A good unit tied to somebody else's shadow.` |

## What you would see on the field

> Two units moving as one, with one of them always between the other and the enemy.

*Shelf: `charges` in `data/orders.json`.*

## Units

- [[Crossbowmen]] — *this page names "Crossbowmen" literally*
- [[The Crown Banner]] — *this page names "The Crown Banner" literally*

## Orders

- [[Advance]] — *this page names "Advance" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:85. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
