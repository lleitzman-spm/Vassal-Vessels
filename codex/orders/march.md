---
type: "order"
id: "order:march"
title: "March"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 114
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:march"
---

# March

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The shape armies move in, never the shape they fight in. On a road it is half again as fast. Reinforcements arrive like this, and buying them the eight or nine seconds they need to deploy is your problem, not theirs.

*Verified verbatim against `data/orders.json`:114 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `5` |
| `compiles` | *undocumented in the file's own `fields` block* | `Column of route, four men wide, toward a named point. Engages nothing. Latecomers enter the field on this charge and must be given time and a new charge to shake out of it.` |
| `theCost` | *undocumented in the file's own `fields` block* | `A column has no front. Everything that hits it, hits it as a flank.` |

## What you would see on the field

> A long snake. If it is still a snake when the enemy arrives, somebody has made a serious mistake.

*Shelf: `charges` in `data/orders.json`.*

## Orders

- [[Charge]] — *this page names "Charge" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:114. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
