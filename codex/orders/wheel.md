---
type: "order"
id: "order:wheel"
title: "Wheel"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 203
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:wheel"
---

# Wheel

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Turn the whole line to face a different way without losing its shape. The order you give when somebody is coming round your side, and the question is always whether the rider arrives in time: a pike block wheels a quarter turn in eight and a half seconds, a troop of light horse in two. That race is why flanking works.

*Verified verbatim against `data/orders.json`:203 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | Which of the three voices this belongs to: a standing charge always in force, an improvised order that must ride to its captain, or a pursuit policy. | `movement` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `1` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `20` |
| `commitTicks` | How long you cannot change your mind after it starts. | `40` |
| `windupTicks` | How long before it begins to take effect at all. | `0` |
| `params` | What the order needs pointing at — a place, a unit, a friend, a facing. An order with nowhere to point is not an order. | `bearing` |
| `theCost` | What issuing it costs you, in something other than coin — usually a contingent doing nothing, or a flank left thin. Every order below is a trade. | `A hundred and eighty thousand cohesion — and if you are already fighting, forty more difficulty AND one tick of free enemy blows at two and a half times damage.` |

## What you would see on the field

> The line pivots like a gate, ragged at the outside end.

*Shelf: `orders` in `data/orders.json`.*

## Backlinks

### transition

- [[They come off the brace]] — *its `consumes` names `WHEEL` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:203. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
