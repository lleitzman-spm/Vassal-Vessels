---
type: "order"
id: "order:set-charge"
title: "Change the Standing Job"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 145
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:set-charge"
---

# Change the Standing Job

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Change what a contingent does when nobody is telling it anything. This is the order that makes the battle never wait for you: every contingent always has a job, and if you go quiet for four minutes the battle will still be fought.

*Verified verbatim against `data/orders.json`:145 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | Which of the three voices this belongs to: a standing charge always in force, an improvised order that must ride to its captain, or a pursuit policy. | `policy` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `1` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `5` |
| `commitTicks` | How long you cannot change your mind after it starts. | `0` |
| `windupTicks` | How long before it begins to take effect at all. | `20` |
| `params` | What the order needs pointing at — a place, a unit, a friend, a facing. An order with nowhere to point is not an order. | `chargeId`, `chargeParams` |
| `theCost` | What issuing it costs you, in something other than coin — usually a contingent doing nothing, or a flank left thin. Every order below is a trade. | `Cheap, but it takes a second to take hold — and the charge is the thing you should have got right before the battle started.` |

## What you would see on the field

> A change in what a contingent does when left alone.

*Shelf: `orders` in `data/orders.json`.*

## Orders

- [[Charge]] — *this page names "Charge" literally*

## Backlinks

### transition

- [[It becomes their job]] — *its `consumes` names `SET_CHARGE` by id*
- [[It stops mattering]] — *its `consumes` names `SET_CHARGE` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:145. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
