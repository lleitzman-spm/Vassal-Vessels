---
type: "order"
id: "order:ride-to"
title: "Move the Banner"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 544
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:ride-to"
---

# Move the Banner

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Move the Crown Banner. Wherever it stands, units that can see it take your orders in one second instead of nine, and every friend within a hundred metres is steadied. So the banner's position is your voice's position, and moving it is a strategic act. Put it forward and you can command the fighting line directly. Put it forward and it can be reached — and if it falls, every unit in the army loses a quarter of its will at once and every order you ever give afterwards is a courier ride.

*Verified verbatim against `data/orders.json`:544 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | Which of the three voices this belongs to: a standing charge always in force, an improvised order that must ride to its captain, or a pursuit policy. | `voice` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `0` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `0` |
| `commitTicks` | How long you cannot change your mind after it starts. | `0` |
| `windupTicks` | How long before it begins to take effect at all. | `0` |
| `params` | What the order needs pointing at — a place, a unit, a friend, a facing. An order with nowhere to point is not an order. | `point` |
| `theCost` | What issuing it costs you, in something other than coin — usually a contingent doing nothing, or a flank left thin. Every order below is a trade. | `While the banner moves your signal range HALVES. You go partly deaf across the whole field in order to be heard better in one part of it.` |

## What you would see on the field

> The tallest thing on the field, moving.

*Shelf: `orders` in `data/orders.json`.*

## Units

- [[The Crown Banner]] — *this page names "The Crown Banner" literally*

## Traits

- [[Command]] — *this page names "Command" literally*

## Backlinks

### transition

- [[He does not arrive]] — *its `consumes` names `RIDE_TO` by id*
- [[The courier goes]] — *its `consumes` names `RIDE_TO` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:544. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
