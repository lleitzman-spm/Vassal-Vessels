---
type: "order"
id: "order:plant-stakes"
title: "Plant Stakes"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 287
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:plant-stakes"
---

# Plant Stakes

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Sharpened poles driven in at an angle, points at a horse's chest: eight hundred of refusal over nine metres, nearly as good as a pike hedge, and unlike pikemen the archers keep shooting the whole time. The single strongest defensive position in the game. Give it early, give it where you are certain the horsemen must come, and be right, because you cannot take it back.

*Verified verbatim against `data/orders.json`:287 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | *undocumented in the file's own `fields` block* | `posture` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `1` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `15` |
| `commitTicks` | How long you cannot change your mind after it starts. | `1800` |
| `windupTicks` | How long before it begins to take effect at all. | `1800` |
| `params` | *undocumented in the file's own `fields` block* | *none* |
| `requires` | *undocumented in the file's own `fields` block* | `Stakes` |
| `theCost` | *undocumented in the file's own `fields` block* | `NINETY SECONDS standing in one place hammering poles into the ground while a battle happens — and the moment you move, the stakes stay behind.` |

## What you would see on the field

> A visible line of poles — and afterwards, cavalry stopped dead in front of archers who are still shooting.

*Shelf: `orders` in `data/orders.json`.*

## Keywords

- [[Stakes]] — *this page names "Stakes" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:287. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
