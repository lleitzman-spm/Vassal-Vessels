---
type: "order"
id: "order:loose-fire"
title: "Loose at Will"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 243
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:loose-fire"
---

# Loose at Will

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Shoot at whatever is in front of you as fast as you can. Simple and wasteful: at long range against a spread-out target you may burn eight shafts to put down one man. The archer who still has arrows when the enemy line finally comes forward is the archer who wins the battle.

*Verified verbatim against `data/orders.json`:243 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `category` | *undocumented in the file's own `fields` block* | `missile` |
| `courierCost` | Riders spent. Zero means it is your own voice or your own horn and costs no rider. | `1` |
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `5` |
| `commitTicks` | How long you cannot change your mind after it starts. | `0` |
| `windupTicks` | How long before it begins to take effect at all. | `0` |
| `params` | *undocumented in the file's own `fields` block* | *none* |
| `requires` | *undocumented in the file's own `fields` block* | `missileWeapon` |
| `theCost` | *undocumented in the file's own `fields` block* | `Arrows. A bowman has thirty-two and a crossbowman twenty-four, and when they are gone they are gone for the day.` |

## What you would see on the field

> A steady, ragged stream of arrows.

*Shelf: `orders` in `data/orders.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:243. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
