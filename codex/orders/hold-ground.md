---
type: "order"
id: "order:hold"
title: "Hold Ground"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 29
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:hold"
---

# Hold Ground

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The safest job and the right one nine times in ten for infantry. A line that keeps its shape cannot be pulled apart, and most defeats begin with somebody wandering forward.

*Verified verbatim against `data/orders.json`:29 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `0` |
| `compiles` | *undocumented in the file's own `fields` block* | `Stand on the ground you were given. Face the nearest threat. Brace if brace-capable and mounted enemies close within 120 m. Shoot anything in range. Do not follow an enemy who falls back. Recover cohesion and wind while unengaged.` |
| `pursuesRepelled` | *undocumented in the file's own `fields` block* | no |
| `theCost` | *undocumented in the file's own `fields` block* | `Tempo. Nothing bad happens and nothing good happens, and meanwhile the other fellow is choosing the moment.` |

## What you would see on the field

> A straight, patient line that stays a straight, patient line.

*Shelf: `charges` in `data/orders.json`.*

## Keywords

- [[Mounted]] — *this page names "Mounted" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:29. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
