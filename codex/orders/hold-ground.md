---
type: "order"
id: "order:hold"
title: "Hold Ground"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/orders.json"
source_line: 41
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "order:hold"
---

# Hold Ground

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The safest job and the right one nine times in ten for infantry. A line that keeps its shape cannot be pulled apart, and most defeats begin with somebody wandering forward.

*Verified verbatim against `data/orders.json`:41 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `difficulty` | Subtracted from the captain's heed when the order lands. High difficulty is where an unpaid wergild becomes a tactic that never happens. | `0` |
| `compiles` | What the order actually becomes when a captain accepts it, in plain words. This is the sentence to read if you want to know what will happen. | `Stand on the ground you were given. Face the nearest threat. Brace if brace-capable and mounted enemies close within 120 m. Shoot anything in range. Do not follow an enemy who falls back. Recover cohesion and wind while unengaged.` |
| `pursuesRepelled` | Whether the unit chases an enemy it has just driven off. Usually the thing you did not want it to do. | no |
| `theCost` | What issuing it costs you, in something other than coin — usually a contingent doing nothing, or a flank left thin. Every order below is a trade. | `Tempo. Nothing bad happens and nothing good happens, and meanwhile the other fellow is choosing the moment.` |

## What you would see on the field

> A straight, patient line that stays a straight, patient line.

*Shelf: `charges` in `data/orders.json`.*

## Keywords

- [[Mounted]] — *this page names "Mounted" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/orders.json`:41. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
