---
type: "season"
id: "season:highsun"
title: "Highsun"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/seasons.json"
source_line: 28
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "season:highsun"
---

# Highsun

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The campaigning season. Dry roads, long days, food in the fields to forage, and everybody expects a war. That cuts both ways: an idle summer after a victory costs the crown four standing, because the host wanted to be led and you did not lead it.

*Verified verbatim against `data/seasons.json`:28 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `modern` | *undocumented in the file's own `fields` block* | `summer` |
| `days` | *undocumented in the file's own `fields` block* | `90` |
| `marchSpeed` | Multiplier on every marching day. Winter roads are not a penalty; they are mud. | `1` |
| `fatigueWear` | Multiplier on the tiredness a day of marching adds — and the host arrives with that tiredness in its hands. | `1` |
| `forage` | What the countryside gives back to an army standing on it. Below 1.0 the granary drains faster than the arithmetic looks. | `1` |
| `willingnessPenalty` | Straight off every house's willingness, before anything else is counted. | `0` |
| `idleStandingPenalty` | *undocumented in the file's own `fields` block* | `-4` |
| `reachesTheBattle` | What the season is still doing once the fighting starts. | `Dry, firm ground: cavalry at its best and mud nowhere to be found.` |

*Shelf: `seasons` in `data/seasons.json`.*

## Backlinks

### Writs that specify it

- [[VASSAL VESSELS — The Constitution]] — *this writ names "Highsun" literally*
- [[WRIT — THE COURT]] — *this writ names "Highsun" literally*

---

*Generated by `tools/codex/emit.mjs` from `data/seasons.json`:28. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
