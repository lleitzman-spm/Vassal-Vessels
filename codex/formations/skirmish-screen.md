---
type: "formation"
id: "formation:screen"
title: "Skirmish Screen"
standing: "built"
standing_source: "derived"
source_path: "data/formations.json"
source_line: 135
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "formation:screen"
---

# Skirmish Screen

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Barely a formation: men scattered with two metres between them. Arrows essentially do not work on these people — nor does anything they might try to do to somebody, which is fine, because their job is to be a nuisance and go home.

*Verified verbatim against `data/formations.json`:135 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `manWidthMm` | How much frontage each man takes. Together with the rank depth this is what turns a number of men into a shape on the ground with a real width and a real flank. | `2000` |
| `rankDepthMm` | How much depth each rank takes. Deep formations are narrow, which is exactly the trade: hard to break through, easy to walk around. | `2200` |
| `targetRanksDefault` | How many ranks are engaged and fighting by default. Men in the ranks behind these are present but not swinging. | `2` |
| `missileDensityPermille` | How good a target the formation makes, in thousandths. Packed men are easier to hit, which is the standing argument against close order under archery. | `114` |
| `speedMulPermille` | Movement speed in thousandths. A tight formation moves slowly because keeping the line is what is slowing it down. | `1150` |
| `shockDeliveredMulPermille` | How much of a charge's force this formation delivers. Concentrated shapes hit harder because the weight arrives in one place. | `100` |
| `shockReceivedMulPermille` | How much of an incoming charge lands. A shape built to absorb shock spreads the blow over more men. | `200` |
| `cohesionCapPermille` | The highest cohesion this formation permits, in thousandths. Loose order cannot be neat — that is what makes it loose. | `400` |
| `changeTicks` | How long the unit takes to change into this formation. Time spent changing is time spent being neither one thing nor the other, and it is when units get caught. | `40` |

## What you would see on the field

> Not a shape. A sprinkle.

*Shelf: `formations` in `data/formations.json`.*

## Modules

- [[src/battle/geometry.ts]] — *this page names `missileDensityPermille`*

## Backlinks

### Units

- [[Bowmen]] — *its `allowedFormations` names `screen` by id*
- [[Foresters]] — *its `allowedFormations` names `screen` by id; its `defaultFormation` names `screen` by id*
- [[March Riders]] — *its `allowedFormations` names `screen` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/formations.json`:135. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
