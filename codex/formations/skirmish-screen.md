---
type: "formation"
id: "formation:screen"
title: "Skirmish Screen"
standing: "built"
standing_source: "derived"
source_path: "data/formations.json"
source_line: 107
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "formation:screen"
---

# Skirmish Screen

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Barely a formation: men scattered with two metres between them. Arrows essentially do not work on these people — nor does anything they might try to do to somebody, which is fine, because their job is to be a nuisance and go home.

*Verified verbatim against `data/formations.json`:107 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `manWidthMm` | *undocumented in the file's own `fields` block* | `2000` |
| `rankDepthMm` | *undocumented in the file's own `fields` block* | `2200` |
| `targetRanksDefault` | *undocumented in the file's own `fields` block* | `2` |
| `missileDensityPermille` | *undocumented in the file's own `fields` block* | `114` |
| `speedMulPermille` | *undocumented in the file's own `fields` block* | `1150` |
| `shockDeliveredMulPermille` | *undocumented in the file's own `fields` block* | `100` |
| `shockReceivedMulPermille` | *undocumented in the file's own `fields` block* | `200` |
| `cohesionCapPermille` | *undocumented in the file's own `fields` block* | `400` |
| `changeTicks` | *undocumented in the file's own `fields` block* | `40` |

## What you would see on the field

> Not a shape. A sprinkle.

*Shelf: `formations` in `data/formations.json`.*

## Modules

- [[src/battle/geometry.ts]] — *this page names `missileDensityPermille`*

## Backlinks

### Units

- [[Bowmen]] — *its `allowedFormations` names `screen` by id*
- [[Foresters]] — *its `allowedFormations` names `screen` by id; its `defaultFormation` names `screen` by id*
- [[March Riders]] — *its `allowedFormations` names `screen` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/formations.json`:107. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
