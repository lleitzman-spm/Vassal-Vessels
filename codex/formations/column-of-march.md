---
type: "formation"
id: "formation:column"
title: "Column of March"
standing: "built"
standing_source: "derived"
source_path: "data/formations.json"
source_line: 101
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "formation:column"
---

# Column of March

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Four men wide and long as a road — the shape armies move in, never the shape they fight in. On a road it is half again as fast. It is also helpless: only four men can fight, and a column caught in contact has no front at all — everything lands as a flank. Reinforcements arrive in column, and buying them the eight-plus seconds to shake out of it is your problem.

*Verified verbatim against `data/formations.json`:101 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `manWidthMm` | How much frontage each man takes. Together with the rank depth this is what turns a number of men into a shape on the ground with a real width and a real flank. | `700` |
| `rankDepthMm` | How much depth each rank takes. Deep formations are narrow, which is exactly the trade: hard to break through, easy to walk around. | `900` |
| `targetFiles` | How many files across are engaged. With the rank count, this decides how many men are actually in the fight rather than merely in the unit. | `4` |
| `speedMulPermille` | Movement speed in thousandths. A tight formation moves slowly because keeping the line is what is slowing it down. | `1300` |
| `roadSpeedMulPermille` | Movement speed along a road, in thousandths. Column is transport, not a fighting shape, and this is why you use it. | `1500` |
| `shockDeliveredMulPermille` | How much of a charge's force this formation delivers. Concentrated shapes hit harder because the weight arrives in one place. | `200` |
| `shockReceivedMulPermille` | How much of an incoming charge lands. A shape built to absorb shock spreads the blow over more men. | `1400` |
| `cohesionCapPermille` | The highest cohesion this formation permits, in thousandths. Loose order cannot be neat — that is what makes it loose. | `1000` |
| `changeTicks` | How long the unit takes to change into this formation. Time spent changing is time spent being neither one thing nor the other, and it is when units get caught. | `200` |

## What you would see on the field

> A long snake. If it is still a snake when the enemy arrives, somebody has made a serious mistake.

*Shelf: `formations` in `data/formations.json`.*

## Backlinks

### Units

- [[Foresters]] — *its `allowedFormations` names `column` by id*
- [[Free Company Horse]] — *its `allowedFormations` names `column` by id*
- [[Free Company Pikes]] — *its `allowedFormations` names `column` by id*
- [[Free Company Swords]] — *its `allowedFormations` names `column` by id*
- [[Household Guard]] — *its `allowedFormations` names `column` by id*
- [[Knights]] — *its `allowedFormations` names `column` by id*
- [[Levy Spearmen]] — *its `allowedFormations` names `column` by id*
- [[March Riders]] — *its `allowedFormations` names `column` by id*
- [[Marines]] — *its `allowedFormations` names `column` by id*
- [[Men-at-Arms]] — *its `allowedFormations` names `column` by id*
- [[Militia Spears]] — *its `allowedFormations` names `column` by id*
- [[Pick-men]] — *its `allowedFormations` names `column` by id*
- [[Relic Bearers]] — *its `allowedFormations` names `column` by id*
- [[Sworn Brothers]] — *its `allowedFormations` names `column` by id*
- [[The Crown Banner]] — *its `allowedFormations` names `column` by id*

### transition

- [[They step off]] — *its `consumes` names `column` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/formations.json`:101. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
