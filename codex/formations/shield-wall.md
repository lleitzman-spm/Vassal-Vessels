---
type: "formation"
id: "formation:shieldwall"
title: "Shield Wall"
standing: "built"
standing_source: "derived"
source_path: "data/formations.json"
source_line: 43
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "formation:shieldwall"
---

# Shield Wall

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Tighter than close order, shields overlapping so each man is partly covered by his neighbour's. Front shield up by a quarter, shock resistance up, speed down to a crawl — and it is the densest arrow target on the field. The trap: overlap only works while the line HOLDS. Below 300,000 cohesion the shield bonus halves, because the wall has stopped being a wall.

*Verified verbatim against `data/formations.json`:43 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `manWidthMm` | *undocumented in the file's own `fields` block* | `620` |
| `rankDepthMm` | *undocumented in the file's own `fields` block* | `900` |
| `targetRanksDefault` | *undocumented in the file's own `fields` block* | `4` |
| `missileDensityPermille` | *undocumented in the file's own `fields` block* | `896` |
| `speedMulPermille` | *undocumented in the file's own `fields` block* | `600` |
| `shockDeliveredMulPermille` | *undocumented in the file's own `fields` block* | `700` |
| `shockReceivedMulPermille` | *undocumented in the file's own `fields` block* | `1000` |
| `shockResistBonus` | *undocumented in the file's own `fields` block* | `60` |
| `frontShieldMulPermille` | *undocumented in the file's own `fields` block* | `1250` |
| `cohesionCapPermille` | *undocumented in the file's own `fields` block* | `1000` |
| `changeTicks` | *undocumented in the file's own `fields` block* | `60` |
| `requires` | *undocumented in the file's own `fields` block* | `ShieldWall` |

## What you would see on the field

> Shields visibly overlapping, and the whole thing moving at a crawl.

*Shelf: `formations` in `data/formations.json`.*

## Formations

- [[Close Order]] — *this page names "Close Order" literally*

## Modules

- [[src/battle/geometry.ts]] — *this page names `missileDensityPermille`*

## Backlinks

### Units

- [[Household Guard]] — *its `allowedFormations` names `shieldwall` by id*
- [[Levy Spearmen]] — *its `allowedFormations` names `shieldwall` by id*
- [[Marines]] — *its `allowedFormations` names `shieldwall` by id; its `defaultFormation` names `shieldwall` by id*
- [[Sworn Brothers]] — *its `allowedFormations` names `shieldwall` by id*

### Orders

- [[Aimed Volley]] — *this page names "Shield Wall" literally*

### Terrain

- [[Woodland]] — *this page names "Shield Wall" literally*

### Quirks

- [[Will Not Stand in Line]] — *this page names "Shield Wall" literally*

---

*Generated by `tools/codex/emit.mjs` from `data/formations.json`:43. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
