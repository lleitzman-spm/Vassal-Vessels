---
type: "formation"
id: "formation:ring"
title: "Ring of Points"
standing: "built"
standing_source: "derived"
source_path: "data/formations.json"
source_line: 91
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "formation:ring"
---

# Ring of Points

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

A ring of pikemen, points outward all the way round. It solves the pike block's one fatal weakness — that bracing is front-arc only — by having no flanks and no rear at all. Cavalry cannot touch it from any direction. And it cannot move. At all. Ever. So it becomes the densest, most obliging arrow target that has ever existed, standing perfectly still. The correct answer to being surrounded by horsemen, and the worst possible answer to anything else.

*Verified verbatim against `data/formations.json`:91 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `manWidthMm` | *undocumented in the file's own `fields` block* | `700` |
| `rankDepthMm` | *undocumented in the file's own `fields` block* | `900` |
| `shape` | *undocumented in the file's own `fields` block* | `ring` |
| `missileDensityPermille` | *undocumented in the file's own `fields` block* | `950` |
| `speedMulPermille` | *undocumented in the file's own `fields` block* | `0` |
| `allArcBrace` | *undocumented in the file's own `fields` block* | yes |
| `refusalMulPermille` | *undocumented in the file's own `fields` block* | `900` |
| `cohesionCapPermille` | *undocumented in the file's own `fields` block* | `1000` |
| `changeTicks` | *undocumented in the file's own `fields` block* | `200` |
| `requires` | *undocumented in the file's own `fields` block* | `CanBrace`, `DeepBlock` |

## What you would see on the field

> A hedgehog. Beautiful, motionless, and doomed if the enemy has bows.

*Shelf: `formations` in `data/formations.json`.*

## Modules

- [[src/battle/geometry.ts]] — *this page names `missileDensityPermille`*

## Backlinks

### Units

- [[Free Company Pikes]] — *its `allowedFormations` names `ring` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/formations.json`:91. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
