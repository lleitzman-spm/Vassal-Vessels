---
type: "troop-source"
id: "troop-source:mercenary"
title: "A free company"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/troop-sources.json"
source_line: 32
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "troop-source:mercenary"
---

# A free company

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Professional soldiers who will fight for anybody. They are very good at their job and they have no reason at all to die for you. They convert money into men, which is the only escape from a shrinking realm — and they insult every vassal who was not asked.

*Verified verbatim against `data/troop-sources.json`:32 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `coin` | *undocumented in the file's own `fields` block* | `2 crowns a man to contract, then 4 a man a season` |
| `daysToStand` | *undocumented in the file's own `fields` block* | `8 to 20 — they must be found and they must ride` |
| `quality` | *undocumented in the file's own `fields` block* | `high, veteran, hard` |
| `whoseMenTheyAre` | *undocumented in the file's own `fields` block* | `nobody's. They serve the purse.` |
| `politicalPrice` | *undocumented in the file's own `fields` block* | `every house that sat idle while you hired foreigners takes a grudge; the Church takes 6 off your legitimacy; unpaid, their treachery climbs 12 a season and they may simply ride off the field` |
| `whenToReachForIt` | *undocumented in the file's own `fields` block* | `When your musters are bleeding and your coffer is not. The ladder out of a losing streak that does not require anybody to like you.` |

*Shelf: `sources` in `data/troop-sources.json`.*

## Backlinks

### Quirks

- [[Fights For Wages]] — *this page names "A free company" literally*

---

*Generated by `tools/codex/emit.mjs` from `data/troop-sources.json`:32. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
