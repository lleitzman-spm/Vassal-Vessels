---
type: "place"
id: "place:muster-answered"
title: "Answers In"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 504
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:muster-answered"
---

# Answers In

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Every house has decided what to send, and the answer is a reading, not a roll — willingness computed from loyalty, cause, season, grudges, distance and an empty cup. Whoever is missing, you can point at the day you lost them.

*Verified verbatim against `data/places.json`:504 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `muster` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Replies coming back, some of them thinner than you hoped.

*Shelf: `places` in `data/places.json`.*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## Backlinks

### transition

- [[Lords collect their men]] — *its `from` names `muster-answered` by id*
- [[The answers come back]] — *its `to` names `muster-answered` by id*
- [[You send them home]] — *its `from` names `muster-answered` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:504. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
