---
type: "place"
id: "place:heed-defy"
title: "Defy"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 323
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:heed-defy"
---

# Defy

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

He refuses outright. This is not a random misfortune — a captain reaches this only when the political arithmetic has been against you for a long time, and the herald will name the reason. Somebody warned you.

*Verified verbatim against `data/places.json`:323 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `heed` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `DEFY` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `| "DEFY"` |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> The courier rides back alone.

*Shelf: `places` in `data/places.json`.*

## Answers

- [[Refuses]] — *this page names "Refuses" literally*

## flow

- [[The Captain's Heed]] — *its `flow` names `heed` by id*

## Backlinks

### flow

- [[The Captain's Heed]] — *its `terminals` names `heed-defy` by id*

### transition

- [["No."]] — *its `to` names `heed-defy` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:323. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
