---
type: "place"
id: "place:heed-hard-hedge"
title: "Hard Hedge"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 312
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:heed-hard-hedge"
---

# Hard Hedge

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Hedging with contempt. He goes through the motions well enough that he cannot be accused of refusing, and commits nothing whatsoever.

*Verified verbatim against `data/places.json`:312 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `heed` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `HARD-HEDGE` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `| "HARD-HEDGE"` |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> A token gesture in the direction of the order, and no real movement at all.

*Shelf: `places` in `data/places.json`.*

## flow

- [[The Captain's Heed]] — *its `flow` names `heed` by id*

## Backlinks

### flow

- [[The Captain's Heed]] — *its `terminals` names `heed-hard-hedge` by id*

### transition

- [[A gesture, and nothing more]] — *its `to` names `heed-hard-hedge` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:312. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
