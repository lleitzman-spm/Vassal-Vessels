---
type: "place"
id: "place:muster-absorbed"
title: "Absorbed"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 549
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:muster-absorbed"
---

# Absorbed

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The aftermath has become RECORDS — deaths, ransoms, rewards, grudges, deeds. Nothing about the battle is stored as a number anywhere; it is all acts on the chronicle, and every future reading computes itself from them.

*Verified verbatim against `data/places.json`:549 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `muster` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> Clerks writing, and a great many people finding out things they will not forget.

*Shelf: `places` in `data/places.json`.*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## Backlinks

### flow

- [[The Muster]] — *its `terminals` names `muster-absorbed` by id*

### transition

- [[It goes into the record]] — *its `to` names `muster-absorbed` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:549. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
