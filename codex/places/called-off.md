---
type: "place"
id: "place:muster-abandoned"
title: "Called Off"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 558
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:muster-abandoned"
---

# Called Off

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You summoned a realm and then did not use it. The men still lost the days, the vessels still drained, and the resentment is real — calling up an army and sending it home is one of the more expensive ways to do nothing.

*Verified verbatim against `data/places.json`:558 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `muster` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> Men going home, unimpressed.

*Shelf: `places` in `data/places.json`.*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## Backlinks

### flow

- [[The Muster]] — *its `terminals` names `muster-abandoned` by id*

### transition

- [[You send them home]] — *its `to` names `muster-abandoned` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:558. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
