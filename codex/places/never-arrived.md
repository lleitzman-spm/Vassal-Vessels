---
type: "place"
id: "place:order-lost"
title: "Never Arrived"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 388
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:order-lost"
---

# Never Arrived

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The courier died on the way. You will not be told. From where you are standing, an order that was never received and an order that was defied look identical, and that ambiguity is deliberate.

*Verified verbatim against `data/places.json`:388 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `order` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> Nothing. Which is exactly the problem.

*Shelf: `places` in `data/places.json`.*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## Backlinks

### flow

- [[An Order's Journey]] — *its `terminals` names `order-lost` by id*

### transition

- [[He does not arrive]] — *its `to` names `order-lost` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:388. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
