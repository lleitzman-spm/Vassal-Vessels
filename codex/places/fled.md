---
type: "place"
id: "place:nerve-fled"
title: "Fled"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 116
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:nerve-fled"
---

# Fled

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Off the field and not coming back. These men are not dead — most of them will be counted alive in the aftermath and will be there next season — but they are no use to you today.

*Verified verbatim against `data/places.json`:116 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `nerve` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `FLED` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `| "FLED"` |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> Gone. Dust on the road out.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### flow

- [[A Unit's Nerve]] — *its `terminals` names `nerve-fled` by id*

### transition

- [[They are gone]] — *its `to` names `nerve-fled` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:116. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
