---
type: "place"
id: "place:vessel-dry"
title: "Dry"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 585
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:vessel-dry"
---

# Dry

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The cup is empty. They owe you nothing more this year. You may still call — and they may still come — but every day past the line costs willingness, and it costs more the second time and more again the third.

*Verified verbatim against `data/places.json`:585 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `vessel` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> A very long pause before the answer comes back.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A House's Vessel]] — *its `flow` names `vessel` by id*

## Backlinks

### transition

- [[The cup runs out]] — *its `to` names `vessel-dry` by id*
- [[The service year turns]] — *its `from` names `vessel-dry` by id*
- [[You call them anyway]] — *its `from` names `vessel-dry` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:585. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
