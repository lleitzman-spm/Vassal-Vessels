---
type: "place"
id: "place:footing-none"
title: "Loose Footing"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 138
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:footing-none"
---

# Loose Footing

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Doing nothing special. A unit in this state can move freely and turn quickly — and will be ridden straight over by cavalry, because nothing is refusing the horses.

*Verified verbatim against `data/places.json`:138 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `footing` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `NONE` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `export type Posture = "NONE" | "BRACED" | "SHOOTING" | "STAKED";` |

## What it beats, and what beats it

- **Role:** entry

## What you would see on the field

> Weapons carried, not set. Men shifting their feet.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A Unit's Footing]] — *its `flow` names `footing` by id*

## Backlinks

### flow

- [[A Unit's Footing]] — *its `entry` names `footing-none` by id*

### transition

- [[Hold!]] — *its `to` names `footing-none` by id*
- [[Loose!]] — *its `from` names `footing-none` by id*
- [[Set!]] — *its `from` names `footing-none` by id*
- [[Stakes in]] — *its `from` names `footing-none` by id*
- [[They come off the brace]] — *its `to` names `footing-none` by id*
- [[They leave the stakes]] — *its `to` names `footing-none` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:138. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
