---
type: "place"
id: "place:order-active"
title: "In Force"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 361
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:order-active"
---

# In Force

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The order has been accepted in some form and is now the contingent's job. It stays its job until it is finished, until you replace it, or until it stops being possible.

*Verified verbatim against `data/places.json`:361 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `order` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> A contingent doing a thing on purpose.

*Shelf: `places` in `data/places.json`.*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## Backlinks

### transition

- [[It becomes their job]] — *its `to` names `order-active` by id*
- [[It is carried out]] — *its `from` names `order-active` by id*
- [[It stops mattering]] — *its `from` names `order-active` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:361. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
