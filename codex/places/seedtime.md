---
type: "place"
id: "place:year-seedtime"
title: "Seedtime"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 433
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:year-seedtime"
---

# Seedtime

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Spring. The roads are bad, the granaries are at their lowest, and calling men off the sowing costs a sixth of the year's food. Spring wars are for people who had no choice.

*Verified verbatim against `data/places.json`:433 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `year` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `seedtime` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `data/seasons.json` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `"id": "seedtime"` |

## What it beats, and what beats it

- **Role:** entry

## What you would see on the field

> Mud, empty barns, and men who want to be planting.

*Shelf: `places` in `data/places.json`.*

## Seasons

- [[Seedtime (season)]] — *this page names "Seedtime" literally*

## flow

- [[The Year]] — *its `flow` names `year` by id*

## Backlinks

### flow

- [[The Year]] — *its `entry` names `year-seedtime` by id*

### transition

- [[The roads dry]] — *its `from` names `year-seedtime` by id*
- [[The year turns]] — *its `to` names `year-seedtime` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:433. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
