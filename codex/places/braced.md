---
type: "place"
id: "place:footing-braced"
title: "Braced"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 149
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:footing-braced"
---

# Braced

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Set against a charge. This is the single most important state in the game: a braced line projects REFUSAL out in front of it, and a horse will not willingly run onto it. It works only to the front, it takes time to set, and it is gone the instant the unit moves. Everything about cavalry is an argument about whether the spearmen got here in time.

*Verified verbatim against `data/places.json`:149 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `footing` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `BRACED` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `export type Posture = "NONE" | "BRACED" | "SHOOTING" | "STAKED";` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Butts in the earth, points level, absolutely still.

*Shelf: `places` in `data/places.json`.*

## Orders

- [[Charge]] — *this page names "Charge" literally*

## flow

- [[A Unit's Footing]] — *its `flow` names `footing` by id*

## Backlinks

### transition

- [[Set!]] — *its `to` names `footing-braced` by id*
- [[They come off the brace]] — *its `from` names `footing-braced` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:149. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
