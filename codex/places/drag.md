---
type: "place"
id: "place:heed-drag"
title: "Drag"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 279
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:heed-drag"
---

# Drag

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

He obeys slowly. No refusal, no argument, nothing you can point at — just enough delay that the moment has passed. The most infuriating outcome in the game and one of the hardest to prove.

*Verified verbatim against `data/places.json`:279 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `heed` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `DRAG` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `| "DRAG"` |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> Nothing, for a while. Then the thing you asked for, too late to matter.

*Shelf: `places` in `data/places.json`.*

## flow

- [[The Captain's Heed]] — *its `flow` names `heed` by id*

## Backlinks

### flow

- [[The Captain's Heed]] — *its `terminals` names `heed-drag` by id*

### transition

- [["Presently."]] — *its `to` names `heed-drag` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:279. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
