---
type: "place"
id: "place:treachery-acting"
title: "Acting Against You"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 415
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:treachery-acting"
---

# Acting Against You

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Working against you without having said so. They have not changed sides — they are simply making sure you lose, which is cheaper and safer for them than switching.

*Verified verbatim against `data/places.json`:415 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `treachery` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Orders quietly ignored, a flank left open that did not need to be, a withdrawal nobody sanctioned.

*Shelf: `places` in `data/places.json`.*

## flow

- [[Turning]] — *its `flow` names `treachery` by id*

## Backlinks

### transition

- [[The banner turns]] — *its `from` names `treachery-acting` by id*
- [[They start working against you]] — *its `to` names `treachery-acting` by id*
- [[You settle with them]] — *its `from` names `treachery-acting` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:415. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
