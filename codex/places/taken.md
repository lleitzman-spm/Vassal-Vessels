---
type: "place"
id: "place:captive-taken"
title: "Taken"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 666
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:captive-taken"
---

# Taken

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Captured rather than killed, which is a decision somebody made in the moment and is worth far more than a corpse. Whether it happens at all depends on who was chasing and how badly they wanted money.

*Verified verbatim against `data/places.json`:666 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `captive` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** entry

## What you would see on the field

> A man being pulled off a horse and not killed.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A Captive]] — *its `flow` names `captive` by id*

## Backlinks

### flow

- [[A Captive]] — *its `entry` names `captive-taken` by id*

### transition

- [[A price is named]] — *its `from` names `captive-taken` by id*
- [[He dies in captivity]] — *its `from` names `captive-taken` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:666. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
