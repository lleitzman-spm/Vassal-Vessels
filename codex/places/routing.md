---
type: "place"
id: "place:nerve-routing"
title: "Routing"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 94
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:nerve-routing"
---

# Routing

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Broken. Cohesion is thrown away entirely, the formation opens to loose, the current order is discarded and the unit runs for its own baseline. A routing unit fights nobody and is killed cheaply by anyone who bothers to chase. It may turn and stand exactly once.

*Verified verbatim against `data/places.json`:94 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `nerve` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `ROUTING` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `| "ROUTING"` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> The unit stops being a unit. Men drop shields and run, and they run faster than they ever moved forward.

*Shelf: `places` in `data/places.json`.*

## Quirks

- [[May Turn]] — *this page names "May Turn" literally*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### transition

- [[They are destroyed where they stood]] — *its `from` names `nerve-routing` by id*
- [[They are gone]] — *its `from` names `nerve-routing` by id*
- [[They break]] — *its `to` names `nerve-routing` by id*
- [[They turn and stand]] — *its `from` names `nerve-routing` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:94. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
