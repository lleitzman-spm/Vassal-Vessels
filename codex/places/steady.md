---
type: "place"
id: "place:nerve-steady"
title: "Steady"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 61
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:nerve-steady"
---

# Steady

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The men are fine. They will advance when told, hold when told, and take casualties without looking over their shoulders. Steady units are also the ones that steady OTHERS — a steady unit nearby is worth real nerve to a shaken neighbour.

*Verified verbatim against `data/places.json`:61 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `nerve` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `STEADY` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `| "STEADY"` |

## What it beats, and what beats it

- **Role:** entry

## What you would see on the field

> A straight line, level weapons, men looking forward.

*Shelf: `places` in `data/places.json`.*

## Orders

- [[Advance]] — *this page names "Advance" literally*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### flow

- [[A Unit's Nerve]] — *its `entry` names `nerve-steady` by id*

### transition

- [[They are destroyed where they stood]] — *its `from` names `nerve-steady` by id*
- [[They are themselves again]] — *its `to` names `nerve-steady` by id*
- [[They begin to feel it]] — *its `from` names `nerve-steady` by id*
- [[They break]] — *its `from` names `nerve-steady` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:61. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
