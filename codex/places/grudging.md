---
type: "place"
id: "place:treachery-grudging"
title: "Grudging"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 406
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:treachery-grudging"
---

# Grudging

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Treachery is climbing. Nothing has happened yet, but this contingent is now a thing you should be watching rather than counting on.

*Verified verbatim against `data/places.json`:406 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `treachery` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Present, engaged, and somehow always the last to close.

*Shelf: `places` in `data/places.json`.*

## flow

- [[Turning]] — *its `flow` names `treachery` by id*

## Backlinks

### transition

- [[Something changes in them]] — *its `to` names `treachery-grudging` by id*
- [[They start working against you]] — *its `from` names `treachery-grudging` by id*
- [[You settle with them]] — *its `from` names `treachery-grudging` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:406. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
