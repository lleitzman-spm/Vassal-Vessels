---
type: "place"
id: "place:order-riding"
title: "Riding"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 343
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:order-riding"
---

# Riding

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

In transit. The courier is a real thing at a real place moving at a real speed, and he can be killed — orders sent across the front of an enemy advance frequently never arrive. Distance is delay, and delay is why you plan.

*Verified verbatim against `data/places.json`:343 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `order` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> A single horseman crossing the field at a gallop, taking the shortest line and not the safest one.

*Shelf: `places` in `data/places.json`.*

## Orders

- [[Advance]] — *this page names "Advance" literally*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## Backlinks

### transition

- [[He does not arrive]] — *its `from` names `order-riding` by id*
- [[He reaches the banner]] — *its `from` names `order-riding` by id*
- [[The courier goes]] — *its `to` names `order-riding` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:343. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
