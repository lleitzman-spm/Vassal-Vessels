---
type: "place"
id: "place:treachery-true"
title: "True"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 397
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:treachery-true"
---

# True

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Loyal enough. Whatever this house thinks of you privately, it is here and it is fighting.

*Verified verbatim against `data/places.json`:397 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `treachery` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** entry

## What you would see on the field

> A contingent fighting your battle.

*Shelf: `places` in `data/places.json`.*

## flow

- [[Turning]] — *its `flow` names `treachery` by id*

## Backlinks

### flow

- [[Turning]] — *its `entry` names `treachery-true` by id*

### transition

- [[Something changes in them]] — *its `from` names `treachery-true` by id*
- [[You settle with them]] — *its `to` names `treachery-true` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:397. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
