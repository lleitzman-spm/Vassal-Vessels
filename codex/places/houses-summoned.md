---
type: "place"
id: "place:muster-summoned"
title: "Houses Summoned"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 495
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:muster-summoned"
---

# Houses Summoned

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The call has gone out to named houses, at a named place, by a named day. From here it is out of your hands and into theirs.

*Verified verbatim against `data/places.json`:495 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `muster` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Riders leaving in every direction with sealed letters.

*Shelf: `places` in `data/places.json`.*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## Backlinks

### transition

- [[The answers come back]] — *its `from` names `muster-summoned` by id*
- [[The call goes out]] — *its `to` names `muster-summoned` by id*
- [[You send them home]] — *its `from` names `muster-summoned` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:495. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
