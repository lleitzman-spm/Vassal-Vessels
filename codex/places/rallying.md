---
type: "place"
id: "place:nerve-rallying"
title: "Rallying"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 105
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:nerve-rallying"
---

# Rallying

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The one second chance in the game. A routing unit that has got its nerve back up, held it there, has no enemy near it, and is either beside its captain or back on its own baseline, will stop and re-form. It comes back SHAKEN, not steady, and it will not do this twice.

*Verified verbatim against `data/places.json`:105 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `nerve` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `RALLYING` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `| "RALLYING"` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> The running stops. Men turn round, out of breath, and start finding each other again.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### transition

- [[They are destroyed where they stood]] — *its `from` names `nerve-rallying` by id*
- [[They re-form]] — *its `from` names `nerve-rallying` by id*
- [[They turn and stand]] — *its `to` names `nerve-rallying` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:105. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
