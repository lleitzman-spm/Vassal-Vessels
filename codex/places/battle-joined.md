---
type: "place"
id: "place:battle-joined"
title: "Battle Joined"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 28
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:battle-joined"
---

# Battle Joined

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The real fight. All thirteen phases run every tick, orders ride, men shoot, lines meet and nerve starts to go. This is where the overwhelming majority of a battle's ticks are spent.

*Verified verbatim against `data/places.json`:28 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `battle` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `BATTLE` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `export type BattlePhase = "DEPLOY" | "BATTLE" | "PURSUIT" | "ENDED";` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Everything at once, which is rather the problem.

*Shelf: `places` in `data/places.json`.*

## Modules

- [[src/battle/types.ts]] — *this page names `BattlePhase`*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## Backlinks

### transition

- [[An army comes apart]] — *its `from` names `battle-joined` by id*
- [[Both sides break contact]] — *its `from` names `battle-joined` by id*
- [[The day runs out (transition)]] — *its `from` names `battle-joined` by id*
- [[The lines start forward]] — *its `to` names `battle-joined` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:28. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
