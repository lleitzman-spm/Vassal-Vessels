---
type: "place"
id: "place:muster-stood"
title: "The Host Stands"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 531
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:muster-stood"
---

# The Host Stands

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Everyone who is coming has come. This is the moment the court half of the game is scored: what you actually have, who is missing, how tired they are, and how much they will take from you.

*Verified verbatim against `data/places.json`:531 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `muster` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> An army, in a field, smaller than the one you imagined.

*Shelf: `places` in `data/places.json`.*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## Backlinks

### transition

- [[The battle is fought]] — *its `from` names `muster-stood` by id*
- [[The host stands]] — *its `to` names `muster-stood` by id*
- [[You send them home]] — *its `from` names `muster-stood` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:531. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
