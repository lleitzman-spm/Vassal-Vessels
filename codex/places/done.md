---
type: "place"
id: "place:grievance-done"
title: "Done"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 603
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:grievance-done"
---

# Done

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The wrong has happened and gone into the record. Until the wronged house learns of it, it does absolutely nothing — a secret injustice is genuinely free, right up until it is not.

*Verified verbatim against `data/places.json`:603 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `grievance` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** entry

## What you would see on the field

> Nothing at all, yet.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A Grievance]] — *its `flow` names `grievance` by id*

## Backlinks

### flow

- [[A Grievance]] — *its `entry` names `grievance-done` by id*

### transition

- [[They find out]] — *its `from` names `grievance-done` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:603. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
