---
type: "place"
id: "place:treachery-defected"
title: "Turned"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 424
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:treachery-defected"
---

# Turned

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Changed sides, on the field, in front of everyone. It only happens when the day is already going badly — a traitor is a coward with arithmetic — and every point that got them here was on the record months ago.

*Verified verbatim against `data/places.json`:424 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `treachery` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> A banner going down and coming back up facing the other way.

*Shelf: `places` in `data/places.json`.*

## flow

- [[Turning]] — *its `flow` names `treachery` by id*

## Backlinks

### flow

- [[Turning]] — *its `terminals` names `treachery-defected` by id*

### transition

- [[The banner turns]] — *its `to` names `treachery-defected` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:424. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
