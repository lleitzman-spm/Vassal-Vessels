---
type: "place"
id: "place:footing-staked"
title: "Staked"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 171
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:footing-staked"
---

# Staked

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Behind planted stakes. Stakes refuse horses the way a braced spear line does, with two enormous differences: they do not get tired and they do not need the men to stand still. Planting them takes time you usually do not have, which is why the archers who manage it are the ones who win.

*Verified verbatim against `data/places.json`:171 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `footing` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `STAKED` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `export type Posture = "NONE" | "BRACED" | "SHOOTING" | "STAKED";` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> A hedge of sharpened poles in the ground, angled outward at a horse's chest.

*Shelf: `places` in `data/places.json`.*

## Keywords

- [[Stakes]] — *this page names "Stakes" literally*

## flow

- [[A Unit's Footing]] — *its `flow` names `footing` by id*

## Backlinks

### transition

- [[Stakes in]] — *its `to` names `footing-staked` by id*
- [[They leave the stakes]] — *its `from` names `footing-staked` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:171. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
