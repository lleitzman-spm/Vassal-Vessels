---
type: "place"
id: "place:vessel-full"
title: "Full"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 567
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:vessel-full"
---

# Full

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The year's service is untouched. Call them and they come without complaint — this is what the enfeoffment bought you and it refills once a year whether you used it or not.

*Verified verbatim against `data/places.json`:567 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `vessel` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** entry

## What you would see on the field

> A house that answers cheerfully.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A House's Vessel]] — *its `flow` names `vessel` by id*

## Backlinks

### flow

- [[A House's Vessel]] — *its `entry` names `vessel-full` by id*

### transition

- [[The first day is served]] — *its `from` names `vessel-full` by id*
- [[The service year turns]] — *its `to` names `vessel-full` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:567. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
