---
type: "place"
id: "place:footing-shooting"
title: "Shooting"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 160
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:footing-shooting"
---

# Shooting

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Loosing at something. Shooting costs ammunition, which does not come back, and a unit that is shooting is not bracing — a fact archers rediscover the moment horsemen appear on their flank.

*Verified verbatim against `data/places.json`:160 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `footing` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `SHOOTING` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `export type Posture = "NONE" | "BRACED" | "SHOOTING" | "STAKED";` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Volleys going out on a rhythm; men reaching for the quiver between them.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A Unit's Footing]] — *its `flow` names `footing` by id*

## Backlinks

### transition

- [[Hold!]] — *its `from` names `footing-shooting` by id*
- [[Loose!]] — *its `to` names `footing-shooting` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:160. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
