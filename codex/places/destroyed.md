---
type: "place"
id: "place:nerve-destroyed"
title: "Destroyed"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 127
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:nerve-destroyed"
---

# Destroyed

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Killed or taken where they stood, without breaking. Rare, grim, and worth far more to a herald than a rout: units that die in place are the ones songs get written about, and the deed goes into the record.

*Verified verbatim against `data/places.json`:127 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `nerve` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `DESTROYED` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `| "DESTROYED"` |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> A shape on the ground where a unit used to be.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### flow

- [[A Unit's Nerve]] — *its `terminals` names `nerve-destroyed` by id*

### transition

- [[They are destroyed where they stood]] — *its `to` names `nerve-destroyed` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:127. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
