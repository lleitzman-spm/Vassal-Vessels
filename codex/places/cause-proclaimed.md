---
type: "place"
id: "place:muster-proclaimed"
title: "Cause Proclaimed"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 486
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:muster-proclaimed"
---

# Cause Proclaimed

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You have said why. The cause you pick sets your legitimacy, and legitimacy is the largest single term in whether anybody comes. A war of defence writes itself; a war of ambition you will have to pay for in favours.

*Verified verbatim against `data/places.json`:486 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `muster` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Criers at every market cross.

*Shelf: `places` in `data/places.json`.*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## Backlinks

### transition

- [[The call goes out]] — *its `from` names `muster-proclaimed` by id*
- [[You name your cause]] — *its `to` names `muster-proclaimed` by id*
- [[You send them home]] — *its `from` names `muster-proclaimed` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:486. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
