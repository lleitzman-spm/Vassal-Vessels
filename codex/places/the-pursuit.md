---
type: "place"
id: "place:battle-pursuit"
title: "The Pursuit"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 39
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:battle-pursuit"
---

# The Pursuit

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

An army has broken and the chase is on. The rules quietly change: the command phase stops running entirely, so NOBODY IS LISTENING — every order you send into a pursuit is thrown away. This is where most of the day's killing and nearly all of its capturing happens, and it is where a won battle gets thrown away by a cavalry commander who will not rein in.

*Verified verbatim against `data/places.json`:39 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `battle` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `PURSUIT` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `export type BattlePhase = "DEPLOY" | "BATTLE" | "PURSUIT" | "ENDED";` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> One army in pieces running away, and the other coming apart chasing it.

*Shelf: `places` in `data/places.json`.*

## Traits

- [[Command]] — *this page names "Command" literally*

## Modules

- [[src/battle/types.ts]] — *this page names `BattlePhase`*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## Backlinks

### transition

- [[An army comes apart]] — *its `to` names `battle-pursuit` by id*
- [[The chase burns out (transition)]] — *its `from` names `battle-pursuit` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:39. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
