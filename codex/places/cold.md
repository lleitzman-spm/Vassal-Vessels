---
type: "place"
id: "place:grievance-cold"
title: "Cold"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 639
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:grievance-cold"
---

# Cold

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Faded to its residue. Still on the record, still read, still counted — just small. Enough of these and a house you have never actually wronged badly will still refuse you.

*Verified verbatim against `data/places.json`:639 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `grievance` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> Nothing you would notice. Which is not the same as nothing.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A Grievance]] — *its `flow` names `grievance` by id*

## Backlinks

### flow

- [[A Grievance]] — *its `terminals` names `grievance-cold` by id*

### transition

- [[It goes cold]] — *its `to` names `grievance-cold` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:639. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
