---
type: "place"
id: "place:vessel-beyond"
title: "Beyond the Vessel"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 594
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:vessel-beyond"
---

# Beyond the Vessel

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Serving past what was owed. It is not forbidden and that is the point: the cost is not a refusal, it is a grievance that compounds, and grievances are what decide whether an order is obeyed a year from now.

*Verified verbatim against `data/places.json`:594 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `vessel` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Men who are here because they were told to be, and want you to know it.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A House's Vessel]] — *its `flow` names `vessel` by id*

## Backlinks

### transition

- [[The service year turns]] — *its `from` names `vessel-beyond` by id*
- [[You call them anyway]] — *its `to` names `vessel-beyond` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:594. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
