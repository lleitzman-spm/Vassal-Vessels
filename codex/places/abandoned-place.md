---
type: "place"
id: "place:order-abandoned"
title: "Abandoned (place)"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 379
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:order-abandoned"
---

# Abandoned (place)

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Overtaken by events — the target is gone, the ground is taken, the unit broke, or you sent something newer. Not a failure so much as a fact.

*Verified verbatim against `data/places.json`:379 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `order` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** terminal

## What you would see on the field

> A contingent giving up on something mid-way and doing something else.

*Shelf: `places` in `data/places.json`.*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## Backlinks

### flow

- [[An Order's Journey]] — *its `terminals` names `order-abandoned` by id*

### transition

- [[It is refused]] — *its `to` names `order-abandoned` by id*
- [[It stops mattering]] — *its `to` names `order-abandoned` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:379. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
