---
type: "place"
id: "place:pace-advance"
title: "Advancing"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 204
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:pace-advance"
---

# Advancing

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

The working pace of a battle. Fast enough to matter, slow enough that the formation survives the trip.

*Verified verbatim against `data/places.json`:204 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `pace` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `ADVANCE` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `export type SpeedTier = "STOP" | "WALK" | "ADVANCE" | "RUN" | "CHARGE" | "ROUT";` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Purposeful, faster than a walk, the line still recognisably a line.

*Shelf: `places` in `data/places.json`.*

## Orders

- [[Advance]] — *this page names "Advance" literally*
- [[Charge]] — *this page names "Charge" literally*

## Modules

- [[src/battle/types.ts]] — *this page names `SpeedTier`*

## flow

- [[A Unit's Pace]] — *its `flow` names `pace` by id*

## Backlinks

### transition

- [[Halt]] — *its `from` names `pace-advance` by id*
- [[They pick it up]] — *its `to` names `pace-advance` by id*
- [[They run]] — *its `from` names `pace-advance` by id*
- [[They run the other way]] — *its `from` names `pace-advance` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:204. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
