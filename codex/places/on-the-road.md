---
type: "place"
id: "place:muster-marching"
title: "On the Road"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 522
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:muster-marching"
---

# On the Road

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Real leagues at a real pace, wearing men out as they go. This is where the season does its damage and where a house that lives far away arrives as less than it left.

*Verified verbatim against `data/places.json`:522 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `muster` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Columns on bad roads, getting slower.

*Shelf: `places` in `data/places.json`.*

## flow

- [[The Muster]] — *its `flow` names `muster` by id*

## Backlinks

### transition

- [[The host stands]] — *its `from` names `muster-marching` by id*
- [[They take the road]] — *its `to` names `muster-marching` by id*
- [[You send them home]] — *its `from` names `muster-marching` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:522. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
