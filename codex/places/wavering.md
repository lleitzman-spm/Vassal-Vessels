---
type: "place"
id: "place:nerve-wavering"
title: "Wavering"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 83
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:nerve-wavering"
---

# Wavering

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

About to go. The men at the back have already decided; the men at the front have not noticed yet. This is the last state from which a captain can save a unit, and the reason exhorting exists.

*Verified verbatim against `data/places.json`:83 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `nerve` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `WAVERING` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `| "WAVERING"` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> The back ranks start drifting rearward while the front ranks are still swinging. The unit gets visibly deeper and narrower.

*Shelf: `places` in `data/places.json`.*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

### transition

- [[The back ranks start to drift]] — *its `to` names `nerve-wavering` by id*
- [[They are destroyed where they stood]] — *its `from` names `nerve-wavering` by id*
- [[They break]] — *its `from` names `nerve-wavering` by id*
- [[They get a grip]] — *its `from` names `nerve-wavering` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:83. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
