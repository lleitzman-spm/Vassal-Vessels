---
type: "place"
id: "place:battle-deploy"
title: "Deploying"
standing: "built"
standing_source: "derived"
source_path: "data/places.json"
source_line: 17
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:battle-deploy"
---

# Deploying

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Before the first tick of fighting. Every unit is placed, faced, put into a formation and given its posture. Nothing is random here and nothing has happened yet — this is the last moment the battle is entirely yours.

*Verified verbatim against `data/places.json`:17 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `battle` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `DEPLOY` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `src/battle/types.ts` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `export type BattlePhase = "DEPLOY" | "BATTLE" | "PURSUIT" | "ENDED";` |

## What it beats, and what beats it

- **Role:** entry

## What you would see on the field

> Two armies standing still, in lines, looking at each other across ground nobody has crossed yet.

*Shelf: `places` in `data/places.json`.*

## Modules

- [[src/battle/types.ts]] — *this page names `BattlePhase`*

## flow

- [[The Day Itself]] — *its `flow` names `battle` by id*

## Backlinks

### flow

- [[The Day Itself]] — *its `entry` names `battle-deploy` by id*

### transition

- [[The lines start forward]] — *its `from` names `battle-deploy` by id*

## Sources this page cites

*Files this page names by path. Again: a citation of the file, nothing more.*

- [[src/battle/types.ts]]

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:17. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
