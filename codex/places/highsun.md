---
type: "place"
id: "place:year-highsun"
title: "Highsun"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 444
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:year-highsun"
---

# Highsun

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Summer, the campaigning season. Everything is at its best — and because everyone knows it, a summer spent idle after a victory costs you standing. The host wanted to be led.

*Verified verbatim against `data/places.json`:444 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `year` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `highsun` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `data/seasons.json` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `"id": "highsun"` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Dry roads, long days, and everyone expecting a war.

*Shelf: `places` in `data/places.json`.*

## Seasons

- [[Highsun (season)]] — *this page names "Highsun" literally*

## flow

- [[The Year]] — *its `flow` names `year` by id*

## Backlinks

### transition

- [[The fields come in]] — *its `from` names `year-highsun` by id*
- [[The roads dry]] — *its `to` names `year-highsun` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:444. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
