---
type: "place"
id: "place:year-wolfmoon"
title: "Wolfmoon"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 466
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:year-wolfmoon"
---

# Wolfmoon

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Winter. Marching is slow and exhausting, forage is near nothing so the granary drains fast, and nobody wants to be out. Winter campaigns are how kingdoms are surprised — and how armies starve.

*Verified verbatim against `data/places.json`:466 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `year` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | `wolfmoon` |
| `sourcePath` | Where this state's literal value actually lives in the tree. A place with no sourcePath is a state the DESIGN names but the code has not yet given a single named value — and it will honestly read as `proposed` until it does. | `data/seasons.json` |
| `quote` | A verbatim line from sourcePath proving the state is real. No quote, no object. | `"id": "wolfmoon"` |

## What it beats, and what beats it

- **Role:** normal

## What you would see on the field

> Short days, hard ground, and nothing at all to eat off the land.

*Shelf: `places` in `data/places.json`.*

## Seasons

- [[Wolfmoon (season)]] — *this page names "Wolfmoon" literally*

## flow

- [[The Year]] — *its `flow` names `year` by id*

## Backlinks

### transition

- [[The cold comes]] — *its `to` names `year-wolfmoon` by id*
- [[The year turns]] — *its `from` names `year-wolfmoon` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:466. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
