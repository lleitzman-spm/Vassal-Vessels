---
type: "place"
id: "place:order-issued"
title: "Issued"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/places.json"
source_line: 334
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "place:order-issued"
---

# Issued

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You have decided. The order exists and has gone nowhere. How many you may have in flight at once is set by your Marshal — which is the court reaching directly onto the battlefield and putting a hand on the controls.

*Verified verbatim against `data/places.json`:334 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `flow` | Which machine this place belongs to (data/flows.json). | `order` |
| `state` | The LITERAL value in the engine's own code, where one exists. This is what makes a place findable rather than invented — an empty string means the state is real in the design but has no single named value in the tree yet. | *unset* |

## What it beats, and what beats it

- **Role:** entry

## What you would see on the field

> A commander turning to speak to a rider.

*Shelf: `places` in `data/places.json`.*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## Backlinks

### flow

- [[An Order's Journey]] — *its `entry` names `order-issued` by id*

### transition

- [[The courier goes]] — *its `from` names `order-issued` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/places.json`:334. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
