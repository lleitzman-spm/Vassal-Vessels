---
type: "unit"
id: "unit:spearmen"
title: "Levy Spearmen"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/units.json"
source_line: 48
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "unit:spearmen"
---

# Levy Spearmen

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Farmers with a season of training, a spear and a round shield, owed by their manor for forty days a year. Their whole value is BRACING: butt in the earth, point level, and a horse will not come on. Seven metres of refusal is just barely enough to stop a gallop — and it is multiplied by how steady and how neat they are, so frightened fyrdmen with a wobbly line are ridden straight over. When the press comes at six seconds they have a bad time; a spear line wants to hit, hurt, and step back out before then.

*Verified verbatim against `data/units.json`:48 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `class` | *undocumented in the file's own `fields` block* | `FOOT` |
| `raisedBy` | Which holding types (data/holdings.json) or bargains (data/obligations.json) raise this unit. | `manor` |
| `typicalMen` | *undocumented in the file's own `fields` block* | `60` |
| `marchSpeed` | Leagues per day on a good summer road. | `8` |
| `dailyWear` | Fatigue points gained per day on the march. | `3` |
| `massPerManKg` | Man plus kit — or horse plus man plus kit. Only matters for shock. | `84` |
| `armour` | *undocumented in the file's own `fields` block* | `padded` |
| `shield` | *undocumented in the file's own `fields` block* | `round` |
| `primaryWeapon` | *undocumented in the file's own `fields` block* | `spear` |
| `sidearm` | *undocumented in the file's own `fields` block* | `long-knife` |
| `skill` | 0-100 individual fighting quality; feeds to-hit. | `25` |
| `drillBase` | 0-100 formation quality; the Host's per-unit drill overrides it. | `40` |
| `refusalBase` | 0-1000. How strongly this unit's set weapons make a charging horse refuse — ONLY when braced, only in the front arc. | `600` |
| `refusalRangeMm` | How far ahead the refusal reaches. Longer weapons project further. | `7000` |
| `turnMulPermille` | Multiplier on the base turn rate. | `950` |
| `fatigueMulPermille` | How fast this unit tires, from armour weight and the work. | `1080` |
| `defaultFormation` | *undocumented in the file's own `fields` block* | `close` |
| `allowedFormations` | *undocumented in the file's own `fields` block* | `close`, `loose`, `shieldwall`, `column` |
| `traits` | *undocumented in the file's own `fields` block* | `CanBrace` |
| `musterCost` | Coin per battle, for the court's arithmetic. Not a battle number. | `55` |

## What it beats, and what beats it

- **Role:** The backbone of every army you will ever field. Braced, they stop horses. Unbraced, they are men with sticks.
- **Countered by:** Swordsmen once the press starts — their knives are poor. Arrows, because a padded jack is not much. Anything that catches them before they brace.

## What you would see on the field

> Braced: butts in the earth, points level, absolutely still. Unbraced: shuffling, points wavering.

*Shelf: `units` in `data/units.json`.*

## Keywords

- [[Can Brace]] — *its `traits` names `CanBrace` by id*

## Equipment

- [[Long Knife]] — *its `sidearm` names `long-knife` by id*
- [[Padded Jack]] — *its `armour` names `padded` by id; this page names "Padded Jack" literally*
- [[Round Shield]] — *its `shield` names `round` by id; this page names "Round Shield" literally*
- [[Spear]] — *its `primaryWeapon` names `spear` by id*

## Formations

- [[Close Order]] — *its `allowedFormations` names `close` by id; its `defaultFormation` names `close` by id*
- [[Column of March]] — *its `allowedFormations` names `column` by id*
- [[Open Order]] — *its `allowedFormations` names `loose` by id*
- [[Shield Wall]] — *its `allowedFormations` names `shieldwall` by id*

## Backlinks

### Worked examples

- [[The Host of Aldmarch]] — *fields a unit of type `spearmen`*

---

*Generated by `tools/codex/emit.mjs` from `data/units.json`:48. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
