---
type: "quirk"
id: "quirk:marched-through-the-night"
title: "Marched Through the Night"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/quirks.json"
source_line: 257
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "quirk:marched-through-the-night"
---

# Marched Through the Night

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

They arrived in time, which is the good news. They arrived exhausted, which is the rest of it. Sometimes that is the right trade and the game will not tell you which times.

*Verified verbatim against `data/quirks.json`:257 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `appliesTo` | captain, contingent or unit — where the court hangs it and where the battle reads it. | `unit` |
| `from` | What in the record book produces it. Every quirk is a consequence of something you did or failed to do. | `a forced march ordered to make the muster day` |
| `battleEffect` | What this canon's engine does, exactly, in the engine's own units. Another engine may do something else, or nothing. | `Starts the battle with fatigue no lower than 550,000 and morale ceiling 60,000 lower.` |

*Shelf: `quirks` in `data/quirks.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/quirks.json`:257. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
