---
type: "obligation"
id: "obligation:castle-guard"
title: "Castle-guard"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/obligations.json"
source_line: 28
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "obligation:castle-guard"
---

# Castle-guard

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Some of a lord's men are not soldiers you can call — they are standing on his walls, and if they come down, his home burns. A frontier fort needs forty men just to go on existing. That is why the lord with the best cavalry in your realm can only bring you ten riders, and why appointing a Constable feels like magic.

*Verified verbatim against `data/obligations.json`:28 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `createdBy` | *undocumented in the file's own `fields` block* | `holding a castle or a march-fort` |
| `readOnTheScreen` | *undocumented in the file's own `fields` block* | `Every holding's roll shows its garrison held back in grey, so the men you cannot have are named as clearly as the men you can.` |

*Shelf: `obligations` in `data/obligations.json`.*

## Holdings

- [[Castle]] — *this page names "Castle" literally*
- [[March-fort]] — *this page names "March-fort" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/obligations.json`:28. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
