---
type: "obligation"
id: "obligation:feudal-service"
title: "Feudal service"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/obligations.json"
source_line: 24
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "obligation:feudal-service"
---

# Feudal service

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

When you give a house land, you write down how many men they owe you and for how many days. That written line is the single most important number in your realm. If you want more soldiers permanently, the cleanest way in the game is to grant more land and write a bigger number in the charter — and unlike money, you can never take it back cheaply.

*Verified verbatim against `data/obligations.json`:24 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `createdBy` | What brings the obligation into being. Nothing here is a standing rule of the world — every one of them starts as an act somebody performed. | `an Enfeoff record — the grant of land IS the contract, and the terms are written into it` |
| `revokedBy` | What ends it. Removal of the record IS revocation; there is no separate switch. | `removing that record, or an Attaint` |
| `k` | The constant path in `data/constants.json` that governs it, so the number lives in the numbers file rather than as a literal in the code. | `court.obligation` |
| `theTrap` | The part players consistently miss until it costs them. Written down because a manual that only lists the rules has not actually taught the game. | `Calling the same house twice in a year drinks a cup that is already half empty. Ask a third time and a loyal man is refusing you in public — and the game drew you the empty cup before you sent every one of those letters.` |

*Shelf: `obligations` in `data/obligations.json`.*

## Governing numbers

- [[Court: Obligation]] — *this page names `court.obligation` literally*

## Backlinks

### Quirks

- [[His Forty Days Are Nearly Done]] — *this page names "Feudal service" literally*

---

*Generated by `tools/codex/emit.mjs` from `data/obligations.json`:24. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
