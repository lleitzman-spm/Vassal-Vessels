---
type: "obligation"
id: "obligation:contract"
title: "The mercenary contract"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/obligations.json"
source_line: 52
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "obligation:contract"
---

# The mercenary contract

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Companies of professional soldiers will fight for anybody who pays. They are good, they are experienced, and they do not care about your family at all. Miss one payment and their willingness to turn on you climbs twelve points a season, and their courage loses its floor entirely.

*Verified verbatim against `data/obligations.json`:52 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `createdBy` | *undocumented in the file's own `fields` block* | `a Contract record` |
| `k` | *undocumented in the file's own `fields` block* | `court.coin` |
| `theTrap` | *undocumented in the file's own `fields` block* | `Every lord who sat at home while you hired foreigners takes a grudge of 4, and the Church takes 6 off your legitimacy. Money into soldiers is the one conversion that does not need anybody to like you — and it makes fewer people like you every time you use it.` |

*Shelf: `obligations` in `data/obligations.json`.*

## Governing numbers

- [[Court: Coin]] — *this page names `court.coin` literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/obligations.json`:52. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
