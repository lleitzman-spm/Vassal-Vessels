---
type: "token"
id: "token:strength"
title: "Men Standing"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/tokens.json"
source_line: 133
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "token:strength"
---

# Men Standing

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

How many are left. Note that captured men leave the tally exactly like dead ones — from inside the battle they are equally gone — and only the aftermath sorts out which is which. That sorting is where a good physician earns his stipend, because a wounded man who lives is a man you have next season.

*Verified verbatim against `data/tokens.json`:133 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `heldBy` | What holds it — a unit, a contingent, an army, a house, the crown. | `a unit` |
| `flow` | The machine this token most belongs to (data/flows.json), where one owns it. | `nerve` |
| `range` | The values it runs between, and in what units. Battle bars run 0..1,000,000 inside the machine and are shown divided down; court scores run 0..100. | `0 to whatever mustered` |
| `stored` | TRUE if it is real mutable state that is written and read back. FALSE if it is a READING — computed fresh from the records every time, never stored anywhere. This one field explains most of the difference between how the battle works and how the court works. | yes |
| `gainedBy` | What puts it up. | `nothing` |
| `spentBy` | What takes it down. | `being killed, and being captured — which come out of the same tally` |
| `refills` | What, if anything, restores it — and `never` is a real and common answer. | `never` |
| `cites` | The constant paths that govern it, dotted, exactly as in data/constants.json. | `battle.army.destroyedStrengthPermille`, `battle.aftermath.woundedPermilleOfCasualtiesBase`, `battle.aftermath.woundedPermillePerPhysician` |

*Shelf: `tokens` in `data/tokens.json`.*

## Governing numbers

- [[Battle: Aftermath]] — *its `cites` names `battle.aftermath.woundedPermilleOfCasualtiesBase` by id; its `cites` names `battle.aftermath.woundedPermillePerPhysician` by id; +3 more*
- [[Battle: Army]] — *its `cites` names `battle.army.destroyedStrengthPermille` by id; this page names `battle.army` literally; +1 more*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/tokens.json`:133. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
