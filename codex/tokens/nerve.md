---
type: "token"
id: "token:morale"
title: "Nerve"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/tokens.json"
source_line: 19
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "token:morale"
---

# Nerve

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The bar the whole game is about. What makes it interesting is the CEILING: a unit's nerve can never rise above a limit set before the battle by the court — by how much the men believe in the cause, how well they were fed, how far they marched. You do not fix a low ceiling on the field. You fixed it, or failed to, months ago.

*Verified verbatim against `data/tokens.json`:19 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `heldBy` | What holds it — a unit, a contingent, an army, a house, the crown. | `a unit` |
| `flow` | The machine this token most belongs to (data/flows.json), where one owns it. | `nerve` |
| `range` | The values it runs between, and in what units. Battle bars run 0..1,000,000 inside the machine and are shown divided down; court scores run 0..100. | `0 to 1,000,000 in the machine; shown as 0-100` |
| `stored` | TRUE if it is real mutable state that is written and read back. FALSE if it is a READING — computed fresh from the records every time, never stored anywhere. This one field explains most of the difference between how the battle works and how the court works. | yes |
| `gainedBy` | What puts it up. | `winning, killing, a captain exhorting, steady friends nearby, the Tide running your way` |
| `spentBy` | What takes it down. | `casualties, being shot at, friends breaking nearby, losing the banner, being flanked` |
| `refills` | What, if anything, restores it — and `never` is a real and common answer. | `slowly, while out of contact — and never above the unit's ceiling` |
| `cites` | The constant paths that govern it, dotted, exactly as in data/constants.json. | `battle.morale.steadyThreshold`, `battle.morale.shakenThreshold`, `battle.morale.breakThreshold`, `battle.morale.pCasualtyPerKillScaled`, `battle.morale.pMissileMultiplierPermille` |

*Shelf: `tokens` in `data/tokens.json`.*

## Governing numbers

- [[Battle: Morale]] — *its `cites` names `battle.morale.breakThreshold` by id; its `cites` names `battle.morale.pCasualtyPerKillScaled` by id; +9 more*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/tokens.json`:19. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
