---
type: "token"
id: "token:cohesion"
title: "Cohesion"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/tokens.json"
source_line: 73
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "token:cohesion"
---

# Cohesion

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

How neat the formation is, and it multiplies everything that depends on the line being a line — refusal against cavalry above all. A ragged spear line braces badly and gets ridden over. This is why running your spearmen into position and bracing them the same second does not save them: they arrive with the cohesion spent.

*Verified verbatim against `data/tokens.json`:73 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `heldBy` | What holds it — a unit, a contingent, an army, a house, the crown. | `a unit` |
| `flow` | The machine this token most belongs to (data/flows.json), where one owns it. | `pace` |
| `range` | The values it runs between, and in what units. Battle bars run 0..1,000,000 inside the machine and are shown divided down; court scores run 0..100. | `0 to 1,000,000 in the machine; shown as 0-100` |
| `stored` | TRUE if it is real mutable state that is written and read back. FALSE if it is a READING — computed fresh from the records every time, never stored anywhere. This one field explains most of the difference between how the battle works and how the court works. | yes |
| `gainedBy` | What puts it up. | `standing still, good drill, time` |
| `spentBy` | What takes it down. | `moving fast, turning, rough ground, taking a charge, and breaking (which throws all of it away at once)` |
| `refills` | What, if anything, restores it — and `never` is a real and common answer. | `by standing still — the most underrated order in the game` |
| `cites` | The constant paths that govern it, dotted, exactly as in data/constants.json. | `battle.cohesion.recoverStandingPerTick`, `battle.cohesion.drillRecoveryBonusPerPoint`, `battle.cohesion.drainRunning`, `battle.cohesion.drainCharging`, `battle.cohesion.startBase`, `battle.cohesion.startPerPoint` |

*Shelf: `tokens` in `data/tokens.json`.*

## Governing numbers

- [[Battle: Cohesion]] — *its `cites` names `battle.cohesion.drainCharging` by id; its `cites` names `battle.cohesion.drainRunning` by id; +11 more*
- [[Battle: Fatigue]] — *this page names `drainCharging`; this page names `drainRunning`*

## Orders

- [[Charge]] — *this page names "Charge" literally*

## flow

- [[A Unit's Pace]] — *its `flow` names `pace` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/tokens.json`:73. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
