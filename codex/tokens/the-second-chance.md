---
type: "token"
id: "token:rallies"
title: "The Second Chance"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/tokens.json"
source_line: 150
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "token:rallies"
---

# The Second Chance

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The smallest token in the game and one of the most decisive. Every unit may come back from a rout exactly once. A commander who knows which of his units have spent theirs is playing a different game from one who does not — because a rallied unit looks like a unit and has nothing behind it.

*Verified verbatim against `data/tokens.json`:150 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `heldBy` | What holds it — a unit, a contingent, an army, a house, the crown. | `a unit` |
| `flow` | The machine this token most belongs to (data/flows.json), where one owns it. | `nerve` |
| `range` | The values it runs between, and in what units. Battle bars run 0..1,000,000 inside the machine and are shown divided down; court scores run 0..100. | `exactly one, per battle` |
| `stored` | TRUE if it is real mutable state that is written and read back. FALSE if it is a READING — computed fresh from the records every time, never stored anywhere. This one field explains most of the difference between how the battle works and how the court works. | yes |
| `gainedBy` | What puts it up. | `nothing` |
| `spentBy` | What takes it down. | `rallying, once` |
| `refills` | What, if anything, restores it — and `never` is a real and common answer. | `never, within a battle` |
| `cites` | The constant paths that govern it, dotted, exactly as in data/constants.json. | `battle.morale.ralliesAllowed` |

*Shelf: `tokens` in `data/tokens.json`.*

## Governing numbers

- [[Battle: Morale]] — *its `cites` names `battle.morale.ralliesAllowed` by id; this page names `battle.morale` literally; +1 more*

## flow

- [[A Unit's Nerve]] — *its `flow` names `nerve` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/tokens.json`:150. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
