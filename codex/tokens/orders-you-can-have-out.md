---
type: "token"
id: "token:orders-in-flight"
title: "Orders You Can Have Out"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/tokens.json"
source_line: 165
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "token:orders-in-flight"
---

# Orders You Can Have Out

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The court's most direct hand on the battle's controls, and the clearest single argument for caring about the council table. A vacant Marshalcy means you fight the entire war two contingents at a time; a great Marshal gives you six. It is the same battle, the same army and the same enemy — and one of them you can actually conduct.

*Verified verbatim against `data/tokens.json`:165 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `heldBy` | What holds it — a unit, a contingent, an army, a house, the crown. | `the commander` |
| `flow` | The machine this token most belongs to (data/flows.json), where one owns it. | `order` |
| `range` | The values it runs between, and in what units. Battle bars run 0..1,000,000 inside the machine and are shown divided down; court scores run 0..100. | `two to six, set by the Marshal's seat` |
| `stored` | TRUE if it is real mutable state that is written and read back. FALSE if it is a READING — computed fresh from the records every time, never stored anywhere. This one field explains most of the difference between how the battle works and how the court works. | yes |
| `gainedBy` | What puts it up. | `filling the Marshalcy with a good man who likes you` |
| `spentBy` | What takes it down. | `issuing an order; returned when it resolves` |
| `refills` | What, if anything, restores it — and `never` is a real and common answer. | `as orders complete` |
| `cites` | The constant paths that govern it, dotted, exactly as in data/constants.json. | `battle.command.planSlotsBonusOverOrderCapacity`, `battle.command.plansPerUnit`, `battle.command.hornChannels`, `court.seats.vacantEffect` |

*Shelf: `tokens` in `data/tokens.json`.*

## Governing numbers

- [[Battle: Command]] — *its `cites` names `battle.command.hornChannels` by id; its `cites` names `battle.command.planSlotsBonusOverOrderCapacity` by id; +5 more*
- [[Court: Seats]] — *its `cites` names `court.seats.vacantEffect` by id; this page names `court.seats` literally; +1 more*

## Traits

- [[Command]] — *this page names "Command" literally*

## Seats

- [[The Marshal]] — *this page names "The Marshal" literally*

## flow

- [[An Order's Journey]] — *its `flow` names `order` by id*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/tokens.json`:165. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
