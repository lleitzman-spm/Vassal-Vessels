---
type: "grievance"
id: "grievance:passed-over"
title: "Passed over for a seat"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 67
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:passed-over"
---

# Passed over for a seat

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You gave an office to somebody else. The stronger his claim, the worse it stings — a man whose father held the Marshalcy and who has won two battles for you does not take it well. The sharpest and most common grudge in the game, because there are seven seats and there are always more claimants.

*Verified verbatim against `data/grievances.json`:67 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | How much it pulls, and how that scales. Grievances ADD rather than merge, which is why eleven small ones are a serious problem with no single cause to point at. | `4 per point of his claim, so 4 to 20` |
| `k` | The constant path in `data/constants.json` that governs the weight — because a governing number belongs in the numbers file, never as a literal in the code. | `court.seats.passedOverPerClaimPoint` |
| `causedBy` | The act on the record that creates it. Every grudge in this game began as a specific thing you did on a specific day, and it can always be traced back to that day. | `an Invest record naming somebody else` |
| `settledBy` | What answers it. The grudge is never deleted — the record of your amends is read ALONGSIDE the record of the wrong, which is both more honest and more durable than pretending it never happened. | `wergild`, `grant`, `another seat`, `justice` |

*Shelf: `grievances` in `data/grievances.json`.*

## Governing numbers

- [[Court: Seats]] — *this page names `court.seats` literally; this page names `passedOverPerClaimPoint`*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:67. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
