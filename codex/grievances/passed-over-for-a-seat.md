---
type: "grievance"
id: "grievance:passed-over"
title: "Passed over for a seat"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 30
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:passed-over"
---

# Passed over for a seat

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You gave an office to somebody else. The stronger his claim, the worse it stings — a man whose father held the Marshalcy and who has won two battles for you does not take it well. The sharpest and most common grudge in the game, because there are seven seats and there are always more claimants.

*Verified verbatim against `data/grievances.json`:30 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | *undocumented in the file's own `fields` block* | `4 per point of his claim, so 4 to 20` |
| `k` | *undocumented in the file's own `fields` block* | `court.seats.passedOverPerClaimPoint` |
| `causedBy` | *undocumented in the file's own `fields` block* | `an Invest record naming somebody else` |
| `settledBy` | *undocumented in the file's own `fields` block* | `wergild`, `grant`, `another seat`, `justice` |

*Shelf: `grievances` in `data/grievances.json`.*

## Governing numbers

- [[Court: Seats]] — *this page names `court.seats` literally; this page names `passedOverPerClaimPoint`*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:30. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
