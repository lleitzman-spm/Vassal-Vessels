---
type: "grievance"
id: "grievance:unseated"
title: "The seat left empty"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 81
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:unseated"
---

# The seat left empty

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Leaving a chair empty is not a way of avoiding the argument. Every man who wanted it gets slightly angrier every year it stands there being nobody's. Small, slow, and the reason 'do nothing' is never free.

*Verified verbatim against `data/grievances.json`:81 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | How much it pulls, and how that scales. Grievances ADD rather than merge, which is why eleven small ones are a serious problem with no single cause to point at. | `1 per year vacant, capped at 8` |
| `k` | The constant path in `data/constants.json` that governs the weight — because a governing number belongs in the numbers file, never as a literal in the code. | `court.seats.unseatedPerYear` |
| `causedBy` | The act on the record that creates it. Every grudge in this game began as a specific thing you did on a specific day, and it can always be traced back to that day. | `not filling a seat he has a claim on` |
| `settledBy` | What answers it. The grudge is never deleted — the record of your amends is read ALONGSIDE the record of the wrong, which is both more honest and more durable than pretending it never happened. | `granting him the seat`, `wergild` |

*Shelf: `grievances` in `data/grievances.json`.*

## Governing numbers

- [[Court: Seats]] — *this page names `court.seats` literally; this page names `unseatedPerYear`*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:81. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
