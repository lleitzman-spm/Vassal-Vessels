---
type: "grievance"
id: "grievance:unseated"
title: "The seat left empty"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 39
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:unseated"
---

# The seat left empty

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Leaving a chair empty is not a way of avoiding the argument. Every man who wanted it gets slightly angrier every year it stands there being nobody's. Small, slow, and the reason 'do nothing' is never free.

*Verified verbatim against `data/grievances.json`:39 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | *undocumented in the file's own `fields` block* | `1 per year vacant, capped at 8` |
| `k` | *undocumented in the file's own `fields` block* | `court.seats.unseatedPerYear` |
| `causedBy` | *undocumented in the file's own `fields` block* | `not filling a seat he has a claim on` |
| `settledBy` | *undocumented in the file's own `fields` block* | `granting him the seat`, `wergild` |

*Shelf: `grievances` in `data/grievances.json`.*

## Governing numbers

- [[Court: Seats]] — *this page names `court.seats` literally; this page names `unseatedPerYear`*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:39. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
