---
type: "grievance"
id: "grievance:taxed-hard"
title: "Taxed beyond custom"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 133
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:taxed-hard"
---

# Taxed beyond custom

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Ordinary taxes are ordinary. Ask for more than that twice and people start counting; three times and you are funding a war against the consequences of funding the war.

*Verified verbatim against `data/grievances.json`:133 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | *undocumented in the file's own `fields` block* | `3 per extraordinary levy` |
| `causedBy` | *undocumented in the file's own `fields` block* | `a Tax record marked extraordinary` |
| `settledBy` | *undocumented in the file's own `fields` block* | `a year without one`, `wergild` |

*Shelf: `grievances` in `data/grievances.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:133. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
