---
type: "grievance"
id: "grievance:justice-denied"
title: "Justice denied"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 141
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:justice-denied"
---

# Justice denied

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

He rode to your court with a complaint and you sent him home without hearing it. Turning a petition away costs no days at all, which is exactly why players do it, which is exactly why it is on this list.

*Verified verbatim against `data/grievances.json`:141 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | *undocumented in the file's own `fields` block* | `8` |
| `causedBy` | *undocumented in the file's own `fields` block* | `turning away his Petition at the crown's gate` |
| `settledBy` | *undocumented in the file's own `fields` block* | `hearing it`, `a judgement in his favour` |

*Shelf: `grievances` in `data/grievances.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:141. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
