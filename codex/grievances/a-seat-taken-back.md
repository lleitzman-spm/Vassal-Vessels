---
type: "grievance"
id: "grievance:seat-taken-back"
title: "A seat taken back"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 48
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:seat-taken-back"
---

# A seat taken back

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The longer a family has held an office, the more they think of it as theirs. After sixteen years, taking it back is not a personnel decision, it is a confiscation — and every other house in the realm files it under 'things the crown does to people'.

*Verified verbatim against `data/grievances.json`:48 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | *undocumented in the file's own `fields` block* | `10 normally, 25 after eight years, 40 after sixteen` |
| `k` | *undocumented in the file's own `fields` block* | `court.seats.customaryGrievance` |
| `causedBy` | *undocumented in the file's own `fields` block* | `removing an Invest record` |
| `settledBy` | *undocumented in the file's own `fields` block* | `restoring it`, `a very large grant` |

*Shelf: `grievances` in `data/grievances.json`.*

## Governing numbers

- [[Court: Seats]] — *this page names `court.seats` literally; this page names `customaryGrievance`*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:48. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
