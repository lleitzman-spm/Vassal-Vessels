---
type: "grievance"
id: "grievance:van-denied"
title: "Denied the place of honour"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 243
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:van-denied"
---

# Denied the place of honour

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You promised him the front of the line — the dangerous, glorious place — and put somebody else there. In a world where a family's whole worth is its reputation, that is a public theft. The battle reports honestly whether the promise was kept, so the court never has to guess.

*Verified verbatim against `data/grievances.json`:243 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | How much it pulls, and how that scales. Grievances ADD rather than merge, which is why eleven small ones are a serious problem with no single cause to point at. | `8` |
| `causedBy` | The act on the record that creates it. Every grudge in this game began as a specific thing you did on a specific day, and it can always be traced back to that day. | `an Aftermath reporting a promised van that was not given` |
| `settledBy` | What answers it. The grudge is never deleted — the record of your amends is read ALONGSIDE the record of the wrong, which is both more honest and more durable than pretending it never happened. | `giving it next time`, `wergild` |

*Shelf: `grievances` in `data/grievances.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:243. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
