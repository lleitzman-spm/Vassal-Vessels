---
type: "grievance"
id: "grievance:mercenaries-preferred"
title: "Foreigners hired over him"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 99
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:mercenaries-preferred"
---

# Foreigners hired over him

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You paid strangers to fight while he sat at home. He hears that as: the crown does not trust me and does not need me. Which becomes, one more step along: the crown will not miss me.

*Verified verbatim against `data/grievances.json`:99 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | *undocumented in the file's own `fields` block* | `4 per campaign he was left out of, capped at 16` |
| `k` | *undocumented in the file's own `fields` block* | `court.willingness.mercenaryScornPerCampaign` |
| `shownAs` | *undocumented in the file's own `fields` block* | `its own line in the willingness tally, not folded into loyalty — so it is counted once` |
| `causedBy` | *undocumented in the file's own `fields` block* | `a Contract record in a campaign he was not summoned to` |
| `settledBy` | *undocumented in the file's own `fields` block* | `summoning him next time`, `wergild` |

*Shelf: `grievances` in `data/grievances.json`.*

## Governing numbers

- [[Court: Willingness]] — *this page names `court.willingness` literally; this page names `mercenaryScornPerCampaign`*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:99. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
