---
type: "grievance"
id: "grievance:mercenaries-preferred"
title: "Foreigners hired over him"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/grievances.json"
source_line: 166
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "grievance:mercenaries-preferred"
---

# Foreigners hired over him

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You paid strangers to fight while he sat at home. He hears that as: the crown does not trust me and does not need me. Which becomes, one more step along: the crown will not miss me.

*Verified verbatim against `data/grievances.json`:166 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `weight` | How much it pulls, and how that scales. Grievances ADD rather than merge, which is why eleven small ones are a serious problem with no single cause to point at. | `4 per campaign he was left out of, capped at 16` |
| `k` | The constant path in `data/constants.json` that governs the weight — because a governing number belongs in the numbers file, never as a literal in the code. | `court.willingness.mercenaryScornPerCampaign` |
| `shownAs` | How the grievance is named to a reader when it surfaces, so a missing wing is a clickable road back to the decision that lost it. | `its own line in the willingness tally, not folded into loyalty — so it is counted once` |
| `causedBy` | The act on the record that creates it. Every grudge in this game began as a specific thing you did on a specific day, and it can always be traced back to that day. | `a Contract record in a campaign he was not summoned to` |
| `settledBy` | What answers it. The grudge is never deleted — the record of your amends is read ALONGSIDE the record of the wrong, which is both more honest and more durable than pretending it never happened. | `summoning him next time`, `wergild` |

*Shelf: `grievances` in `data/grievances.json`.*

## Governing numbers

- [[Court: Willingness]] — *this page names `court.willingness` literally; this page names `mercenaryScornPerCampaign`*

## Backlinks

### transition

- [[Something changes in them]] — *its `consumes` names `mercenaries-preferred` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/grievances.json`:166. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
