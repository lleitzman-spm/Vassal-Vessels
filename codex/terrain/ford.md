---
type: "terrain"
id: "terrain:ford"
title: "Ford"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/terrain.json"
source_line: 125
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "terrain:ford"
---

# Ford

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The one shallow place in the river. Slow, exhausting, in column, at four-tenths cohesion — a unit coming out of a ford is not a unit, it is a queue. Whoever holds a ford fights the enemy a few files at a time however many thousands are waiting behind. Anybody who lets you finish crossing has made a mistake; anybody who hits you halfway across has won.

*Verified verbatim against `data/terrain.json`:125 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `speedMulPermille` | *undocumented in the file's own `fields` block* | `400` |
| `chargeSpeedMulPermille` | *undocumented in the file's own `fields` block* | `300` |
| `cohesionCapPermille` | *undocumented in the file's own `fields` block* | `400` |
| `fatigueMulPermille` | *undocumented in the file's own `fields` block* | `1800` |
| `cover` | *undocumented in the file's own `fields` block* | `0` |
| `forcesColumn` | *undocumented in the file's own `fields` block* | yes |

## What you would see on the field

> A thin dribble of men emerging one at a time. If you are on the far bank with cavalry, this is your moment.

*Shelf: `groundTypes` in `data/terrain.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/terrain.json`:125. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
