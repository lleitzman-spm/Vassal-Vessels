---
type: "terrain"
id: "terrain:marsh"
title: "Marsh"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/terrain.json"
source_line: 79
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "terrain:marsh"
---

# Marsh

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Soft ground's worse cousin. Nothing crosses a marsh quickly and nothing crosses it in good order. Treat it as a wall you are allowed to walk through slowly.

*Verified verbatim against `data/terrain.json`:79 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `speedMulPermille` | *undocumented in the file's own `fields` block* | `450` |
| `chargeSpeedMulPermille` | *undocumented in the file's own `fields` block* | `350` |
| `cohesionCapPermille` | *undocumented in the file's own `fields` block* | `550` |
| `fatigueMulPermille` | *undocumented in the file's own `fields` block* | `2000` |
| `cover` | *undocumented in the file's own `fields` block* | `1` |

## What you would see on the field

> A unit entering as a block and leaving as a queue.

*Shelf: `groundTypes` in `data/terrain.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/terrain.json`:79. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
