---
type: "terrain"
id: "terrain:soft"
title: "Soft Ground and Mud"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/terrain.json"
source_line: 68
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "terrain:soft"
---

# Soft Ground and Mud

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

THE CAVALRY KILLER, and the most important tile in this file. Charge speed halves; shock is speed squared; so a knight charge across mud does a QUARTER of its damage. Meanwhile everybody's wind burns sixty per cent faster, which hurts the men in the heaviest armour most. If you are outnumbered by better troops, find mud and stand behind it.

*Verified verbatim against `data/terrain.json`:68 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `speedMulPermille` | *undocumented in the file's own `fields` block* | `620` |
| `chargeSpeedMulPermille` | *undocumented in the file's own `fields` block* | `500` |
| `cohesionCapPermille` | *undocumented in the file's own `fields` block* | `700` |
| `fatigueMulPermille` | *undocumented in the file's own `fields` block* | `1600` |
| `cover` | *undocumented in the file's own `fields` block* | `0` |

## What you would see on the field

> Horses visibly labouring and armoured men stopping to breathe — the most watchable thing in the game.

*Shelf: `groundTypes` in `data/terrain.json`.*

## Orders

- [[Charge]] — *this page names "Charge" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/terrain.json`:68. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
