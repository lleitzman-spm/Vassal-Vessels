---
type: "terrain"
id: "terrain:road"
title: "Road"
standing: "built"
standing_source: "derived"
source_path: "data/terrain.json"
source_line: 56
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "terrain:road"
---

# Road

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree — a module or a test stands under it.  
> *Derived from the tree — the code is there to be read.*

Fast to march on and it saves your men's legs — but it is narrow, so units on a road are squeezed into column, four men wide and helpless. Roads are for getting there, never for being there.

*Verified verbatim against `data/terrain.json`:56 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `speedMulPermille` | How much it slows ordinary movement, in thousandths. Ground is not scenery — it is the largest thing on the field you did not choose. | `1250` |
| `chargeSpeedMulPermille` | How much it slows a charge specifically. Shock scales with the SQUARE of closing speed, so ground that halves a charge does far more than halve it. | `900` |
| `cohesionCapPermille` | The highest cohesion a unit can hold while standing on it. Broken ground makes a ragged line, and a ragged line braces badly. | `1000` |
| `fatigueMulPermille` | How much faster men tire crossing it. Mud is not a movement penalty; it is an exhaustion tax you pay for the rest of the day. | `850` |
| `cover` | How much protection it gives from missiles. The reason a wood full of archers is a problem that has to be solved rather than shot at. | `0` |
| `forcesColumn` | Whether crossing it breaks a unit into column. A unit in column is transport, not a fighting shape, and catching one is how bridges decide battles. | yes |

## What you would see on the field

> A long thin unit going quickly. Alarming if the enemy is close.

*Shelf: `groundTypes` in `data/terrain.json`.*

## Backlinks

### transition

- [[Charge!]] — *its `consumes` names `road` by id*
- [[The lines start forward]] — *its `consumes` names `road` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/terrain.json`:56. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
