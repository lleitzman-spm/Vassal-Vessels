---
type: "terrain"
id: "terrain:road"
title: "Road"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/terrain.json"
source_line: 45
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "terrain:road"
---

# Road

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Fast to march on and it saves your men's legs — but it is narrow, so units on a road are squeezed into column, four men wide and helpless. Roads are for getting there, never for being there.

*Verified verbatim against `data/terrain.json`:45 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `speedMulPermille` | *undocumented in the file's own `fields` block* | `1250` |
| `chargeSpeedMulPermille` | *undocumented in the file's own `fields` block* | `900` |
| `cohesionCapPermille` | *undocumented in the file's own `fields` block* | `1000` |
| `fatigueMulPermille` | *undocumented in the file's own `fields` block* | `850` |
| `cover` | *undocumented in the file's own `fields` block* | `0` |
| `forcesColumn` | *undocumented in the file's own `fields` block* | yes |

## What you would see on the field

> A long thin unit going quickly. Alarming if the enemy is close.

*Shelf: `groundTypes` in `data/terrain.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/terrain.json`:45. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
