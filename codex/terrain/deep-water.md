---
type: "terrain"
id: "terrain:water"
title: "Deep Water"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/terrain.json"
source_line: 137
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "terrain:water"
---

# Deep Water

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The river either side of the ford. Nobody goes here, which is the useful part: a river anchors a flank, and an anchored flank is the cheapest defence in the game, because a flank attack is worth four frontal ones and a rear attack eight.

*Verified verbatim against `data/terrain.json`:137 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `speedMulPermille` | *undocumented in the file's own `fields` block* | `0` |
| `chargeSpeedMulPermille` | *undocumented in the file's own `fields` block* | `0` |
| `cohesionCapPermille` | *undocumented in the file's own `fields` block* | `0` |
| `fatigueMulPermille` | *undocumented in the file's own `fields` block* | `0` |
| `cover` | *undocumented in the file's own `fields` block* | `0` |
| `impassable` | *undocumented in the file's own `fields` block* | yes |

## What you would see on the field

> A line nobody crosses.

*Shelf: `groundTypes` in `data/terrain.json`.*

## Orders

- [[Attack]] — *this page names "Attack" literally*

## Backlinks

### Terrain

- [[Crag]] — *this page names "Deep Water" literally*

---

*Generated by `tools/codex/emit.mjs` from `data/terrain.json`:137. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
