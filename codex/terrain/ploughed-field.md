---
type: "terrain"
id: "terrain:ploughed"
title: "Ploughed Field"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/terrain.json"
source_line: 57
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "terrain:ploughed"
---

# Ploughed Field

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Furrows and clods. Slower, more tiring, and your ranks will not stay straight across it however well drilled they are.

*Verified verbatim against `data/terrain.json`:57 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `speedMulPermille` | *undocumented in the file's own `fields` block* | `880` |
| `chargeSpeedMulPermille` | *undocumented in the file's own `fields` block* | `800` |
| `cohesionCapPermille` | *undocumented in the file's own `fields` block* | `850` |
| `fatigueMulPermille` | *undocumented in the file's own `fields` block* | `1200` |
| `cover` | *undocumented in the file's own `fields` block* | `0` |

## What you would see on the field

> A block that keeps having to dress itself.

*Shelf: `groundTypes` in `data/terrain.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/terrain.json`:57. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
