---
type: "holding"
id: "holding:mine"
title: "Mine"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/holdings.json"
source_line: 147
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "holding:mine"
---

# Mine

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The richest single thing in the realm. It grows nothing and it raises forty men with picks who are strong, angry and no use whatsoever in a battle line. Miners riot more than anybody else, so a mine is money with a temper.

*Verified verbatim against `data/holdings.json`:147 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `coinPerSeason` | Crowns into the coffer every season, before the Steward's quarter. | `200` |
| `grainPerSeason` | Sacks into the granary every season. A hundred men eat one sack a day. | `0` |
| `garrison` | Men who must stay standing on the walls and can never march. A Constable takes a quarter of them off. | `0` |
| `musterRoll` | The named units this land raises, and how many men each is at full strength. Rows in data/units.json. | `[object Object]` |

*Shelf: `holdingTypes` in `data/holdings.json`.*

## Units

- [[Pick-men]] — *this page names "Pick-men" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/holdings.json`:147. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
