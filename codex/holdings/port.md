---
type: "holding"
id: "holding:port"
title: "Port"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/holdings.json"
source_line: 75
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "holding:port"
---

# Port

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The richest land in the realm, and companies of mercenaries land here looking for work — hiring costs six fewer days if you hold a port, which against a forty-one-day deadline is an enormous amount. Its own soldiers are hard men used to boarding actions in confined spaces, which turns out to be excellent training for the press.

*Verified verbatim against `data/holdings.json`:75 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `coinPerSeason` | Crowns into the coffer every season, before the Steward's quarter. | `180` |
| `grainPerSeason` | Sacks into the granary every season. A hundred men eat one sack a day. | `30` |
| `garrison` | Men who must stay standing on the walls and can never march. A Constable takes a quarter of them off. | `10` |
| `musterRoll` | The named units this land raises, and how many men each is at full strength. Rows in data/units.json. | `[object Object]` |

*Shelf: `holdingTypes` in `data/holdings.json`.*

## Units

- [[Marines]] — *this page names "Marines" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/holdings.json`:75. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
