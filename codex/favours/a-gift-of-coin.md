---
type: "favour"
id: "favour:gift"
title: "A gift of coin"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/favours.json"
source_line: 72
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "favour:gift"
---

# A gift of coin

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Money. It works, it is instant, it is capped at twelve points however much you send, and it evaporates in a couple of years. Use it to fix one specific problem before one specific muster, never to buy a friendship.

*Verified verbatim against `data/favours.json`:72 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `standing` | The part that never fades while the thing it rests on is still true. | `0` |
| `fading` | The part that halves every half-life. | `1 per 50 crowns, capped at 12` |
| `halfLifeYears` | How long the fading part takes to become half of itself. | `2` |
| `causedBy` | The act on the record that creates this favour. Like every grievance, a favour is COMPUTED from a record rather than stored as a score — which is why it can be pointed at, and why it decays honestly. | `a Pay record` |

*Shelf: `favours` in `data/favours.json`.*

## Backlinks

### transition

- [[You make it right]] — *its `consumes` names `gift` by id*
- [[You settle with them]] — *its `consumes` names `gift` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/favours.json`:72. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
