---
type: "favour"
id: "favour:feast"
title: "A feast"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/favours.json"
source_line: 108
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "favour:feast"
---

# A feast

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Twelve days and some money for three points of goodwill with everyone invited. Not much on its own — but a feast is also where grudges get said out loud, so it is the cheapest way to LEARN what people are angry about if you have no Spymaster.

*Verified verbatim against `data/favours.json`:108 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `standing` | The part that never fades while the thing it rests on is still true. | `0` |
| `fading` | The part that halves every half-life. | `3` |
| `halfLifeYears` | How long the fading part takes to become half of itself. | `1` |
| `causedBy` | The act on the record that creates this favour. Like every grievance, a favour is COMPUTED from a record rather than stored as a score — which is why it can be pointed at, and why it decays honestly. | `a Feast record` |

*Shelf: `favours` in `data/favours.json`.*

## Backlinks

### transition

- [[It cools]] — *its `consumes` names `feast` by id*
- [[You settle with them]] — *its `consumes` names `feast` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/favours.json`:108. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
