---
type: "favour"
id: "favour:wardship-given"
title: "A wardship given away"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/favours.json"
source_line: 36
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "favour:wardship-given"
---

# A wardship given away

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You let him draw the income and the soldiers of a child's estate until the child grows up. It costs you money you were collecting and men you were calling, and it buys twenty points of loyalty that never fade while it lasts. One of the best-value gifts in the game and one of the least used.

*Verified verbatim against `data/favours.json`:36 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `standing` | The part that never fades while the thing it rests on is still true. | `20` |
| `fading` | The part that halves every half-life. | `0` |
| `halfLifeYears` | How long the fading part takes to become half of itself. | `0` |
| `causedBy` | The act on the record that creates this favour. Like every grievance, a favour is COMPUTED from a record rather than stored as a score — which is why it can be pointed at, and why it decays honestly. | `granting the wardship of a child's lands to him` |

*Shelf: `favours` in `data/favours.json`.*

## Backlinks

### transition

- [[You make it right]] — *its `consumes` names `wardship-given` by id*
- [[You settle with them]] — *its `consumes` names `wardship-given` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/favours.json`:36. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
