---
type: "favour"
id: "favour:justice-for"
title: "Judgement given in his favour"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/favours.json"
source_line: 63
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "favour:justice-for"
---

# Judgement given in his favour

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

You heard his complaint and ruled for him. Cheap in money, expensive in days, and it makes whoever lost the case angry instead — which is the whole difficulty of being a judge, and the reason the Justiciar's seat is worth more than it looks.

*Verified verbatim against `data/favours.json`:63 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `standing` | The part that never fades while the thing it rests on is still true. | `0` |
| `fading` | The part that halves every half-life. | `10` |
| `halfLifeYears` | How long the fading part takes to become half of itself. | `4` |
| `causedBy` | *undocumented in the file's own `fields` block* | `an Amends record with the judgement method` |

*Shelf: `favours` in `data/favours.json`.*

## Seats

- [[The Justiciar]] — *this page names "The Justiciar" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/favours.json`:63. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
