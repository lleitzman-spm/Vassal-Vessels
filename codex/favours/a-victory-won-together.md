---
type: "favour"
id: "favour:victory-shared"
title: "A victory won together"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/favours.json"
source_line: 90
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "favour:victory-shared"
---

# A victory won together

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Nothing makes people like you like winning. This is the free favour, and it is why a realm that fights well is easier to govern than one that fights carefully.

*Verified verbatim against `data/favours.json`:90 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `standing` | The part that never fades while the thing it rests on is still true. | `0` |
| `fading` | The part that halves every half-life. | `6` |
| `halfLifeYears` | How long the fading part takes to become half of itself. | `2` |
| `causedBy` | The act on the record that creates this favour. Like every grievance, a favour is COMPUTED from a record rather than stored as a score — which is why it can be pointed at, and why it decays honestly. | `an Aftermath with a victory where his contingent was present` |

*Shelf: `favours` in `data/favours.json`.*

## Backlinks

### transition

- [[It goes into the record]] — *its `consumes` names `victory-shared` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/favours.json`:90. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
