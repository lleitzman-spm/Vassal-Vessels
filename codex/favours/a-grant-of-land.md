---
type: "favour"
id: "favour:enfeoffment"
title: "A grant of land"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/favours.json"
source_line: 18
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "favour:enfeoffment"
---

# A grant of land

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The biggest thing you can give anybody: worth 25 the year you give it, settling to 8 that never fades while he holds the land. It is also the only gift that increases the men he owes you, because the grant IS the contract and you write the number into it yourself. Everything else on this list buys you a season. This buys you an army, forever, and you can never really take it back.

*Verified verbatim against `data/favours.json`:18 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `standing` | The part that never fades while the thing it rests on is still true. | `8` |
| `fading` | The part that halves every half-life. | `17` |
| `halfLifeYears` | How long the fading part takes to become half of itself. | `4` |
| `causedBy` | *undocumented in the file's own `fields` block* | `an Enfeoff record` |

*Shelf: `favours` in `data/favours.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/favours.json`:18. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
