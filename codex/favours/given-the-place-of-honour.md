---
type: "favour"
id: "favour:van-given"
title: "Given the place of honour"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/favours.json"
source_line: 99
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "favour:van-given"
---

# Given the place of honour

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The front of the line is where reputations are made. Promising it costs nothing; keeping the promise is worth eight; breaking it is worth minus eight and a public humiliation. The battle reports which happened, so nobody has to take anybody's word for it.

*Verified verbatim against `data/favours.json`:99 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `standing` | The part that never fades while the thing it rests on is still true. | `0` |
| `fading` | The part that halves every half-life. | `8` |
| `halfLifeYears` | How long the fading part takes to become half of itself. | `3` |
| `causedBy` | The act on the record that creates this favour. Like every grievance, a favour is COMPUTED from a record rather than stored as a score — which is why it can be pointed at, and why it decays honestly. | `an Aftermath confirming a promised van was given` |

*Shelf: `favours` in `data/favours.json`.*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/favours.json`:99. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
