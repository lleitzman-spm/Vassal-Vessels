---
type: "favour"
id: "favour:seat"
title: "A seat at court"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/favours.json"
source_line: 27
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "favour:seat"
---

# A seat at court

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Power, income and standing all at once — worth 25 the year you grant it. The catch is on the other side of the ledger: everybody else who wanted it is now angry, and after eight years he starts thinking of the office as his.

*Verified verbatim against `data/favours.json`:27 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `standing` | The part that never fades while the thing it rests on is still true. | `15` |
| `fading` | The part that halves every half-life. | `10` |
| `halfLifeYears` | How long the fading part takes to become half of itself. | `3` |
| `causedBy` | The act on the record that creates this favour. Like every grievance, a favour is COMPUTED from a record rather than stored as a score — which is why it can be pointed at, and why it decays honestly. | `an Invest record` |

*Shelf: `favours` in `data/favours.json`.*

## Backlinks

### transition

- [[You invest someone]] — *its `consumes` names `seat` by id*
- [[You make it right]] — *its `consumes` names `seat` by id*
- [[You settle with them]] — *its `consumes` names `seat` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/favours.json`:27. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
