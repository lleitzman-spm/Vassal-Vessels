---
type: "equipment"
id: "equipment:spear"
title: "Spear"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/equipment.json"
source_line: 41
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "equipment:spear"
---

# Spear

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The commonest weapon there has ever been, for good reason: two ranks fight at once and it out-reaches a sword by more than a metre. For six seconds a spear wall beats swordsmen better than two to one; after six seconds, pressed together with no room to draw back the shaft, the same wall LOSES more than two to one. The whole art of spears: hit, hurt, and get out before the press.

*Verified verbatim against `data/equipment.json`:41 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `reachMm` | How far the point or edge reaches from the man's body. | `2200` |
| `pen` | Penetration 0-100, cubed against armour cubed. | `45` |
| `ratePer1000Ticks` | Killing attempts per man per 50 seconds. | `6` |
| `fightingRanks` | Ranks that can use it at once in the clash. | `2` |
| `pressPenaltyPermille` | Crowd penalty once the press starts. 0 means the weapon is happy in a shoving match. | `350` |
| `shockOnly` | True if it only works at the moment of impact. | no |
| `twoHanded` | True means no shield. | no |

## What you would see on the field

> A hedge of points, two ranks working — then a sudden loss of purpose when the enemy gets inside.

*Shelf: `weapons` in `data/equipment.json`.*

## Backlinks

### Units

- [[Levy Spearmen]] — *its `primaryWeapon` names `spear` by id*
- [[March Riders]] — *its `primaryWeapon` names `spear` by id*
- [[Marines]] — *its `primaryWeapon` names `spear` by id*
- [[Militia Spears]] — *its `primaryWeapon` names `spear` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/equipment.json`:41. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
