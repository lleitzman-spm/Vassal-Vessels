---
type: "equipment"
id: "equipment:bombard-stone"
title: "Bombard Stone"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/equipment.json"
source_line: 175
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "equipment:bombard-stone"
---

# Bombard Stone

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

A stone the size of a man's chest, twice a minute, at an alarmingly vague part of the field. It kills few and terrifies everybody — near three times the fear of an arrow. One shot in sixteen bursts the tube.

*Verified verbatim against `data/equipment.json`:175 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `rangeMm` | *undocumented in the file's own `fields` block* | `340000` |
| `pen` | Penetration 0-100, cubed against armour cubed. | `95` |
| `shotsPer1000Ticks` | *undocumented in the file's own `fields` block* | `2` |
| `ammo` | *undocumented in the file's own `fields` block* | `8` |
| `flightSpeedMmPerTick` | *undocumented in the file's own `fields` block* | `4000` |
| `spreadPermilleOfRange` | *undocumented in the file's own `fields` block* | `220` |
| `moraleMultiplierPermille` | *undocumented in the file's own `fields` block* | `4000` |
| `canShootMoving` | *undocumented in the file's own `fields` block* | no |
| `misfirePermille` | *undocumented in the file's own `fields` block* | `60` |

## What you would see on the field

> One enormous noise and the entire enemy line flinching at once.

*Shelf: `missileWeapons` in `data/equipment.json`.*

## Governing numbers

- [[Battle: Missiles]] — *this page names `moraleMultiplierPermille`*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/equipment.json`:175. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
