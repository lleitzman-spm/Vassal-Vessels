---
type: "equipment"
id: "equipment:hunting-bow"
title: "Hunting Bow"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/equipment.json"
source_line: 121
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "equipment:hunting-bow"
---

# Hunting Bow

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

What a forester carries. Fast to shoot, easy to learn, deadly to men in cloth, and it will not get through mail at any distance worth mentioning.

*Verified verbatim against `data/equipment.json`:121 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `rangeMm` | *undocumented in the file's own `fields` block* | `150000` |
| `pen` | Penetration 0-100, cubed against armour cubed. | `30` |
| `shotsPer1000Ticks` | *undocumented in the file's own `fields` block* | `60` |
| `ammo` | *undocumented in the file's own `fields` block* | `30` |
| `flightSpeedMmPerTick` | *undocumented in the file's own `fields` block* | `2400` |
| `spreadPermilleOfRange` | *undocumented in the file's own `fields` block* | `100` |
| `moraleMultiplierPermille` | *undocumented in the file's own `fields` block* | `1400` |
| `canShootMoving` | *undocumented in the file's own `fields` block* | no |

*Shelf: `missileWeapons` in `data/equipment.json`.*

## Governing numbers

- [[Battle: Missiles]] — *this page names `moraleMultiplierPermille`*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/equipment.json`:121. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
