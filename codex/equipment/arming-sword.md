---
type: "equipment"
id: "equipment:sword"
title: "Arming Sword"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/equipment.json"
source_line: 28
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "equipment:sword"
---

# Arming Sword

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The best weapon in the world for the second minute of a fight and one of the worst for the first six seconds. It cannot reach past a spear; but when the lines are jammed together and the spears are stuck, the sword is home. Note what it does to plate: 38 in 1000. You cannot cut a knight.

*Verified verbatim against `data/equipment.json`:28 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `reachMm` | How far the point or edge reaches from the man's body. | `900` |
| `pen` | Penetration 0-100, cubed against armour cubed. | `30` |
| `ratePer1000Ticks` | Killing attempts per man per 50 seconds. | `8` |
| `fightingRanks` | Ranks that can use it at once in the clash. | `1` |
| `pressPenaltyPermille` | Crowd penalty once the press starts. 0 means the weapon is happy in a shoving match. | `0` |
| `shockOnly` | True if it only works at the moment of impact. | no |
| `twoHanded` | True means no shield. | no |

## What you would see on the field

> Short, close, fast work. This is what winning the press looks like.

*Shelf: `weapons` in `data/equipment.json`.*

## Backlinks

### Units

- [[Crossbowmen]] — *its `primaryWeapon` names `sword` by id*
- [[Free Company Horse]] — *its `sidearm` names `sword` by id*
- [[Free Company Swords]] — *its `primaryWeapon` names `sword` by id; its `sidearm` names `sword` by id*
- [[Knights]] — *its `sidearm` names `sword` by id*
- [[March Riders]] — *its `sidearm` names `sword` by id*
- [[Marines]] — *its `sidearm` names `sword` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/equipment.json`:28. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
