---
type: "equipment"
id: "equipment:long-knife"
title: "Long Knife"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/equipment.json"
source_line: 15
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "equipment:long-knife"
---

# Long Knife

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

A poor man's sidearm. Useless at reach; exactly the right length in the crush where nobody can lift an arm. This is what saves a spearman when his spear becomes a nuisance — and what fails to save a pikeman, because a knife is a knife.

*Verified verbatim against `data/equipment.json`:15 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `reachMm` | How far the point or edge reaches from the man's body. | `350` |
| `pen` | Penetration 0-100, cubed against armour cubed. | `22` |
| `ratePer1000Ticks` | Killing attempts per man per 50 seconds. | `10` |
| `fightingRanks` | Ranks that can use it at once in the clash. | `1` |
| `pressPenaltyPermille` | Crowd penalty once the press starts. 0 means the weapon is happy in a shoving match. | `0` |
| `shockOnly` | True if it only works at the moment of impact. | no |
| `twoHanded` | True means no shield. | no |

## What you would see on the field

> Men drop the shaft and go to the belt.

*Shelf: `weapons` in `data/equipment.json`.*

## Backlinks

### Units

- [[Bowmen]] — *its `sidearm` names `long-knife` by id*
- [[Crossbowmen]] — *its `sidearm` names `long-knife` by id*
- [[Foresters]] — *its `primaryWeapon` names `long-knife` by id; its `sidearm` names `long-knife` by id*
- [[Free Company Pikes]] — *its `sidearm` names `long-knife` by id*
- [[Levy Spearmen]] — *its `sidearm` names `long-knife` by id*
- [[Militia Spears]] — *its `sidearm` names `long-knife` by id*
- [[Pick-men]] — *its `sidearm` names `long-knife` by id*
- [[Relic Bearers]] — *its `primaryWeapon` names `long-knife` by id; its `sidearm` names `long-knife` by id*
- [[The Bombard]] — *its `primaryWeapon` names `long-knife` by id; its `sidearm` names `long-knife` by id*

---

*Generated by `tools/codex/emit.mjs` from `data/equipment.json`:15. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
