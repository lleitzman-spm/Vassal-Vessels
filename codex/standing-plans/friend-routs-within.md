---
type: "standing-plan"
id: "standing-plan:friend-routs-within"
title: "FRIEND_ROUTS_WITHIN"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/standing-plans.json"
source_line: 98
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "standing-plan:friend-routs-within"
---

# FRIEND_ROUTS_WITHIN

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Panic reaches ninety metres and courage only forty-five, so the moment a neighbour breaks is exactly when the next unit along needs to be doing something deliberate rather than drifting backwards. Pair with HOLD_POSITION or BRACE.

*Verified verbatim against `data/standing-plans.json`:98 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `valueUnit` | *undocumented in the file's own `fields` block* | `millimetres` |
| `typicalValue` | *undocumented in the file's own `fields` block* | `80000` |
| `checkedAgainst` | *undocumented in the file's own `fields` block* | `any friendly unit entering ROUTING` |
| `herald` | *undocumented in the file's own `fields` block* | `The Household closed up the instant the militia went.` |

## What you would see on the field

> One unit runs and the unit beside it visibly tightens instead of following.

*Shelf: `triggers` in `data/standing-plans.json`.*

## Obligations

- [[The Household]] — *this page names "The Household" literally*

## Troop sources

- [[The Household (troop-source)]] — *this page names "The Household" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/standing-plans.json`:98. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
