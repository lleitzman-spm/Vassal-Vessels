---
type: "standing-plan"
id: "standing-plan:friend-routs-within"
title: "FRIEND_ROUTS_WITHIN"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/standing-plans.json"
source_line: 163
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "standing-plan:friend-routs-within"
---

# FRIEND_ROUTS_WITHIN

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Panic reaches ninety metres and courage only forty-five, so the moment a neighbour breaks is exactly when the next unit along needs to be doing something deliberate rather than drifting backwards. Pair with HOLD_POSITION or BRACE.

*Verified verbatim against `data/standing-plans.json`:163 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `valueUnit` | What the trigger's number is measured in — millimetres, ticks, men, a share. A trigger with no unit is a number nobody can set sensibly. | `millimetres` |
| `typicalValue` | A sane starting value, so a reader binding a first plan is not guessing. | `80000` |
| `checkedAgainst` | Exactly what the engine measures to decide whether the trigger has fired. Written out because a plan you have misunderstood is worse than no plan. | `any friendly unit entering ROUTING` |
| `herald` | How the fired plan is announced, so a plan that saved you reads as a sentence rather than as a silent absence of disaster. | `The Household closed up the instant the militia went.` |

## What you would see on the field

> One unit runs and the unit beside it visibly tightens instead of following.

*Shelf: `triggers` in `data/standing-plans.json`.*

## Obligations

- [[The Household]] — *this page names "The Household" literally*

## Troop sources

- [[The Household (troop-source)]] — *this page names "The Household" literally*

## Backlinks

### transition

- [[They break]] — *its `consumes` names `FRIEND_ROUTS_WITHIN` by id; this page names "FRIEND_ROUTS_WITHIN" literally*

---

*Generated by `tools/codex/emit.mjs` from `data/standing-plans.json`:163. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
