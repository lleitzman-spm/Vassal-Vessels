---
type: "standing-plan"
id: "standing-plan:morale-below"
title: "MORALE_BELOW"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/standing-plans.json"
source_line: 70
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "standing-plan:morale-below"
---

# MORALE_BELOW

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Fires when a unit's will drops past a line you drew. A unit that withdraws at three hundred can rally; a unit that routs at one hundred and fifty usually does not. This is the cheapest insurance in the game for troops you know are brittle.

*Verified verbatim against `data/standing-plans.json`:70 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `valueUnit` | *undocumented in the file's own `fields` block* | `morale, 0 to 1000 as displayed` |
| `typicalValue` | *undocumented in the file's own `fields` block* | `300` |
| `checkedAgainst` | *undocumented in the file's own `fields` block* | `the unit's own morale` |
| `herald` | *undocumented in the file's own `fields` block* | `The Pick-men of Deepcut pulled out of the line while they were still a unit.` |

## What you would see on the field

> A unit leaves the line while it is still in order. Nothing looks more professional.

*Shelf: `triggers` in `data/standing-plans.json`.*

## Units

- [[Pick-men]] — *this page names "Pick-men" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/standing-plans.json`:70. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
