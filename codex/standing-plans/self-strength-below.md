---
type: "standing-plan"
id: "standing-plan:self-strength-below"
title: "SELF_STRENGTH_BELOW"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/standing-plans.json"
source_line: 80
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "standing-plan:self-strength-below"
---

# SELF_STRENGTH_BELOW

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Half a unit is worth saving. A quarter of a unit is worth nothing to anybody, and its rout will frighten everything within ninety metres on the way past.

*Verified verbatim against `data/standing-plans.json`:80 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `valueUnit` | *undocumented in the file's own `fields` block* | `per mille of the unit's muster strength` |
| `typicalValue` | *undocumented in the file's own `fields` block* | `400` |
| `checkedAgainst` | *undocumented in the file's own `fields` block* | `the unit's own men remaining` |
| `herald` | *undocumented in the file's own `fields` block* | `Down to two men in five, the Men-at-arms of Oakhold came out.` |

*Shelf: `triggers` in `data/standing-plans.json`.*

## Units

- [[Men-at-Arms]] — *this page names "Men-at-Arms" literally*

## Backlinks

*Nothing in the Codex points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run codex:lint` counts these.*

---

*Generated by `tools/codex/emit.mjs` from `data/standing-plans.json`:80. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
