---
type: "standing-plan"
id: "standing-plan:engaged-for-ticks"
title: "ENGAGED_FOR_TICKS"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/standing-plans.json"
source_line: 60
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "standing-plan:engaged-for-ticks"
---

# ENGAGED_FOR_TICKS

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

THE SIX SECONDS, automated. A melee changes character at a hundred and twenty ticks: long weapons drop to one fighting rank and their reach becomes a penalty instead of an advantage. Set this to a hundred and ten on your spearmen with FALL_BACK and they will hit, hurt and get out just before the fight turns against them — a manoeuvre almost nobody can time by hand.

*Verified verbatim against `data/standing-plans.json`:60 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `valueUnit` | *undocumented in the file's own `fields` block* | `ticks of continuous contact` |
| `typicalValue` | *undocumented in the file's own `fields` block* | `110` |
| `checkedAgainst` | *undocumented in the file's own `fields` block* | `the unit's own contact counter` |
| `herald` | *undocumented in the file's own `fields` block* | `The Spears of Greyfen hit, held five seconds, and stepped cleanly back before the press could turn.` |

## What you would see on the field

> The spear line hits, holds five seconds, steps back. Then does it again.

*Shelf: `triggers` in `data/standing-plans.json`.*

## Backlinks

### Writs that specify it

- [[OPEN QUESTIONS]] — *this writ names "ENGAGED_FOR_TICKS" literally*

---

*Generated by `tools/codex/emit.mjs` from `data/standing-plans.json`:60. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
