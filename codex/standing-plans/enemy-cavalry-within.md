---
type: "standing-plan"
id: "standing-plan:enemy-cavalry-within"
title: "ENEMY_CAVALRY_WITHIN"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/standing-plans.json"
source_line: 64
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "standing-plan:enemy-cavalry-within"
---

# ENEMY_CAVALRY_WITHIN

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

The classic pairing, and the first plan most players learn. Bracing takes a second and a half; a charging horse covers thirty metres in that time. You cannot afford to notice and then react — you have to have decided already.

*Verified verbatim against `data/standing-plans.json`:64 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `valueUnit` | What the trigger's number is measured in — millimetres, ticks, men, a share. A trigger with no unit is a number nobody can set sensibly. | `millimetres` |
| `typicalValue` | A sane starting value, so a reader binding a first plan is not guessing. | `100000` |
| `checkedAgainst` | Exactly what the engine measures to decide whether the trigger has fired. Written out because a plan you have misunderstood is worse than no plan. | `the nearest enemy MOUNTED unit's centre` |
| `herald` | How the fired plan is announced, so a plan that saved you reads as a sentence rather than as a silent absence of disaster. | `The Spears of Millrow set their points before the rider could have reached them.` |

## What you would see on the field

> Spears go level with no rider anywhere near. It looks like the unit read the enemy's mind.

*Shelf: `triggers` in `data/standing-plans.json`.*

## Keywords

- [[Mounted]] — *this page names "Mounted" literally*

## Backlinks

### transition

- [[Set!]] — *its `consumes` names `ENEMY_CAVALRY_WITHIN` by id; this page names "ENEMY_CAVALRY_WITHIN" literally*

---

*Generated by `tools/codex/emit.mjs` from `data/standing-plans.json`:64. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
