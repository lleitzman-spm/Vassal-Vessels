---
type: "standing-plan"
id: "standing-plan:enemy-within"
title: "ENEMY_WITHIN"
standing: "proposed"
standing_source: "defaulted"
source_path: "data/standing-plans.json"
source_line: 79
origin: "mined"
generator: "tools/codex/emit.mjs"
aliases:
  - "standing-plan:enemy-within"
---

# ENEMY_WITHIN

> **STANDING — PROPOSED ⚠**  
> NOT BUILT. A design in `data/` with no engine reading it yet. This page may NEVER be cited as evidence that the game plays this way.  
> *NOT backed by anything in the tree yet; this is the compiler's default for a design in `data/` with no engine behind it.*

Pair it with LOOSE_FIRE to make archers hold their arrows until the target is close enough to be worth shooting — which is worth about four times shooting at him now, and is nearly impossible to time by hand across a nine-second ride.

*Verified verbatim against `data/standing-plans.json`:79 on every lint — no quote, no object.*

## The numbers

| field | meaning | value |
|---|---|---|
| `valueUnit` | What the trigger's number is measured in — millimetres, ticks, men, a share. A trigger with no unit is a number nobody can set sensibly. | `millimetres` |
| `typicalValue` | A sane starting value, so a reader binding a first plan is not guessing. | `70000` |
| `checkedAgainst` | Exactly what the engine measures to decide whether the trigger has fired. Written out because a plan you have misunderstood is worse than no plan. | `the nearest enemy unit of any kind` |
| `herald` | How the fired plan is announced, so a plan that saved you reads as a sentence rather than as a silent absence of disaster. | `The bows of Thornbury open at seventy paces, as they were told to.` |

*Shelf: `triggers` in `data/standing-plans.json`.*

## Backlinks

### transition

- [[He reaches the banner]] — *its `consumes` names `ENEMY_WITHIN` by id; this page names "ENEMY_WITHIN" literally*
- [[Set!]] — *its `consumes` names `ENEMY_WITHIN` by id; this page names "ENEMY_WITHIN" literally*

---

*Generated by `tools/codex/emit.mjs` from `data/standing-plans.json`:79. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run codex`).*
